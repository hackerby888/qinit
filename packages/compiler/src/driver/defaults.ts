// Re-exported from @qinit/core/wasm/sizing so the clang backend and this one cannot drift apart.
export { DEFAULT_ARENA_BYTES as DEFAULT_COMPILE_ARENA_SIZE_BYTES, DEFAULT_GTEST_ARENA_BYTES as DEFAULT_GTEST_ARENA_SIZE_BYTES } from "@qinit/core/wasm/sizing";
