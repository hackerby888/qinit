// NestedLoopBreakContinue — ported from test/libsolidity/semanticTests/controlFlow/loops.sol
// Stresses: break and continue at two nesting levels
// Variant: entryOrder=reversed entryShape=direct initStyle=full loopShape=zero placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedLoopBreakContinue2
{
};

struct NestedLoopBreakContinue : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 product;
        uint64 broken;
        uint64 skipped;
        uint64 visited;
    };

    struct Walk_input
    {
        uint64 n;
    };

    struct Walk_output {};

    struct Walk_locals
    {
        uint64 i;
        uint64 j;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Walk)
    {
        for (locals.i = 0; locals.i < 0; locals.i++)
        {
        if (QPI::mod(locals.i, 3ULL) == 2)
        {
        state.mut().skipped++;
        continue;
        }
        for (locals.j = 0; locals.j < 4; locals.j++)
        {
        if (locals.j > locals.i)
        {
        state.mut().broken++;
        break;
        }
        state.mut().visited++;
        state.mut().product += locals.i * 4 + locals.j;
        }
        }
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 visited;
        uint64 skipped;
        uint64 broken;
        uint64 product;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.visited = state.get().visited;
        output.skipped = state.get().skipped;
        output.broken = state.get().broken;
        output.product = state.get().product;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Walk, 1);
    }

    INITIALIZE()
    {
        state.mut().visited = 0;
        state.mut().skipped = 0;
        state.mut().broken = 0;
        state.mut().product = 0;
    }
};
