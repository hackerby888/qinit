// BothArmsWriteDifferentMembers — ported from test/libsolidity/semanticTests/controlFlow/if_else.sol
// Stresses: a branch where each arm writes a different member and neither writes the other's — so a merged or hoisted store shows up as a member that moved when it should not have
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BothArmsWriteDifferentMembers2
{
};

struct BothArmsWriteDifferentMembers : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 untouched;
        uint64 shared;
        uint64 oddPath;
        uint64 evenPath;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 evenPath;
        uint64 oddPath;
        uint64 shared;
        uint64 untouched;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.evenPath = state.get().evenPath;
        output.oddPath = state.get().oddPath;
        output.shared = state.get().shared;
        output.untouched = state.get().untouched;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint64 value;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.value = input.a;
        if (QPI::mod(locals.value, 2ULL) == 0)
        {
        state.mut().evenPath = locals.value;
        state.mut().shared = locals.value * 2;
        }
        else
        {
        state.mut().oddPath = locals.value;
        state.mut().shared = locals.value * 3;
        }

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
