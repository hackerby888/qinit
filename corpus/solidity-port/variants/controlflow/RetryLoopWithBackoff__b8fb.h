// RetryLoopWithBackoff — ported from test/libsolidity/semanticTests/controlFlow/while_loop.sol
// Stresses: a retry loop whose delay doubles each attempt and whose budget is consumed by that delay — the loop stops for two different reasons, and which one fired is recorded
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct RetryLoopWithBackoff2
{
};

struct RetryLoopWithBackoff : public ContractBase
{
    struct Scratch
    {
        uint64 runDelay;
        uint64 runBudget;
        uint64 runTries;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 hitAttemptCap;
        uint64 exhaustedBudget;
        uint64 spent;
        uint64 attempts;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 attempts;
        uint64 spent;
        uint64 exhaustedBudget;
        uint64 hitAttemptCap;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.scratch.runDelay = 1;
        state.mut().inner.scratch.runBudget = input.a;
        state.mut().inner.scratch.runTries = 0;
        while (state.mut().inner.scratch.runBudget >= state.mut().inner.scratch.runDelay && state.mut().inner.scratch.runTries < 8)
        {
        state.mut().inner.scratch.runBudget -= state.mut().inner.scratch.runDelay;
        state.mut().inner.scratch.runDelay = state.mut().inner.scratch.runDelay * 2;
        state.mut().inner.scratch.runTries++;
        }
        state.mut().inner.attempts = state.mut().inner.scratch.runTries;
        state.mut().inner.spent = input.a - state.mut().inner.scratch.runBudget;
        if (state.mut().inner.scratch.runTries == 8)
        {
        state.mut().inner.hitAttemptCap++;
        }
        else
        {
        state.mut().inner.exhaustedBudget++;
        }

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.attempts = state.get().inner.attempts;
        output.spent = state.get().inner.spent;
        output.exhaustedBudget = state.get().inner.exhaustedBudget;
        output.hitAttemptCap = state.get().inner.hitAttemptCap;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
