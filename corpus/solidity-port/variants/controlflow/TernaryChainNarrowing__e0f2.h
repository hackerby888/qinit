// TernaryChainNarrowing — ported from test/libsolidity/semanticTests/expressions/conditional_expression_type.sol
// Stresses: the common type of a three-deep conditional chain
// Port caveat: the result is stored at two widths so a promotion is visible
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct TernaryChainNarrowing2
{
};

struct TernaryChainNarrowing : public ContractBase
{
    struct Scratch
    {
        uint64 pickChosen;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        sint64 signedWide;
        uint8 narrow;
        uint64 wide;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 wide;
        uint8 narrow;
        sint64 signedWide;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().inner.wide;
        output.narrow = state.get().inner.narrow;
        output.signedWide = state.get().inner.signedWide;
        output.calls = state.get().inner.calls;
    }

    struct Pick_input
    {
        uint8 small;
        uint32 medium;
        uint64 large;
    };

    struct Pick_output {};

    PUBLIC_PROCEDURE(Pick)
    {
        state.mut().inner.scratch.pickChosen = input.small > 100 ? (uint64)input.large : (input.medium > 1000 ? (uint64)input.medium : (uint64)input.small);
        state.mut().inner.wide = state.mut().inner.scratch.pickChosen;
        state.mut().inner.narrow = (uint8)(input.small > 100 ? input.large : (input.medium > 1000 ? input.medium : input.small));
        state.mut().inner.signedWide = input.small > 100 ? -(sint64)input.large : (sint64)input.medium;
        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Pick, 1);
    }
};
