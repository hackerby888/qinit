// GuardLadderEightDeep — ported from test/libsolidity/semanticTests/controlFlow/nested_if.sol
// Stresses: eight levels of nested guards with a write at each level
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct GuardLadderEightDeep2
{
};

struct GuardLadderEightDeep : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 bottom;
        uint64 marks;
        uint64 depth;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Descend_input
    {
        uint64 value;
    };

    struct Descend_output {};

    struct Descend_locals
    {
        Descend_input forwardedInput;
        Descend_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Descend)
    {
        locals.forwardedInput = input;
        CALL(DescendBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 depth;
        uint64 marks;
        uint64 bottom;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using DescendBody_input = Descend_input;

    using DescendBody_output = Descend_output;

    struct DescendBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(DescendBody)
    {
        if (input.value > 0)
        {
        state.mut().inner.depth = 1;
        state.mut().inner.marks += 1;
        if (input.value > 10)
        {
        state.mut().inner.depth = 2;
        state.mut().inner.marks += 2;
        if (input.value > 20)
        {
        state.mut().inner.depth = 3;
        state.mut().inner.marks += 3;
        if (input.value > 30)
        {
        state.mut().inner.depth = 4;
        state.mut().inner.marks += 4;
        if (input.value > 40)
        {
        state.mut().inner.depth = 5;
        state.mut().inner.marks += 5;
        if (input.value > 50)
        {
        state.mut().inner.depth = 6;
        state.mut().inner.marks += 6;
        if (input.value > 60)
        {
        state.mut().inner.depth = 7;
        state.mut().inner.marks += 7;
        if (input.value > 70)
        {
        state.mut().inner.depth = 8;
        state.mut().inner.marks += 8;
        state.mut().inner.bottom++;
        }
        }
        }
        }
        }
        }
        }
        }
        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.depth = state.get().inner.depth;
        output.marks = state.get().inner.marks;
        output.bottom = state.get().inner.bottom;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Descend, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.depth = 0;
        state.mut().inner.marks = 0;
        state.mut().inner.bottom = 0;
        state.mut().inner.calls = 0;
    }
};
