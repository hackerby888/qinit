// IfLadderDenseVersusSparse — ported from test/libsolidity/semanticTests/various/switch_statement.sol
// Stresses: a dense arm ladder and a sparse one over one input
// Port caveat: Solidity has no switch statement; both spellings are if-ladders
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct IfLadderDenseVersusSparse2
{
};

struct IfLadderDenseVersusSparse : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 misses;
        uint64 sparseHits;
        uint64 denseHits;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 denseHits;
        uint64 sparseHits;
        uint64 misses;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Dispatch_input
    {
        uint64 selector;
    };

    struct Dispatch_output {};

    struct Dispatch_locals
    {
        Dispatch_input forwardedInput;
        Dispatch_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Dispatch)
    {
        locals.forwardedInput = input;
        CALL(DispatchBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.denseHits = state.get().inner.denseHits;
        output.sparseHits = state.get().inner.sparseHits;
        output.misses = state.get().inner.misses;
        output.calls = state.get().inner.calls;
    }

    using DispatchBody_input = Dispatch_input;

    using DispatchBody_output = Dispatch_output;

    struct DispatchBody_locals
    {
        uint64 before;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(DispatchBody)
    {
        locals.before = state.get().inner.denseHits + state.get().inner.sparseHits;
        if (input.selector == 0)
        {
        state.mut().inner.denseHits += 1;
        }
        else if (input.selector == 1)
        {
        state.mut().inner.denseHits += 2;
        }
        else if (input.selector == 2)
        {
        state.mut().inner.denseHits += 3;
        }
        else if (input.selector == 3)
        {
        state.mut().inner.denseHits += 4;
        }
        else if (input.selector == 4)
        {
        state.mut().inner.denseHits += 5;
        }
        else if (input.selector == 5)
        {
        state.mut().inner.denseHits += 6;
        }
        else if (input.selector == 6)
        {
        state.mut().inner.denseHits += 7;
        }
        else if (input.selector == 7)
        {
        state.mut().inner.denseHits += 8;
        }
        if (input.selector == 1ULL)
        {
        state.mut().inner.sparseHits += 1;
        }
        else if (input.selector == 17ULL)
        {
        state.mut().inner.sparseHits += 2;
        }
        else if (input.selector == 4096ULL)
        {
        state.mut().inner.sparseHits += 3;
        }
        else if (input.selector == 65537ULL)
        {
        state.mut().inner.sparseHits += 4;
        }
        else if (input.selector == 1048576ULL)
        {
        state.mut().inner.sparseHits += 5;
        }
        if (locals.before == state.get().inner.denseHits + state.get().inner.sparseHits)
        {
        state.mut().inner.misses++;
        }
        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Dispatch, 1);
    }
};
