// BreakOutOfThreeLevels — ported from test/libsolidity/semanticTests/controlFlow/return_and_break.sol
// Stresses: three nested loops with a break at the innermost and a flag carrying the exit outward — the pattern C++ needs because it has no labelled break
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BreakOutOfThreeLevels2
{
};

struct BreakOutOfThreeLevels : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
        uint64 runJ;
        uint64 runK;
        uint64 runDone;
        uint64 runTarget;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 completed;
        uint64 broke;
        uint64 foundAt;
        uint64 visited;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 visited;
        uint64 foundAt;
        uint64 broke;
        uint64 completed;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.visited = state.get().inner.visited;
        output.foundAt = state.get().inner.foundAt;
        output.broke = state.get().inner.broke;
        output.completed = state.get().inner.completed;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.scratch.runTarget = QPI::mod(input.a, 64ULL);
        state.mut().inner.scratch.runDone = 0;
        state.mut().inner.visited = 0;
        for (state.mut().inner.scratch.runI = 0; state.mut().inner.scratch.runI < 4 && state.mut().inner.scratch.runDone == 0; state.mut().inner.scratch.runI++)
        {
        for (state.mut().inner.scratch.runJ = 0; state.mut().inner.scratch.runJ < 4 && state.mut().inner.scratch.runDone == 0; state.mut().inner.scratch.runJ++)
        {
        for (state.mut().inner.scratch.runK = 0; state.mut().inner.scratch.runK < 4; state.mut().inner.scratch.runK++)
        {
        state.mut().inner.visited++;
        if (state.mut().inner.scratch.runI * 16 + state.mut().inner.scratch.runJ * 4 + state.mut().inner.scratch.runK == state.mut().inner.scratch.runTarget)
        {
        state.mut().inner.foundAt = state.mut().inner.scratch.runTarget;
        state.mut().inner.scratch.runDone = 1;
        break;
        }
        }
        }
        }
        if (state.mut().inner.scratch.runDone != 0)
        {
        state.mut().inner.broke++;
        }
        else
        {
        state.mut().inner.completed++;
        }

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.visited = 0;
        state.mut().inner.foundAt = 0;
        state.mut().inner.broke = 0;
        state.mut().inner.completed = 0;
        state.mut().inner.calls = 0;
    }
};
