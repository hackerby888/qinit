// TernaryChainNarrowing — ported from test/libsolidity/semanticTests/expressions/conditional_expression_type.sol
// Stresses: the common type of a three-deep conditional chain
// Port caveat: the result is stored at two widths so a promotion is visible
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct TernaryChainNarrowing2
{
};

struct TernaryChainNarrowing : public ContractBase
{
    struct Scratch
    {
        uint64 pickChosen;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        sint64 signedWide;
        uint8 narrow;
        uint64 wide;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 wide;
        uint8 narrow;
        sint64 signedWide;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Pick_input
    {
        uint8 small;
        uint32 medium;
        uint64 large;
    };

    struct Pick_output {};

    struct Pick_locals
    {
        Pick_input forwardedInput;
        Pick_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Pick)
    {
        locals.forwardedInput = input;
        CALL(PickBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.signedWide = state.get().signedWide;
        output.calls = state.get().calls;
    }

    using PickBody_input = Pick_input;

    using PickBody_output = Pick_output;

    PRIVATE_PROCEDURE(PickBody)
    {
        state.mut().scratch.pickChosen = input.small > 100 ? (uint64)input.large : (input.medium > 1000 ? (uint64)input.medium : (uint64)input.small);
        state.mut().wide = state.mut().scratch.pickChosen;
        state.mut().narrow = (uint8)(input.small > 100 ? input.large : (input.medium > 1000 ? input.medium : input.small));
        state.mut().signedWide = input.small > 100 ? -(sint64)input.large : (sint64)input.medium;
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Pick, 1);
    }

    INITIALIZE()
    {
        state.mut().wide = 0;
        state.mut().narrow = 0;
        state.mut().signedWide = 0;
        state.mut().calls = 0;
    }
};
