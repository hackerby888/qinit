// NestedTernaryDispatch — ported from test/libsolidity/semanticTests/expressions/conditional_expression_type.sol
// Stresses: a four-way dispatch written as nested conditionals and again as an if-ladder, with both results stored so a mis-nested arm is a wrong number
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=absent placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedTernaryDispatch2
{
};

struct NestedTernaryDispatch : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 disagreements;
        uint64 viaLadder;
        uint64 viaTernary;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 viaTernary;
        uint64 viaLadder;
        uint64 disagreements;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.viaTernary = state.get().viaTernary;
        output.viaLadder = state.get().viaLadder;
        output.disagreements = state.get().disagreements;
        output.calls = state.get().calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        uint64 value;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.value = input.a;
        state.mut().viaTernary = locals.value < 10 ? 1 : (locals.value < 100 ? 2 : (locals.value < 1000 ? 3 : 4));
        if (locals.value < 10)
        {
        state.mut().viaLadder = 1;
        }
        else if (locals.value < 100)
        {
        state.mut().viaLadder = 2;
        }
        else if (locals.value < 1000)
        {
        state.mut().viaLadder = 3;
        }
        else
        {
        state.mut().viaLadder = 4;
        }
        if (state.get().viaTernary != state.get().viaLadder)
        {
        state.mut().disagreements++;
        }

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
