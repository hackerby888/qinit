// UnrollBoundaryEightIterations — ported from test/libsolidity/semanticTests/controlFlow/loops.sol
// Stresses: a loop whose trip count is one, two, four, eight or nine — the sizes a compiler is most likely to unroll, with a running sum that must not depend on how it was compiled
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct UnrollBoundaryEightIterations2
{
};

struct UnrollBoundaryEightIterations : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
        uint64 runBounded;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 sum;
        uint64 squares;
        uint64 iterations;
        uint64 lastIndex;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 sum;
        uint64 squares;
        uint64 iterations;
        uint64 lastIndex;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.sum = state.get().inner.sum;
        output.squares = state.get().inner.squares;
        output.iterations = state.get().inner.iterations;
        output.lastIndex = state.get().inner.lastIndex;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runBounded = input.a > 32 ? 32 : input.a;
        state.mut().inner.sum = 0;
        state.mut().inner.squares = 0;
        for (state.mut().inner.scratch.runI = 0; state.mut().inner.scratch.runI < state.mut().inner.scratch.runBounded; state.mut().inner.scratch.runI++)
        {
        state.mut().inner.sum += state.mut().inner.scratch.runI + 1;
        state.mut().inner.squares += (state.mut().inner.scratch.runI + 1) * (state.mut().inner.scratch.runI + 1);
        state.mut().inner.lastIndex = state.mut().inner.scratch.runI;
        }
        state.mut().inner.iterations = state.mut().inner.scratch.runBounded;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
