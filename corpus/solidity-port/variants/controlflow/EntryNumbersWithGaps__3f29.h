// EntryNumbersWithGaps — ported from test/libsolidity/semanticTests/functionSelector/function_selector.sol
// Stresses: a sparse registration table
// Port caveat: QPI entry numbers are chosen, not hashed
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct EntryNumbersWithGaps2
{
};

struct EntryNumbersWithGaps : public ContractBase
{
    struct Scratch
    {
        uint64 firstScratch;
        uint64 fifthScratch;
        uint64 ninthScratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 first;
        uint64 fifth;
        uint64 ninth;
        uint64 calls;
        Scratch scratch;
    };

    struct Fifth_input
    {
        uint64 value;
    };

    struct Fifth_output {};

    struct Fifth_locals
    {
        Fifth_input forwardedInput;
        Fifth_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Fifth)
    {
        locals.forwardedInput = input;
        CALL(FifthBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct First_input
    {
        uint64 value;
    };

    struct First_output {};

    struct First_locals
    {
        First_input forwardedInput;
        First_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(First)
    {
        locals.forwardedInput = input;
        CALL(FirstBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Ninth_input
    {
        uint64 value;
    };

    struct Ninth_output {};

    struct Ninth_locals
    {
        Ninth_input forwardedInput;
        Ninth_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Ninth)
    {
        locals.forwardedInput = input;
        CALL(NinthBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 first;
        uint64 fifth;
        uint64 ninth;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using FifthBody_input = Fifth_input;

    using FifthBody_output = Fifth_output;

    PRIVATE_PROCEDURE(FifthBody)
    {
        state.mut().fifth += input.value * 5;
        state.mut().calls++;
    }

    using FirstBody_input = First_input;

    using FirstBody_output = First_output;

    PRIVATE_PROCEDURE(FirstBody)
    {
        state.mut().first += input.value;
        state.mut().calls++;
    }

    using NinthBody_input = Ninth_input;

    using NinthBody_output = Ninth_output;

    PRIVATE_PROCEDURE(NinthBody)
    {
        state.mut().ninth += input.value * 9;
        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.first = state.get().first;
        output.fifth = state.get().fifth;
        output.ninth = state.get().ninth;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 3);
        REGISTER_USER_PROCEDURE(First, 1);
        REGISTER_USER_PROCEDURE(Fifth, 5);
        REGISTER_USER_PROCEDURE(Ninth, 9);
    }

    INITIALIZE()
    {

    }
};
