// GuardLadderEightDeep — ported from test/libsolidity/semanticTests/controlFlow/nested_if.sol
// Stresses: eight levels of nested guards with a write at each level
// Variant: base
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct GuardLadderEightDeep2
{
};

struct GuardLadderEightDeep : public ContractBase
{
    struct StateData
    {
        uint64 depth;
        uint64 marks;
        uint64 bottom;
        uint64 calls;
        uint64 placementGuard;
    };

    struct Descend_input
    {
        uint64 value;
    };

    struct Descend_output {};

    struct Descend_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Descend)
    {
        if (input.value > 0)
        {
        state.mut().depth = 1;
        state.mut().marks += 1;
        if (input.value > 10)
        {
        state.mut().depth = 2;
        state.mut().marks += 2;
        if (input.value > 20)
        {
        state.mut().depth = 3;
        state.mut().marks += 3;
        if (input.value > 30)
        {
        state.mut().depth = 4;
        state.mut().marks += 4;
        if (input.value > 40)
        {
        state.mut().depth = 5;
        state.mut().marks += 5;
        if (input.value > 50)
        {
        state.mut().depth = 6;
        state.mut().marks += 6;
        if (input.value > 60)
        {
        state.mut().depth = 7;
        state.mut().marks += 7;
        if (input.value > 70)
        {
        state.mut().depth = 8;
        state.mut().marks += 8;
        state.mut().bottom++;
        }
        }
        }
        }
        }
        }
        }
        }
        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 depth;
        uint64 marks;
        uint64 bottom;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.depth = state.get().depth;
        output.marks = state.get().marks;
        output.bottom = state.get().bottom;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Descend, 1);
    }

    INITIALIZE()
    {
        state.mut().depth = 0;
        state.mut().marks = 0;
        state.mut().bottom = 0;
        state.mut().calls = 0;
    }
};
