// SwitchFallthrough — ported from test/libsolidity/semanticTests/statements/switch_emulation.sol
// Stresses: switch fall-through against the equivalent if/else ladder
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SwitchFallthrough2
{
};

struct SwitchFallthrough : public ContractBase
{
    struct Scratch
    {
        uint64 classifyS;
        uint64 classifyL;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 viaSwitch;
        uint64 viaLadder;
        uint64 agree;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 viaSwitch;
        uint64 viaLadder;
        uint64 agree;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Classify_input
    {
        uint64 value;
        uint64 unused;
    };

    struct Classify_output {};

    struct Classify_locals
    {
        Classify_input forwardedInput;
        Classify_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Classify)
    {
        locals.forwardedInput = input;
        CALL(ClassifyBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.viaSwitch = state.get().inner.viaSwitch;
        output.viaLadder = state.get().inner.viaLadder;
        output.agree = state.get().inner.agree;
    }

    using ClassifyBody_input = Classify_input;

    using ClassifyBody_output = Classify_output;

    PRIVATE_PROCEDURE(ClassifyBody)
    {
        state.mut().inner.scratch.classifyS = 0;
        switch (input.value)
        {
        case 0:
        state.mut().inner.scratch.classifyS += 1;
        case 1:
        state.mut().inner.scratch.classifyS += 10;
        break;
        case 2:
        state.mut().inner.scratch.classifyS += 100;
        break;
        default:
        state.mut().inner.scratch.classifyS += 1000;
        break;
        }
        state.mut().inner.scratch.classifyL = 0;
        if (input.value == 0)
        {
        state.mut().inner.scratch.classifyL += 11;
        }
        else if (input.value == 1)
        {
        state.mut().inner.scratch.classifyL += 10;
        }
        else if (input.value == 2)
        {
        state.mut().inner.scratch.classifyL += 100;
        }
        else
        {
        state.mut().inner.scratch.classifyL += 1000;
        }
        state.mut().inner.viaSwitch = state.mut().inner.scratch.classifyS;
        state.mut().inner.viaLadder = state.mut().inner.scratch.classifyL;
        state.mut().inner.agree = (state.mut().inner.scratch.classifyS == state.mut().inner.scratch.classifyL) ? 1 : 0;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Classify, 1);
    }

    INITIALIZE()
    {

    }
};
