// GuardedDivisionPaths — ported from test/libsolidity/semanticTests/expressions/division_by_zero.sol
// Stresses: three ways of not dividing by zero — a guard before, QPI::div's own guard, and a substituted divisor — with the three results compared
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct GuardedDivisionPaths2
{
};

struct GuardedDivisionPaths : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 zeroDivisors;
        uint64 allAgree;
        uint64 substituted;
        uint64 builtin;
        uint64 guarded;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 guarded;
        uint64 builtin;
        uint64 substituted;
        uint64 allAgree;
        uint64 zeroDivisors;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        uint64 divisor;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.divisor = input.b;
        if (locals.divisor == 0)
        {
        state.mut().inner.guarded = 0;
        state.mut().inner.zeroDivisors++;
        }
        else
        {
        state.mut().inner.guarded = QPI::div(input.a, locals.divisor);
        }
        state.mut().inner.builtin = QPI::div(input.a, locals.divisor);
        state.mut().inner.substituted = QPI::div(input.a, locals.divisor == 0 ? 1ULL : locals.divisor);
        state.mut().inner.allAgree = state.get().inner.guarded == state.get().inner.builtin ? 1 : 0;

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.guarded = state.get().inner.guarded;
        output.builtin = state.get().inner.builtin;
        output.substituted = state.get().inner.substituted;
        output.allAgree = state.get().inner.allAgree;
        output.zeroDivisors = state.get().inner.zeroDivisors;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
