// NestedLoopBreakContinue — ported from test/libsolidity/semanticTests/controlFlow/loops.sol
// Stresses: break and continue at two nesting levels
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full loopShape=constant placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedLoopBreakContinue2
{
};

struct NestedLoopBreakContinue : public ContractBase
{
    struct Scratch
    {
        uint64 walkI;
        uint64 walkJ;
    };

    struct StateData
    {
        uint64 visited;
        uint64 skipped;
        uint64 broken;
        uint64 product;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Walk_input
    {
        uint64 n;
    };

    struct Walk_output {};

    struct Walk_locals
    {
        Walk_input forwardedInput;
        Walk_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Walk)
    {
        locals.forwardedInput = input;
        CALL(WalkBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 visited;
        uint64 skipped;
        uint64 broken;
        uint64 product;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using WalkBody_input = Walk_input;

    using WalkBody_output = Walk_output;

    PRIVATE_PROCEDURE(WalkBody)
    {
        for (state.mut().scratch.walkI = 0; state.mut().scratch.walkI < 6; state.mut().scratch.walkI++)
        {
        if (QPI::mod(state.mut().scratch.walkI, 3ULL) == 2)
        {
        state.mut().skipped++;
        continue;
        }
        for (state.mut().scratch.walkJ = 0; state.mut().scratch.walkJ < 4; state.mut().scratch.walkJ++)
        {
        if (state.mut().scratch.walkJ > state.mut().scratch.walkI)
        {
        state.mut().broken++;
        break;
        }
        state.mut().visited++;
        state.mut().product += state.mut().scratch.walkI * 4 + state.mut().scratch.walkJ;
        }
        }
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.visited = state.get().visited;
        output.skipped = state.get().skipped;
        output.broken = state.get().broken;
        output.product = state.get().product;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Walk, 1);
    }

    INITIALIZE()
    {
        state.mut().visited = 0;
        state.mut().skipped = 0;
        state.mut().broken = 0;
        state.mut().product = 0;
    }
};
