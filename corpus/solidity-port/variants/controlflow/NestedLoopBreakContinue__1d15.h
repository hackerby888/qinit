// NestedLoopBreakContinue — ported from test/libsolidity/semanticTests/controlFlow/loops.sol
// Stresses: break and continue at two nesting levels
// Variant: entryOrder=reversed entryShape=direct initStyle=empty loopShape=zero placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedLoopBreakContinue2
{
};

struct NestedLoopBreakContinue : public ContractBase
{
    struct Scratch
    {
        uint64 walkI;
        uint64 walkJ;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 visited;
        uint64 skipped;
        uint64 broken;
        uint64 product;
        Scratch scratch;
    };

    struct Walk_input
    {
        uint64 n;
    };

    struct Walk_output {};

    PUBLIC_PROCEDURE(Walk)
    {
        for (state.mut().scratch.walkI = 0; state.mut().scratch.walkI < 0; state.mut().scratch.walkI++)
        {
        if (QPI::mod(state.mut().scratch.walkI, 3ULL) == 2)
        {
        state.mut().skipped++;
        continue;
        }
        for (state.mut().scratch.walkJ = 0; state.mut().scratch.walkJ < 4; state.mut().scratch.walkJ++)
        {
        if (state.mut().scratch.walkJ > state.mut().scratch.walkI)
        {
        state.mut().broken++;
        break;
        }
        state.mut().visited++;
        state.mut().product += state.mut().scratch.walkI * 4 + state.mut().scratch.walkJ;
        }
        }
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 visited;
        uint64 skipped;
        uint64 broken;
        uint64 product;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.visited = state.get().visited;
        output.skipped = state.get().skipped;
        output.broken = state.get().broken;
        output.product = state.get().product;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Walk, 1);
    }

    INITIALIZE()
    {

    }
};
