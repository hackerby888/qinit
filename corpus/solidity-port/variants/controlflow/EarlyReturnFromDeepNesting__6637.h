// EarlyReturnFromDeepNesting — ported from test/libsolidity/semanticTests/controlFlow/return_and_break.sol
// Stresses: returning out of four nested scopes with writes already committed
// Port caveat: the writes before the return must survive
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct EarlyReturnFromDeepNesting2
{
};

struct EarlyReturnFromDeepNesting : public ContractBase
{
    struct Scratch
    {
        uint64 searchI;
        uint64 searchJ;
    };

    struct StateData
    {
        uint64 completed;
        uint64 returnedAt;
        uint64 afterReturn;
        uint64 beforeReturn;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 beforeReturn;
        uint64 afterReturn;
        uint64 returnedAt;
        uint64 completed;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Search_input
    {
        uint64 target;
    };

    struct Search_output
    {
        uint64 found;
    };

    struct Search_locals
    {
        Search_input forwardedInput;
        Search_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Search)
    {
        locals.forwardedInput = input;
        CALL(SearchBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.beforeReturn = state.get().beforeReturn;
        output.afterReturn = state.get().afterReturn;
        output.returnedAt = state.get().returnedAt;
        output.completed = state.get().completed;
    }

    using SearchBody_input = Search_input;

    using SearchBody_output = Search_output;

    PRIVATE_PROCEDURE(SearchBody)
    {
        state.mut().beforeReturn++;
        if (input.target != 0)
        {
        if (input.target < 100)
        {
        for (state.mut().scratch.searchI = 0; state.mut().scratch.searchI < 5; state.mut().scratch.searchI++)
        {
        for (state.mut().scratch.searchJ = 0; state.mut().scratch.searchJ < 5; state.mut().scratch.searchJ++)
        {
        if (state.mut().scratch.searchI * 5 + state.mut().scratch.searchJ == input.target)
        {
        state.mut().returnedAt = state.mut().scratch.searchI * 5 + state.mut().scratch.searchJ;
        output.found = 1;
        return;
        }
        }
        }
        }
        }
        state.mut().afterReturn++;
        state.mut().completed++;
        output.found = 0;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Search, 1);
    }
};
