// IfLadderDenseVersusSparse — ported from test/libsolidity/semanticTests/various/switch_statement.sol
// Stresses: a dense arm ladder and a sparse one over one input
// Port caveat: Solidity has no switch statement; both spellings are if-ladders
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct IfLadderDenseVersusSparse2
{
};

struct IfLadderDenseVersusSparse : public ContractBase
{
    struct Scratch
    {
        uint64 dispatchBefore;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 denseHits;
        uint64 sparseHits;
        uint64 misses;
        uint64 calls;
        Scratch scratch;
    };

    struct Dispatch_input
    {
        uint64 selector;
    };

    struct Dispatch_output {};

    PUBLIC_PROCEDURE(Dispatch)
    {
        state.mut().scratch.dispatchBefore = state.get().denseHits + state.get().sparseHits;
        if (input.selector == 0)
        {
        state.mut().denseHits += 1;
        }
        else if (input.selector == 1)
        {
        state.mut().denseHits += 2;
        }
        else if (input.selector == 2)
        {
        state.mut().denseHits += 3;
        }
        else if (input.selector == 3)
        {
        state.mut().denseHits += 4;
        }
        else if (input.selector == 4)
        {
        state.mut().denseHits += 5;
        }
        else if (input.selector == 5)
        {
        state.mut().denseHits += 6;
        }
        else if (input.selector == 6)
        {
        state.mut().denseHits += 7;
        }
        else if (input.selector == 7)
        {
        state.mut().denseHits += 8;
        }
        if (input.selector == 1ULL)
        {
        state.mut().sparseHits += 1;
        }
        else if (input.selector == 17ULL)
        {
        state.mut().sparseHits += 2;
        }
        else if (input.selector == 4096ULL)
        {
        state.mut().sparseHits += 3;
        }
        else if (input.selector == 65537ULL)
        {
        state.mut().sparseHits += 4;
        }
        else if (input.selector == 1048576ULL)
        {
        state.mut().sparseHits += 5;
        }
        if (state.mut().scratch.dispatchBefore == state.get().denseHits + state.get().sparseHits)
        {
        state.mut().misses++;
        }
        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 denseHits;
        uint64 sparseHits;
        uint64 misses;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.denseHits = state.get().denseHits;
        output.sparseHits = state.get().sparseHits;
        output.misses = state.get().misses;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Dispatch, 1);
    }

    INITIALIZE()
    {
        state.mut().denseHits = 0;
        state.mut().sparseHits = 0;
        state.mut().misses = 0;
        state.mut().calls = 0;
    }
};
