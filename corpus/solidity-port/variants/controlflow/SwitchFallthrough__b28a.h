// SwitchFallthrough — ported from test/libsolidity/semanticTests/statements/switch_emulation.sol
// Stresses: switch fall-through against the equivalent if/else ladder
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=first stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SwitchFallthrough2
{
};

struct SwitchFallthrough : public ContractBase
{
    struct StateData
    {
        uint64 agree;
        uint64 viaLadder;
        uint64 viaSwitch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 viaSwitch;
        uint64 viaLadder;
        uint64 agree;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Classify_input
    {
        uint64 value;
        uint64 unused;
    };

    struct Classify_output {};

    struct Classify_locals
    {
        Classify_input forwardedInput;
        Classify_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Classify)
    {
        locals.forwardedInput = input;
        CALL(ClassifyBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.viaSwitch = state.get().viaSwitch;
        output.viaLadder = state.get().viaLadder;
        output.agree = state.get().agree;
    }

    using ClassifyBody_input = Classify_input;

    using ClassifyBody_output = Classify_output;

    struct ClassifyBody_locals
    {
        uint64 s;
        uint64 l;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(ClassifyBody)
    {
        locals.s = 0;
        switch (input.value)
        {
        case 0:
        locals.s += 1;
        case 1:
        locals.s += 10;
        break;
        case 2:
        locals.s += 100;
        break;
        default:
        locals.s += 1000;
        break;
        }
        locals.l = 0;
        if (input.value == 0)
        {
        locals.l += 11;
        }
        else if (input.value == 1)
        {
        locals.l += 10;
        }
        else if (input.value == 2)
        {
        locals.l += 100;
        }
        else
        {
        locals.l += 1000;
        }
        state.mut().viaSwitch = locals.s;
        state.mut().viaLadder = locals.l;
        state.mut().agree = (locals.s == locals.l) ? 1 : 0;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Classify, 1);
    }
};
