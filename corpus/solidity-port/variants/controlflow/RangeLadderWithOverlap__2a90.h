// RangeLadderWithOverlap — ported from test/libsolidity/semanticTests/various/switch_statement.sol
// Stresses: a range ladder whose arms deliberately overlap, so only the order of the tests decides the answer — and a second ladder written in the opposite order for comparison
// Variant: entryOrder=reversed entryShape=direct initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct RangeLadderWithOverlap2
{
};

struct RangeLadderWithOverlap : public ContractBase
{
    struct Scratch
    {
        uint64 runValue;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 differ;
        uint64 lastMatch;
        uint64 firstMatch;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runValue = input.a;
        if (state.mut().inner.scratch.runValue < 1000)
        {
        state.mut().inner.firstMatch = 1;
        }
        else if (state.mut().inner.scratch.runValue < 100)
        {
        state.mut().inner.firstMatch = 2;
        }
        else if (state.mut().inner.scratch.runValue < 10)
        {
        state.mut().inner.firstMatch = 3;
        }
        else
        {
        state.mut().inner.firstMatch = 4;
        }
        if (state.mut().inner.scratch.runValue < 10)
        {
        state.mut().inner.lastMatch = 3;
        }
        else if (state.mut().inner.scratch.runValue < 100)
        {
        state.mut().inner.lastMatch = 2;
        }
        else if (state.mut().inner.scratch.runValue < 1000)
        {
        state.mut().inner.lastMatch = 1;
        }
        else
        {
        state.mut().inner.lastMatch = 4;
        }
        if (state.get().inner.firstMatch != state.get().inner.lastMatch)
        {
        state.mut().inner.differ++;
        }

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 firstMatch;
        uint64 lastMatch;
        uint64 differ;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.firstMatch = state.get().inner.firstMatch;
        output.lastMatch = state.get().inner.lastMatch;
        output.differ = state.get().inner.differ;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.firstMatch = 0;
        state.mut().inner.lastMatch = 0;
        state.mut().inner.differ = 0;
        state.mut().inner.calls = 0;
    }
};
