// NestedTernaryDispatch — ported from test/libsolidity/semanticTests/expressions/conditional_expression_type.sol
// Stresses: a four-way dispatch written as nested conditionals and again as an if-ladder, with both results stored so a mis-nested arm is a wrong number
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedTernaryDispatch2
{
};

struct NestedTernaryDispatch : public ContractBase
{
    struct Scratch
    {
        uint64 runValue;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 viaTernary;
        uint64 viaLadder;
        uint64 disagreements;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 viaTernary;
        uint64 viaLadder;
        uint64 disagreements;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.scratch.runValue = input.a;
        state.mut().inner.viaTernary = state.mut().inner.scratch.runValue < 10 ? 1 : (state.mut().inner.scratch.runValue < 100 ? 2 : (state.mut().inner.scratch.runValue < 1000 ? 3 : 4));
        if (state.mut().inner.scratch.runValue < 10)
        {
        state.mut().inner.viaLadder = 1;
        }
        else if (state.mut().inner.scratch.runValue < 100)
        {
        state.mut().inner.viaLadder = 2;
        }
        else if (state.mut().inner.scratch.runValue < 1000)
        {
        state.mut().inner.viaLadder = 3;
        }
        else
        {
        state.mut().inner.viaLadder = 4;
        }
        if (state.get().inner.viaTernary != state.get().inner.viaLadder)
        {
        state.mut().inner.disagreements++;
        }

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.viaTernary = state.get().inner.viaTernary;
        output.viaLadder = state.get().inner.viaLadder;
        output.disagreements = state.get().inner.disagreements;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.viaTernary = 0;
        state.mut().inner.viaLadder = 0;
        state.mut().inner.disagreements = 0;
        state.mut().inner.calls = 0;
    }
};
