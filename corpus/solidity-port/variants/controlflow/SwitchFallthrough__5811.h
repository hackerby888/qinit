// SwitchFallthrough — ported from test/libsolidity/semanticTests/statements/switch_emulation.sol
// Stresses: switch fall-through against the equivalent if/else ladder
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SwitchFallthrough2
{
};

struct SwitchFallthrough : public ContractBase
{
    struct Scratch
    {
        uint64 classifyS;
        uint64 classifyL;
    };

    struct StateData
    {
        uint64 viaSwitch;
        uint64 viaLadder;
        uint64 agree;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 viaSwitch;
        uint64 viaLadder;
        uint64 agree;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.viaSwitch = state.get().viaSwitch;
        output.viaLadder = state.get().viaLadder;
        output.agree = state.get().agree;
    }

    struct Classify_input
    {
        uint64 value;
        uint64 unused;
    };

    struct Classify_output {};

    PUBLIC_PROCEDURE(Classify)
    {
        state.mut().scratch.classifyS = 0;
        switch (input.value)
        {
        case 0:
        state.mut().scratch.classifyS += 1;
        case 1:
        state.mut().scratch.classifyS += 10;
        break;
        case 2:
        state.mut().scratch.classifyS += 100;
        break;
        default:
        state.mut().scratch.classifyS += 1000;
        break;
        }
        state.mut().scratch.classifyL = 0;
        if (input.value == 0)
        {
        state.mut().scratch.classifyL += 11;
        }
        else if (input.value == 1)
        {
        state.mut().scratch.classifyL += 10;
        }
        else if (input.value == 2)
        {
        state.mut().scratch.classifyL += 100;
        }
        else
        {
        state.mut().scratch.classifyL += 1000;
        }
        state.mut().viaSwitch = state.mut().scratch.classifyS;
        state.mut().viaLadder = state.mut().scratch.classifyL;
        state.mut().agree = (state.mut().scratch.classifyS == state.mut().scratch.classifyL) ? 1 : 0;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Classify, 1);
    }
};
