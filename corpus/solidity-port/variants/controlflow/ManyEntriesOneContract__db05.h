// ManyEntriesOneContract — ported from test/libsolidity/semanticTests/functionSelector/many_functions.sol
// Stresses: twelve registered entries over eight state members
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=absent placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ManyEntriesOneContract2
{
};

struct ManyEntriesOneContract : public ContractBase
{
    struct Scratch
    {
        uint64 set0Scratch;
        uint64 set1Scratch;
        uint64 set2Scratch;
        uint64 set3Scratch;
        uint64 set4Scratch;
        uint64 set5Scratch;
        uint64 set6Scratch;
        uint64 set7Scratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 slot7;
        uint64 slot6;
        uint64 slot5;
        uint64 slot4;
        uint64 slot3;
        uint64 slot2;
        uint64 slot1;
        uint64 slot0;
        Scratch scratch;
    };

    struct Read0_input {};

    struct Read0_output
    {
        uint64 a;
        uint64 b;
        uint64 calls;
    };

    struct Read0_locals
    {
        Read0_input forwardedInput;
        Read0_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read0)
    {
        CALL(Read0Body, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Read1_input {};

    struct Read1_output
    {
        uint64 a;
        uint64 b;
        uint64 calls;
    };

    struct Read1_locals
    {
        Read1_input forwardedInput;
        Read1_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read1)
    {
        CALL(Read1Body, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Read2_input {};

    struct Read2_output
    {
        uint64 a;
        uint64 b;
        uint64 calls;
    };

    struct Read2_locals
    {
        Read2_input forwardedInput;
        Read2_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read2)
    {
        CALL(Read2Body, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Read3_input {};

    struct Read3_output
    {
        uint64 a;
        uint64 b;
        uint64 calls;
    };

    struct Read3_locals
    {
        Read3_input forwardedInput;
        Read3_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read3)
    {
        CALL(Read3Body, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Set0_input
    {
        uint64 value;
    };

    struct Set0_output {};

    struct Set0_locals
    {
        Set0_input forwardedInput;
        Set0_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set0)
    {
        locals.forwardedInput = input;
        CALL(Set0Body, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Set1_input
    {
        uint64 value;
    };

    struct Set1_output {};

    struct Set1_locals
    {
        Set1_input forwardedInput;
        Set1_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set1)
    {
        locals.forwardedInput = input;
        CALL(Set1Body, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Set2_input
    {
        uint64 value;
    };

    struct Set2_output {};

    struct Set2_locals
    {
        Set2_input forwardedInput;
        Set2_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set2)
    {
        locals.forwardedInput = input;
        CALL(Set2Body, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Set3_input
    {
        uint64 value;
    };

    struct Set3_output {};

    struct Set3_locals
    {
        Set3_input forwardedInput;
        Set3_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set3)
    {
        locals.forwardedInput = input;
        CALL(Set3Body, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Set4_input
    {
        uint64 value;
    };

    struct Set4_output {};

    struct Set4_locals
    {
        Set4_input forwardedInput;
        Set4_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set4)
    {
        locals.forwardedInput = input;
        CALL(Set4Body, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Set5_input
    {
        uint64 value;
    };

    struct Set5_output {};

    struct Set5_locals
    {
        Set5_input forwardedInput;
        Set5_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set5)
    {
        locals.forwardedInput = input;
        CALL(Set5Body, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Set6_input
    {
        uint64 value;
    };

    struct Set6_output {};

    struct Set6_locals
    {
        Set6_input forwardedInput;
        Set6_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set6)
    {
        locals.forwardedInput = input;
        CALL(Set6Body, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Set7_input
    {
        uint64 value;
    };

    struct Set7_output {};

    struct Set7_locals
    {
        Set7_input forwardedInput;
        Set7_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set7)
    {
        locals.forwardedInput = input;
        CALL(Set7Body, locals.forwardedInput, locals.forwardedOutput);
    }

    using Read0Body_input = Read0_input;

    using Read0Body_output = Read0_output;

    PRIVATE_FUNCTION(Read0Body)
    {
        output.a = state.get().slot0;
        output.b = state.get().slot1;
        output.calls = state.get().calls;
    }

    using Read1Body_input = Read1_input;

    using Read1Body_output = Read1_output;

    PRIVATE_FUNCTION(Read1Body)
    {
        output.a = state.get().slot2;
        output.b = state.get().slot3;
        output.calls = state.get().calls;
    }

    using Read2Body_input = Read2_input;

    using Read2Body_output = Read2_output;

    PRIVATE_FUNCTION(Read2Body)
    {
        output.a = state.get().slot4;
        output.b = state.get().slot5;
        output.calls = state.get().calls;
    }

    using Read3Body_input = Read3_input;

    using Read3Body_output = Read3_output;

    PRIVATE_FUNCTION(Read3Body)
    {
        output.a = state.get().slot6;
        output.b = state.get().slot7;
        output.calls = state.get().calls;
    }

    using Set0Body_input = Set0_input;

    using Set0Body_output = Set0_output;

    PRIVATE_PROCEDURE(Set0Body)
    {
        state.mut().scratch.set0Scratch = input.value + 0;
        state.mut().slot0 = state.mut().scratch.set0Scratch;
        state.mut().calls++;
    }

    using Set1Body_input = Set1_input;

    using Set1Body_output = Set1_output;

    PRIVATE_PROCEDURE(Set1Body)
    {
        state.mut().scratch.set1Scratch = input.value + 1;
        state.mut().slot1 = state.mut().scratch.set1Scratch;
        state.mut().calls++;
    }

    using Set2Body_input = Set2_input;

    using Set2Body_output = Set2_output;

    PRIVATE_PROCEDURE(Set2Body)
    {
        state.mut().scratch.set2Scratch = input.value + 2;
        state.mut().slot2 = state.mut().scratch.set2Scratch;
        state.mut().calls++;
    }

    using Set3Body_input = Set3_input;

    using Set3Body_output = Set3_output;

    PRIVATE_PROCEDURE(Set3Body)
    {
        state.mut().scratch.set3Scratch = input.value + 3;
        state.mut().slot3 = state.mut().scratch.set3Scratch;
        state.mut().calls++;
    }

    using Set4Body_input = Set4_input;

    using Set4Body_output = Set4_output;

    PRIVATE_PROCEDURE(Set4Body)
    {
        state.mut().scratch.set4Scratch = input.value + 4;
        state.mut().slot4 = state.mut().scratch.set4Scratch;
        state.mut().calls++;
    }

    using Set5Body_input = Set5_input;

    using Set5Body_output = Set5_output;

    PRIVATE_PROCEDURE(Set5Body)
    {
        state.mut().scratch.set5Scratch = input.value + 5;
        state.mut().slot5 = state.mut().scratch.set5Scratch;
        state.mut().calls++;
    }

    using Set6Body_input = Set6_input;

    using Set6Body_output = Set6_output;

    PRIVATE_PROCEDURE(Set6Body)
    {
        state.mut().scratch.set6Scratch = input.value + 6;
        state.mut().slot6 = state.mut().scratch.set6Scratch;
        state.mut().calls++;
    }

    using Set7Body_input = Set7_input;

    using Set7Body_output = Set7_output;

    PRIVATE_PROCEDURE(Set7Body)
    {
        state.mut().scratch.set7Scratch = input.value + 7;
        state.mut().slot7 = state.mut().scratch.set7Scratch;
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read0, 1);
        REGISTER_USER_FUNCTION(Read1, 2);
        REGISTER_USER_FUNCTION(Read2, 3);
        REGISTER_USER_FUNCTION(Read3, 4);
        REGISTER_USER_PROCEDURE(Set0, 1);
        REGISTER_USER_PROCEDURE(Set1, 2);
        REGISTER_USER_PROCEDURE(Set2, 3);
        REGISTER_USER_PROCEDURE(Set3, 4);
        REGISTER_USER_PROCEDURE(Set4, 5);
        REGISTER_USER_PROCEDURE(Set5, 6);
        REGISTER_USER_PROCEDURE(Set6, 7);
        REGISTER_USER_PROCEDURE(Set7, 8);
    }
};
