// BreakOutOfThreeLevels — ported from test/libsolidity/semanticTests/controlFlow/return_and_break.sol
// Stresses: three nested loops with a break at the innermost and a flag carrying the exit outward — the pattern C++ needs because it has no labelled break
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BreakOutOfThreeLevels2
{
};

struct BreakOutOfThreeLevels : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
        uint64 runJ;
        uint64 runK;
        uint64 runDone;
        uint64 runTarget;
    };

    struct StateData
    {
        uint64 calls;
        uint64 completed;
        uint64 broke;
        uint64 foundAt;
        uint64 visited;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runTarget = QPI::mod(input.a, 64ULL);
        state.mut().scratch.runDone = 0;
        state.mut().visited = 0;
        for (state.mut().scratch.runI = 0; state.mut().scratch.runI < 4 && state.mut().scratch.runDone == 0; state.mut().scratch.runI++)
        {
        for (state.mut().scratch.runJ = 0; state.mut().scratch.runJ < 4 && state.mut().scratch.runDone == 0; state.mut().scratch.runJ++)
        {
        for (state.mut().scratch.runK = 0; state.mut().scratch.runK < 4; state.mut().scratch.runK++)
        {
        state.mut().visited++;
        if (state.mut().scratch.runI * 16 + state.mut().scratch.runJ * 4 + state.mut().scratch.runK == state.mut().scratch.runTarget)
        {
        state.mut().foundAt = state.mut().scratch.runTarget;
        state.mut().scratch.runDone = 1;
        break;
        }
        }
        }
        }
        if (state.mut().scratch.runDone != 0)
        {
        state.mut().broke++;
        }
        else
        {
        state.mut().completed++;
        }

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 visited;
        uint64 foundAt;
        uint64 broke;
        uint64 completed;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.visited = state.get().visited;
        output.foundAt = state.get().foundAt;
        output.broke = state.get().broke;
        output.completed = state.get().completed;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
