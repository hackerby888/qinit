// IncrementOrderInExpression — ported from test/libsolidity/semanticTests/expressions/order_of_evaluation.sol
// Stresses: pre/post increment inside one expression and as an index
// Port caveat: the order is unspecified in both languages; this records which one each backend picks
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct IncrementOrderInExpression2
{
};

struct IncrementOrderInExpression : public ContractBase
{
    struct Scratch
    {
        uint64 mixI;
        uint64 mixValue;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 sum;
        uint64 counter;
        Array<uint64, 8> slots;
        uint64 indexAfter;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 sum;
        uint64 counter;
        uint64 slotZero;
        uint64 indexAfter;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.sum = state.get().inner.sum;
        output.counter = state.get().inner.counter;
        output.slotZero = state.get().inner.slots.get(0);
        output.indexAfter = state.get().inner.indexAfter;
    }

    struct Mix_input
    {
        uint64 seed;
    };

    struct Mix_output {};

    PUBLIC_PROCEDURE(Mix)
    {
        state.mut().inner.scratch.mixI = input.seed;
        state.mut().inner.scratch.mixValue = state.mut().inner.scratch.mixI++ + 10;
        state.mut().inner.sum += state.mut().inner.scratch.mixValue;
        state.mut().inner.scratch.mixValue = ++state.mut().inner.scratch.mixI + 100;
        state.mut().inner.sum += state.mut().inner.scratch.mixValue;
        state.mut().inner.scratch.mixI = QPI::mod(input.seed, 8ULL);
        state.mut().inner.slots.set(state.mut().inner.scratch.mixI++, state.mut().inner.scratch.mixI);
        state.mut().inner.indexAfter = state.mut().inner.scratch.mixI;
        state.mut().inner.counter++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Mix, 1);
    }
};
