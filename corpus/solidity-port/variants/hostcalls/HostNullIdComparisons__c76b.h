// HostNullIdComparisons — ported from test/libsolidity/semanticTests/various/address_zero.sol
// Stresses: NULL_ID compared against SELF, against a derived id and against itself, with the results packed — the zero-address check every access guard opens with
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostNullIdComparisons2
{
};

struct HostNullIdComparisons : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 nullIsNull;
        uint64 selfIsNull;
        uint64 derivedIsNull;
        uint64 packed;
        uint64 calls;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 nullIsNull;
        uint64 selfIsNull;
        uint64 derivedIsNull;
        uint64 packed;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.nullIsNull = state.get().nullIsNull;
        output.selfIsNull = state.get().selfIsNull;
        output.derivedIsNull = state.get().derivedIsNull;
        output.packed = state.get().packed;
        output.calls = state.get().calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        id derived;
        id nothing;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.nothing = NULL_ID;
        locals.derived = qpi.K12(input.a);
        state.mut().nullIsNull = locals.nothing == NULL_ID ? 1 : 0;
        state.mut().selfIsNull = SELF == NULL_ID ? 1 : 0;
        state.mut().derivedIsNull = locals.derived == NULL_ID ? 1 : 0;
        state.mut().packed = state.get().nullIsNull + state.get().selfIsNull * 2 + state.get().derivedIsNull * 4;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().nullIsNull = 0;
        state.mut().selfIsNull = 0;
        state.mut().derivedIsNull = 0;
        state.mut().packed = 0;
        state.mut().calls = 0;
    }
};
