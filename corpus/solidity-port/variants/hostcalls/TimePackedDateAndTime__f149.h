// TimePackedDateAndTime — ported from bit-packed timestamp pattern
// Stresses: pack and unpack the calendar through shifts and masks
// Port caveat: the packing pattern is Solidity's, the fields are QPI's
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct TimePackedDateAndTime2
{
};

struct TimePackedDateAndTime : public ContractBase
{
    struct Scratch
    {
        uint64 samplePacked;
        uint64 sampleYear;
        uint64 sampleMonth;
        uint64 sampleDay;
        uint64 sampleHour;
        uint64 sampleMinute;
        uint64 sampleSecond;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 packed;
        uint64 roundTrips;
        uint64 mismatches;
        uint64 samples;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 packed;
        uint64 roundTrips;
        uint64 mismatches;
        uint64 samples;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.packed = state.get().inner.packed;
        output.roundTrips = state.get().inner.roundTrips;
        output.mismatches = state.get().inner.mismatches;
        output.samples = state.get().inner.samples;
    }

    struct Sample_input {};

    struct Sample_output {};

    PUBLIC_PROCEDURE(Sample)
    {
        state.mut().inner.scratch.samplePacked = 0;
        state.mut().inner.scratch.samplePacked = state.mut().inner.scratch.samplePacked | ((uint64)qpi.year() << 40);
        state.mut().inner.scratch.samplePacked = state.mut().inner.scratch.samplePacked | ((uint64)qpi.month() << 36);
        state.mut().inner.scratch.samplePacked = state.mut().inner.scratch.samplePacked | ((uint64)qpi.day() << 31);
        state.mut().inner.scratch.samplePacked = state.mut().inner.scratch.samplePacked | ((uint64)qpi.hour() << 26);
        state.mut().inner.scratch.samplePacked = state.mut().inner.scratch.samplePacked | ((uint64)qpi.minute() << 20);
        state.mut().inner.scratch.samplePacked = state.mut().inner.scratch.samplePacked | ((uint64)qpi.second() << 14);
        state.mut().inner.packed = state.mut().inner.scratch.samplePacked;

        state.mut().inner.scratch.sampleYear = (state.mut().inner.scratch.samplePacked >> 40) & 255ULL;
        state.mut().inner.scratch.sampleMonth = (state.mut().inner.scratch.samplePacked >> 36) & 15ULL;
        state.mut().inner.scratch.sampleDay = (state.mut().inner.scratch.samplePacked >> 31) & 31ULL;
        state.mut().inner.scratch.sampleHour = (state.mut().inner.scratch.samplePacked >> 26) & 31ULL;
        state.mut().inner.scratch.sampleMinute = (state.mut().inner.scratch.samplePacked >> 20) & 63ULL;
        state.mut().inner.scratch.sampleSecond = (state.mut().inner.scratch.samplePacked >> 14) & 63ULL;
        if (state.mut().inner.scratch.sampleYear != (uint64)qpi.year() || state.mut().inner.scratch.sampleMonth != (uint64)qpi.month() || state.mut().inner.scratch.sampleDay != (uint64)qpi.day())
        {
        state.mut().inner.mismatches++;
        }
        else if (state.mut().inner.scratch.sampleHour != (uint64)qpi.hour() || state.mut().inner.scratch.sampleMinute != (uint64)qpi.minute() || state.mut().inner.scratch.sampleSecond != (uint64)qpi.second())
        {
        state.mut().inner.mismatches++;
        }
        else
        {
        state.mut().inner.roundTrips++;
        }
        state.mut().inner.samples++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Sample, 1);
    }

    INITIALIZE()
    {

    }
};
