// HostK12ExpressionVersusVariable — ported from openzeppelin-contracts/contracts/utils/cryptography/MerkleProof.sol
// Stresses: K12 of a computed expression next to K12 of a named local holding the same value — the exact pair F203 is about, kept as a live probe so a fix shows up here first
// Port caveat: This archetype is expected to diverge while F203 is open: the TypeScript backend hashes different bytes for the expression spelling. It is deliberately *not* pinned with expectedVerdict, because F203 is a defect to be fixed rather than an asymmetry to be documented — these rows should go green when it is.
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostK12ExpressionVersusVariable2
{
};

struct HostK12ExpressionVersusVariable : public ContractBase
{
    struct Scratch
    {
        uint64 runValue;
        id runFromExpr;
        id runFromVar;
    };

    struct StateData
    {
        uint64 fromExpression;
        uint64 fromVariable;
        uint64 agree;
        uint64 calls;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromExpression;
        uint64 fromVariable;
        uint64 agree;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromExpression = state.get().fromExpression;
        output.fromVariable = state.get().fromVariable;
        output.agree = state.get().agree;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runValue = input.a + input.b;
        state.mut().scratch.runFromExpr = qpi.K12(input.a + input.b);
        state.mut().scratch.runFromVar = qpi.K12(state.mut().scratch.runValue);
        state.mut().fromExpression = state.mut().scratch.runFromExpr.u64._0;
        state.mut().fromVariable = state.mut().scratch.runFromVar.u64._0;
        state.mut().agree = state.mut().scratch.runFromExpr == state.mut().scratch.runFromVar ? 1 : 0;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
