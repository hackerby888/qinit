// HostAssetIssuedProbe — ported from openzeppelin-contracts/contracts/token/ERC20/ERC20.sol
// Stresses: isAssetIssued for a name this contract never issued, for one it did, and for the null issuer — a bool host call whose three answers the contract stores
// Variant: entryOrder=reversed entryShape=direct initStyle=full placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostAssetIssuedProbe2
{
};

struct HostAssetIssuedProbe : public ContractBase
{
    struct Scratch
    {
        uint64 runName;
        sint64 runResult;
    };

    struct StateData
    {
        uint64 calls;
        sint64 issueResult;
        uint64 nullIssuer;
        uint64 afterIssue;
        uint64 beforeIssue;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runName = 5525825;
        state.mut().beforeIssue = qpi.isAssetIssued(SELF, state.mut().scratch.runName) ? 1 : 0;
        state.mut().scratch.runResult = qpi.issueAsset(state.mut().scratch.runName, SELF, 0, (sint64)input.a, 0);
        state.mut().issueResult = state.mut().scratch.runResult;
        state.mut().afterIssue = qpi.isAssetIssued(SELF, state.mut().scratch.runName) ? 1 : 0;
        state.mut().nullIssuer = qpi.isAssetIssued(NULL_ID, state.mut().scratch.runName) ? 1 : 0;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 beforeIssue;
        uint64 afterIssue;
        uint64 nullIssuer;
        sint64 issueResult;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.beforeIssue = state.get().beforeIssue;
        output.afterIssue = state.get().afterIssue;
        output.nullIssuer = state.get().nullIssuer;
        output.issueResult = state.get().issueResult;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().beforeIssue = 0;
        state.mut().afterIssue = 0;
        state.mut().nullIssuer = 0;
        state.mut().issueResult = 0;
        state.mut().calls = 0;
    }
};
