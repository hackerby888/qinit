// TimeAllFieldsSampled — ported from test/libsolidity/semanticTests/various/block_timestamp.sol
// Stresses: the seven calendar host calls and a recomposed ordinal
// Port caveat: no unix timestamp in QPI
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct TimeAllFieldsSampled2
{
};

struct TimeAllFieldsSampled : public ContractBase
{
    struct Scratch
    {
        uint64 samplePacked;
    };

    struct StateData
    {
        struct Inner
        {
        uint8 year;
        uint8 month;
        uint8 day;
        uint8 hour;
        uint8 minute;
        uint8 second;
        uint16 millisecond;
        uint64 ordinal;
        uint64 samples;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Sample_input {};

    struct Sample_output {};

    struct Sample_locals
    {
        Sample_input forwardedInput;
        Sample_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Sample)
    {
        CALL(SampleBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint8 year;
        uint8 month;
        uint8 day;
        uint8 hour;
        uint8 minute;
        uint8 second;
        uint16 millisecond;
        uint64 ordinal;
        uint64 samples;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using SampleBody_input = Sample_input;

    using SampleBody_output = Sample_output;

    PRIVATE_PROCEDURE(SampleBody)
    {
        state.mut().inner.year = qpi.year();
        state.mut().inner.month = qpi.month();
        state.mut().inner.day = qpi.day();
        state.mut().inner.hour = qpi.hour();
        state.mut().inner.minute = qpi.minute();
        state.mut().inner.second = qpi.second();
        state.mut().inner.millisecond = qpi.millisecond();
        // The recomposition every deadline check needs, done in uint64 so nothing wraps.
        state.mut().inner.scratch.samplePacked = (uint64)qpi.year() * 10000000000ULL;
        state.mut().inner.scratch.samplePacked += (uint64)qpi.month() * 100000000ULL;
        state.mut().inner.scratch.samplePacked += (uint64)qpi.day() * 1000000ULL;
        state.mut().inner.scratch.samplePacked += (uint64)qpi.hour() * 10000ULL;
        state.mut().inner.scratch.samplePacked += (uint64)qpi.minute() * 100ULL;
        state.mut().inner.scratch.samplePacked += (uint64)qpi.second();
        state.mut().inner.ordinal = state.mut().inner.scratch.samplePacked;
        state.mut().inner.samples++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.year = state.get().inner.year;
        output.month = state.get().inner.month;
        output.day = state.get().inner.day;
        output.hour = state.get().inner.hour;
        output.minute = state.get().inner.minute;
        output.second = state.get().inner.second;
        output.millisecond = state.get().inner.millisecond;
        output.ordinal = state.get().inner.ordinal;
        output.samples = state.get().inner.samples;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Sample, 1);
    }
};
