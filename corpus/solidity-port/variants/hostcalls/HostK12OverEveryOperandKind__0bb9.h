// HostK12OverEveryOperandKind — ported from openzeppelin-contracts/contracts/utils/cryptography/MerkleProof.sol
// Stresses: K12 taken over a scalar, a struct, a fixed array and an id — four operand kinds, each hashed from a named local, with the first word of each digest stored
// Port caveat: Solidity's keccak256 takes bytes; QPI's K12 takes a typed object, so what is hashed is the object's memory image and its padding is part of the input.
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostK12OverEveryOperandKind2
{
};

struct HostK12OverEveryOperandKind : public ContractBase
{
    struct Payload
    {
        uint64 a;
        uint32 b;
        uint8 c;
    };
    struct Scratch
    {
        uint64 runScalar;
        Payload runPayload;
        Array<uint64, 4> runValues;
        id runIdentity;
        id runDigest;
        uint64 runI;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 fromScalar;
        uint64 fromStruct;
        uint64 fromArray;
        uint64 fromId;
        uint64 allDistinct;
        uint64 calls;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromScalar;
        uint64 fromStruct;
        uint64 fromArray;
        uint64 fromId;
        uint64 allDistinct;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.fromScalar = state.get().fromScalar;
        output.fromStruct = state.get().fromStruct;
        output.fromArray = state.get().fromArray;
        output.fromId = state.get().fromId;
        output.allDistinct = state.get().allDistinct;
        output.calls = state.get().calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().scratch.runScalar = input.a;
        state.mut().scratch.runDigest = qpi.K12(state.mut().scratch.runScalar);
        state.mut().fromScalar = state.mut().scratch.runDigest.u64._0;

        state.mut().scratch.runPayload.a = input.a;
        state.mut().scratch.runPayload.b = (uint32)input.b;
        state.mut().scratch.runPayload.c = (uint8)input.b;
        state.mut().scratch.runDigest = qpi.K12(state.mut().scratch.runPayload);
        state.mut().fromStruct = state.mut().scratch.runDigest.u64._0;

        for (state.mut().scratch.runI = 0; state.mut().scratch.runI < 4; state.mut().scratch.runI++)
        {
        state.mut().scratch.runValues.set(state.mut().scratch.runI, input.a + state.mut().scratch.runI);
        }
        state.mut().scratch.runDigest = qpi.K12(state.mut().scratch.runValues);
        state.mut().fromArray = state.mut().scratch.runDigest.u64._0;

        state.mut().scratch.runIdentity = qpi.invocator();
        state.mut().scratch.runDigest = qpi.K12(state.mut().scratch.runIdentity);
        state.mut().fromId = state.mut().scratch.runDigest.u64._0;

        state.mut().allDistinct =
        state.get().fromScalar != state.get().fromStruct && state.get().fromStruct != state.get().fromArray ? 1 : 0;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().fromScalar = 0;
        state.mut().fromStruct = 0;
        state.mut().fromArray = 0;
        state.mut().fromId = 0;
        state.mut().allDistinct = 0;
        state.mut().calls = 0;
    }
};
