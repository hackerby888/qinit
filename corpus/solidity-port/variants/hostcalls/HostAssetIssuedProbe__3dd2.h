// HostAssetIssuedProbe — ported from openzeppelin-contracts/contracts/token/ERC20/ERC20.sol
// Stresses: isAssetIssued for a name this contract never issued, for one it did, and for the null issuer — a bool host call whose three answers the contract stores
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostAssetIssuedProbe2
{
};

struct HostAssetIssuedProbe : public ContractBase
{
    struct Scratch
    {
        uint64 runName;
        sint64 runResult;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 beforeIssue;
        uint64 afterIssue;
        uint64 nullIssuer;
        sint64 issueResult;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runName = 5525825;
        state.mut().inner.beforeIssue = qpi.isAssetIssued(SELF, state.mut().inner.scratch.runName) ? 1 : 0;
        state.mut().inner.scratch.runResult = qpi.issueAsset(state.mut().inner.scratch.runName, SELF, 0, (sint64)input.a, 0);
        state.mut().inner.issueResult = state.mut().inner.scratch.runResult;
        state.mut().inner.afterIssue = qpi.isAssetIssued(SELF, state.mut().inner.scratch.runName) ? 1 : 0;
        state.mut().inner.nullIssuer = qpi.isAssetIssued(NULL_ID, state.mut().inner.scratch.runName) ? 1 : 0;

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 beforeIssue;
        uint64 afterIssue;
        uint64 nullIssuer;
        sint64 issueResult;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.beforeIssue = state.get().inner.beforeIssue;
        output.afterIssue = state.get().inner.afterIssue;
        output.nullIssuer = state.get().inner.nullIssuer;
        output.issueResult = state.get().inner.issueResult;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
