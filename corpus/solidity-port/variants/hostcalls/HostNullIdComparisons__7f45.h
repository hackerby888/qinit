// HostNullIdComparisons — ported from test/libsolidity/semanticTests/various/address_zero.sol
// Stresses: NULL_ID compared against SELF, against a derived id and against itself, with the results packed — the zero-address check every access guard opens with
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostNullIdComparisons2
{
};

struct HostNullIdComparisons : public ContractBase
{
    struct Scratch
    {
        id runDerived;
        id runNothing;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 nullIsNull;
        uint64 selfIsNull;
        uint64 derivedIsNull;
        uint64 packed;
        uint64 calls;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 nullIsNull;
        uint64 selfIsNull;
        uint64 derivedIsNull;
        uint64 packed;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.nullIsNull = state.get().nullIsNull;
        output.selfIsNull = state.get().selfIsNull;
        output.derivedIsNull = state.get().derivedIsNull;
        output.packed = state.get().packed;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runNothing = NULL_ID;
        state.mut().scratch.runDerived = qpi.K12(input.a);
        state.mut().nullIsNull = state.mut().scratch.runNothing == NULL_ID ? 1 : 0;
        state.mut().selfIsNull = SELF == NULL_ID ? 1 : 0;
        state.mut().derivedIsNull = state.mut().scratch.runDerived == NULL_ID ? 1 : 0;
        state.mut().packed = state.get().nullIsNull + state.get().selfIsNull * 2 + state.get().derivedIsNull * 4;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
