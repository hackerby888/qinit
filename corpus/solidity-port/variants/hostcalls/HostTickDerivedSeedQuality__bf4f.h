// HostTickDerivedSeedQuality — ported from not-so-smart-contracts/bad_randomness
// Stresses: a seed built from the tick, the epoch and the spectrum digest, hashed and reduced — the shape a contract reaches for when it wants randomness, with the reduction bias measured
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostTickDerivedSeedQuality2
{
};

struct HostTickDerivedSeedQuality : public ContractBase
{
    struct Seed
    {
        uint64 tick;
        uint64 epoch;
        id digest;
        uint64 nonce;
    };
    struct Scratch
    {
        Seed runSeed;
        id runHashed;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 reduced;
        uint64 rawWord;
        uint64 inTopHalf;
        uint64 draws;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runSeed.tick = (uint64)qpi.tick();
        state.mut().inner.scratch.runSeed.epoch = (uint64)qpi.epoch();
        state.mut().inner.scratch.runSeed.digest = qpi.getPrevSpectrumDigest();
        state.mut().inner.scratch.runSeed.nonce = input.a;
        state.mut().inner.scratch.runHashed = qpi.K12(state.mut().inner.scratch.runSeed);
        state.mut().inner.rawWord = state.mut().inner.scratch.runHashed.u64._0;
        state.mut().inner.reduced = QPI::mod(state.mut().inner.scratch.runHashed.u64._0, 100ULL);
        state.mut().inner.inTopHalf = state.mut().inner.scratch.runHashed.u64._0 > 9223372036854775807ULL ? 1 : 0;
        state.mut().inner.draws++;

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 reduced;
        uint64 rawWord;
        uint64 inTopHalf;
        uint64 draws;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.reduced = state.get().inner.reduced;
        output.rawWord = state.get().inner.rawWord;
        output.inTopHalf = state.get().inner.inTopHalf;
        output.draws = state.get().inner.draws;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
