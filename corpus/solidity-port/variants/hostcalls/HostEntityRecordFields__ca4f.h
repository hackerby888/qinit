// HostEntityRecordFields — ported from test/libsolidity/semanticTests/various/balance.sol
// Stresses: every field of the Entity record the host fills in
// Port caveat: address.balance becomes a multi-field record
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostEntityRecordFields2
{
};

struct HostEntityRecordFields : public ContractBase
{
    struct Scratch
    {
        Entity lookRecord;
        bit lookOk;
    };

    struct StateData
    {
        struct Inner
        {
        sint64 incoming;
        sint64 outgoing;
        uint64 inTransfers;
        uint64 outTransfers;
        uint64 found;
        uint64 missing;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        sint64 incoming;
        sint64 outgoing;
        uint64 inTransfers;
        uint64 outTransfers;
        uint64 found;
        uint64 missing;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.incoming = state.get().inner.incoming;
        output.outgoing = state.get().inner.outgoing;
        output.inTransfers = state.get().inner.inTransfers;
        output.outTransfers = state.get().inner.outTransfers;
        output.found = state.get().inner.found;
        output.missing = state.get().inner.missing;
    }

    struct Look_input
    {
        id who;
    };

    struct Look_output {};

    PUBLIC_PROCEDURE(Look)
    {
        state.mut().inner.scratch.lookOk = qpi.getEntity(input.who, state.mut().inner.scratch.lookRecord);
        if (state.mut().inner.scratch.lookOk)
        {
        state.mut().inner.found++;
        state.mut().inner.incoming = state.mut().inner.scratch.lookRecord.incomingAmount;
        state.mut().inner.outgoing = state.mut().inner.scratch.lookRecord.outgoingAmount;
        state.mut().inner.inTransfers = (uint64)state.mut().inner.scratch.lookRecord.numberOfIncomingTransfers;
        state.mut().inner.outTransfers = (uint64)state.mut().inner.scratch.lookRecord.numberOfOutgoingTransfers;
        }
        else
        {
        state.mut().inner.missing++;
        }
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Look, 1);
    }

    INITIALIZE()
    {

    }
};
