// HostFeeReserveQuery — ported from no Solidity analogue (protocol fee reserve)
// Stresses: qpi.queryFeeReserve for this contract and for a neighbouring slot — a uint32 argument and a sint64 answer, where an out-of-range slot must not read a neighbour's reserve
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=first stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostFeeReserveQuery2
{
};

struct HostFeeReserveQuery : public ContractBase
{
    struct StateData
    {
        sint64 ownReserve;
        sint64 neighbourReserve;
        sint64 farReserve;
        uint64 negatives;
        uint64 calls;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        sint64 ownReserve;
        sint64 neighbourReserve;
        sint64 farReserve;
        uint64 negatives;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.ownReserve = state.get().ownReserve;
        output.neighbourReserve = state.get().neighbourReserve;
        output.farReserve = state.get().farReserve;
        output.negatives = state.get().negatives;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        sint64 value;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.value = qpi.queryFeeReserve(SELF_INDEX);
        state.mut().ownReserve = locals.value;
        locals.value = qpi.queryFeeReserve(SELF_INDEX - 1);
        state.mut().neighbourReserve = locals.value;
        locals.value = qpi.queryFeeReserve((uint32)QPI::mod(input.a, 1024ULL));
        state.mut().farReserve = locals.value;
        state.mut().negatives = state.get().ownReserve < 0 || state.get().neighbourReserve < 0 ? 1 : 0;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
