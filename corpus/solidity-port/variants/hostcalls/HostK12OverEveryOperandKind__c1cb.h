// HostK12OverEveryOperandKind — ported from openzeppelin-contracts/contracts/utils/cryptography/MerkleProof.sol
// Stresses: K12 taken over a scalar, a struct, a fixed array and an id — four operand kinds, each hashed from a named local, with the first word of each digest stored
// Port caveat: Solidity's keccak256 takes bytes; QPI's K12 takes a typed object, so what is hashed is the object's memory image and its padding is part of the input.
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostK12OverEveryOperandKind2
{
};

struct HostK12OverEveryOperandKind : public ContractBase
{
    struct Payload
    {
        uint64 a;
        uint32 b;
        uint8 c;
    };
    struct Scratch
    {
        uint64 runScalar;
        Payload runPayload;
        Array<uint64, 4> runValues;
        id runIdentity;
        id runDigest;
        uint64 runI;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 fromScalar;
        uint64 fromStruct;
        uint64 fromArray;
        uint64 fromId;
        uint64 allDistinct;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromScalar;
        uint64 fromStruct;
        uint64 fromArray;
        uint64 fromId;
        uint64 allDistinct;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromScalar = state.get().inner.fromScalar;
        output.fromStruct = state.get().inner.fromStruct;
        output.fromArray = state.get().inner.fromArray;
        output.fromId = state.get().inner.fromId;
        output.allDistinct = state.get().inner.allDistinct;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runScalar = input.a;
        state.mut().inner.scratch.runDigest = qpi.K12(state.mut().inner.scratch.runScalar);
        state.mut().inner.fromScalar = state.mut().inner.scratch.runDigest.u64._0;

        state.mut().inner.scratch.runPayload.a = input.a;
        state.mut().inner.scratch.runPayload.b = (uint32)input.b;
        state.mut().inner.scratch.runPayload.c = (uint8)input.b;
        state.mut().inner.scratch.runDigest = qpi.K12(state.mut().inner.scratch.runPayload);
        state.mut().inner.fromStruct = state.mut().inner.scratch.runDigest.u64._0;

        for (state.mut().inner.scratch.runI = 0; state.mut().inner.scratch.runI < 4; state.mut().inner.scratch.runI++)
        {
        state.mut().inner.scratch.runValues.set(state.mut().inner.scratch.runI, input.a + state.mut().inner.scratch.runI);
        }
        state.mut().inner.scratch.runDigest = qpi.K12(state.mut().inner.scratch.runValues);
        state.mut().inner.fromArray = state.mut().inner.scratch.runDigest.u64._0;

        state.mut().inner.scratch.runIdentity = qpi.invocator();
        state.mut().inner.scratch.runDigest = qpi.K12(state.mut().inner.scratch.runIdentity);
        state.mut().inner.fromId = state.mut().inner.scratch.runDigest.u64._0;

        state.mut().inner.allDistinct =
        state.get().inner.fromScalar != state.get().inner.fromStruct && state.get().inner.fromStruct != state.get().inner.fromArray ? 1 : 0;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.fromScalar = 0;
        state.mut().inner.fromStruct = 0;
        state.mut().inner.fromArray = 0;
        state.mut().inner.fromId = 0;
        state.mut().inner.allDistinct = 0;
        state.mut().inner.calls = 0;
    }
};
