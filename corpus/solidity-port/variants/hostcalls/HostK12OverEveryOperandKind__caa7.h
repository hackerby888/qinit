// HostK12OverEveryOperandKind — ported from openzeppelin-contracts/contracts/utils/cryptography/MerkleProof.sol
// Stresses: K12 taken over a scalar, a struct, a fixed array and an id — four operand kinds, each hashed from a named local, with the first word of each digest stored
// Port caveat: Solidity's keccak256 takes bytes; QPI's K12 takes a typed object, so what is hashed is the object's memory image and its padding is part of the input.
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HostK12OverEveryOperandKind2
{
};

struct HostK12OverEveryOperandKind : public ContractBase
{
    struct Payload
    {
        uint64 a;
        uint32 b;
        uint8 c;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 allDistinct;
        uint64 fromId;
        uint64 fromArray;
        uint64 fromStruct;
        uint64 fromScalar;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint64 scalar;
        Payload payload;
        Array<uint64, 4> values;
        id identity;
        id digest;
        uint64 i;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.scalar = input.a;
        locals.digest = qpi.K12(locals.scalar);
        state.mut().inner.fromScalar = locals.digest.u64._0;

        locals.payload.a = input.a;
        locals.payload.b = (uint32)input.b;
        locals.payload.c = (uint8)input.b;
        locals.digest = qpi.K12(locals.payload);
        state.mut().inner.fromStruct = locals.digest.u64._0;

        for (locals.i = 0; locals.i < 4; locals.i++)
        {
        locals.values.set(locals.i, input.a + locals.i);
        }
        locals.digest = qpi.K12(locals.values);
        state.mut().inner.fromArray = locals.digest.u64._0;

        locals.identity = qpi.invocator();
        locals.digest = qpi.K12(locals.identity);
        state.mut().inner.fromId = locals.digest.u64._0;

        state.mut().inner.allDistinct =
        state.get().inner.fromScalar != state.get().inner.fromStruct && state.get().inner.fromStruct != state.get().inner.fromArray ? 1 : 0;

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 fromScalar;
        uint64 fromStruct;
        uint64 fromArray;
        uint64 fromId;
        uint64 allDistinct;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromScalar = state.get().inner.fromScalar;
        output.fromStruct = state.get().inner.fromStruct;
        output.fromArray = state.get().inner.fromArray;
        output.fromId = state.get().inner.fromId;
        output.allDistinct = state.get().inner.allDistinct;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
