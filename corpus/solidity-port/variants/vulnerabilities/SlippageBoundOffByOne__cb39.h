// SlippageBoundOffByOne — ported from uniswap-v2-periphery/UniswapV2Router02.sol (amountOutMin)
// Stresses: a minimum-output check written with `<` and with `<=` on the same trade — the boundary case where the trade returns exactly the minimum
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=absent placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SlippageBoundOffByOne2
{
};

struct SlippageBoundOffByOne : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 acceptedStrict;
        uint64 acceptedLenient;
        uint64 boundaryCases;
        uint64 amountOut;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 acceptedStrict;
        uint64 acceptedLenient;
        uint64 boundaryCases;
        uint64 amountOut;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.acceptedStrict = state.get().inner.acceptedStrict;
        output.acceptedLenient = state.get().inner.acceptedLenient;
        output.boundaryCases = state.get().inner.boundaryCases;
        output.amountOut = state.get().inner.amountOut;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        uint64 out;
        uint64 minimum;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.minimum = input.b;
        locals.out = QPI::div(input.a * 997ULL, 1000ULL);
        state.mut().inner.amountOut = locals.out;
        state.mut().inner.acceptedStrict = locals.out > locals.minimum ? 1 : 0;
        state.mut().inner.acceptedLenient = locals.out >= locals.minimum ? 1 : 0;
        if (locals.out == locals.minimum)
        {
        state.mut().inner.boundaryCases++;
        }

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
