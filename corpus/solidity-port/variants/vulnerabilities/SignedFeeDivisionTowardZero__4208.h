// SignedFeeDivisionTowardZero — ported from fee split rounding
// Stresses: truncation toward zero across the sign boundary, with the remainder tracked
// Port caveat: faithful apart from the width
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedFeeDivisionTowardZero2
{
};

struct SignedFeeDivisionTowardZero : public ContractBase
{
    struct Scratch
    {
        sint64 settleFee;
        sint64 settleRemainder;
    };

    struct StateData
    {
        sint64 treasury;
        sint64 counterparty;
        sint64 dust;
        uint64 settlements;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Settle_input
    {
        sint64 amount;
        sint64 feeBasisPoints;
    };

    struct Settle_output {};

    PUBLIC_PROCEDURE(Settle)
    {
        state.mut().scratch.settleFee = QPI::div(input.amount * input.feeBasisPoints, (sint64)10000);
        state.mut().scratch.settleRemainder = QPI::mod(input.amount * input.feeBasisPoints, (sint64)10000);
        state.mut().treasury += state.mut().scratch.settleFee;
        state.mut().counterparty += input.amount - state.mut().scratch.settleFee;
        state.mut().dust += state.mut().scratch.settleRemainder;
        state.mut().settlements++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 treasury;
        sint64 counterparty;
        sint64 dust;
        uint64 settlements;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.treasury = state.get().treasury;
        output.counterparty = state.get().counterparty;
        output.dust = state.get().dust;
        output.settlements = state.get().settlements;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Settle, 1);
    }

    INITIALIZE()
    {

    }
};
