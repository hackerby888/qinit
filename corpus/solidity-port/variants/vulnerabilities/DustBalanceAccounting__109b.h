// DustBalanceAccounting — ported from rekt: dust-balance griefing
// Stresses: a withdrawal that leaves a balance below the dust threshold, with the sweep that is supposed to zero it written two ways — one of which leaves the dust behind
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct DustBalanceAccounting2
{
};

struct DustBalanceAccounting : public ContractBase
{
    struct Scratch
    {
        uint64 runAmount;
        uint64 runRemaining;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 balance;
        uint64 swept;
        uint64 dustLeft;
        uint64 sweeps;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 balance;
        uint64 swept;
        uint64 dustLeft;
        uint64 sweeps;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.balance = state.get().inner.balance;
        output.swept = state.get().inner.swept;
        output.dustLeft = state.get().inner.dustLeft;
        output.sweeps = state.get().inner.sweeps;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.balance = input.a;
        state.mut().inner.scratch.runAmount = input.b > input.a ? input.a : input.b;
        state.mut().inner.scratch.runRemaining = input.a - state.mut().inner.scratch.runAmount;
        if (state.mut().inner.scratch.runRemaining < 10 && state.mut().inner.scratch.runRemaining > 0)
        {
        state.mut().inner.swept += state.mut().inner.scratch.runRemaining;
        state.mut().inner.balance = 0;
        state.mut().inner.sweeps++;
        }
        else
        {
        state.mut().inner.balance = state.mut().inner.scratch.runRemaining;
        }
        state.mut().inner.dustLeft = state.get().inner.balance > 0 && state.get().inner.balance < 10 ? state.get().inner.balance : 0;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
