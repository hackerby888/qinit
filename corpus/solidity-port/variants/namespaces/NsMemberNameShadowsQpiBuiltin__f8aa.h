// NsMemberNameShadowsQpiBuiltin — ported from test/libsolidity/semanticTests/operators/userDefined/operator_definition_shadowing_builtin_keccak256.sol
// Stresses: members named like QPI builtins
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NsMemberNameShadowsQpiBuiltin2
{
};

struct NsMemberNameShadowsQpiBuiltin : public ContractBase
{
    struct Scratch
    {
        uint64 computeScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 div;
        uint64 mod;
        uint64 tick;
        uint64 realDiv;
        uint64 realMod;
        uint64 realTick;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 div;
        uint64 mod;
        uint64 tick;
        uint64 realDiv;
        uint64 realMod;
        uint64 realTick;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Compute_input
    {
        uint64 a;
        uint64 b;
    };

    struct Compute_output {};

    struct Compute_locals
    {
        Compute_input forwardedInput;
        Compute_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Compute)
    {
        locals.forwardedInput = input;
        CALL(ComputeBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.div = state.get().inner.div;
        output.mod = state.get().inner.mod;
        output.tick = state.get().inner.tick;
        output.realDiv = state.get().inner.realDiv;
        output.realMod = state.get().inner.realMod;
        output.realTick = state.get().inner.realTick;
    }

    using ComputeBody_input = Compute_input;

    using ComputeBody_output = Compute_output;

    PRIVATE_PROCEDURE(ComputeBody)
    {
        state.mut().inner.div = input.a;
        state.mut().inner.mod = input.b;
        state.mut().inner.tick = 99;
        // The shadowing members above must not capture these qualified calls.
        state.mut().inner.scratch.computeScratch = QPI::div(input.a, input.b);
        state.mut().inner.realDiv = state.mut().inner.scratch.computeScratch;
        state.mut().inner.scratch.computeScratch = QPI::mod(input.a, input.b);
        state.mut().inner.realMod = state.mut().inner.scratch.computeScratch;
        state.mut().inner.realTick = (uint64)qpi.tick();
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Compute, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.div = 0;
        state.mut().inner.mod = 0;
        state.mut().inner.tick = 0;
        state.mut().inner.realDiv = 0;
        state.mut().inner.realMod = 0;
        state.mut().inner.realTick = 0;
    }
};
