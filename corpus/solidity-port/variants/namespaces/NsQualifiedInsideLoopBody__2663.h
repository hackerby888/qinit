// NsQualifiedInsideLoopBody — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: the qualified constant read inside a loop body, so the resolution happens where a compiler is most likely to hoist or fold it
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Step
{
static constexpr uint64 size = 5;
}

namespace Other
{
static constexpr uint64 size = 50;
}

struct NsQualifiedInsideLoopBody2
{
};

struct NsQualifiedInsideLoopBody : public ContractBase
{
    struct Scratch
    {
        uint64 snapI;
        uint64 snapTotal;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 lastStep;
        uint64 iterations;
        uint64 loopTotal;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().inner.scratch.snapTotal = 0;
        for (state.mut().inner.scratch.snapI = 0; state.mut().inner.scratch.snapI < 4; state.mut().inner.scratch.snapI++)
        {
        state.mut().inner.scratch.snapTotal += Step::size * state.mut().inner.scratch.snapI + Other::size;
        state.mut().inner.lastStep = Step::size;
        }
        state.mut().inner.loopTotal = state.mut().inner.scratch.snapTotal;
        state.mut().inner.iterations = state.mut().inner.scratch.snapI;

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 loopTotal;
        uint64 iterations;
        uint64 lastStep;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.loopTotal = state.get().inner.loopTotal;
        output.iterations = state.get().inner.iterations;
        output.lastStep = state.get().inner.lastStep;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {

    }
};
