// NsUsingDirectiveThenBothQualified — ported from test/libsolidity/semanticTests/scoping/scoping_activation.sol
// Stresses: a using-directive brings one namespace in wholesale while a second namespace declares the same name; every read here is qualified, so the directive must not change any of them
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Wide
{
static constexpr uint64 limit = 12;
}

namespace Narrow
{
static constexpr uint64 limit = 400;
}

using namespace Wide;

struct NsUsingDirectiveThenBothQualified2
{
};

struct NsUsingDirectiveThenBothQualified : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 viaWide;
        uint64 viaNarrow;
        uint64 product;
        uint64 difference;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 viaWide;
        uint64 viaNarrow;
        uint64 product;
        uint64 difference;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.viaWide = state.get().inner.viaWide;
        output.viaNarrow = state.get().inner.viaNarrow;
        output.product = state.get().inner.product;
        output.difference = state.get().inner.difference;
        output.calls = state.get().inner.calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        state.mut().inner.viaWide = Wide::limit;
        state.mut().inner.viaNarrow = Narrow::limit;
        state.mut().inner.product = Wide::limit * Narrow::limit;
        state.mut().inner.difference = Narrow::limit - Wide::limit;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {

    }
};
