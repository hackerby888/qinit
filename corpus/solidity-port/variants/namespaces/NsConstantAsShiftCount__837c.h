// NsConstantAsShiftCount — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: same-named constants used as shift counts, where picking the wrong one moves the bit by a different distance — combined with F204's territory, since one of the counts is at the operand width
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Small
{
static constexpr uint64 shift = 3;
}

namespace Big
{
static constexpr uint64 shift = 40;
}

struct NsConstantAsShiftCount2
{
};

struct NsConstantAsShiftCount : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 combined;
        uint64 big;
        uint64 small;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 small;
        uint64 big;
        uint64 combined;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.small = state.get().inner.small;
        output.big = state.get().inner.big;
        output.combined = state.get().inner.combined;
        output.calls = state.get().inner.calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().inner.small = 1ULL << Small::shift;
        state.mut().inner.big = 1ULL << Big::shift;
        state.mut().inner.combined = (1ULL << Small::shift) | (1ULL << Big::shift);

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
