// NsTwinConstant — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: a namespaced capacity constant with a same-named twin of a different value
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty ns=aliasOfAlias placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
static constexpr uint64 CAPACITY = 8;
}

struct NsTwinConstant2
{
};

struct NsTwinConstant : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        Array<uint64, Alpha::CAPACITY> slots;
        uint64 capacity;
        uint64 checksum;
    };

    struct Fill_input
    {
        uint64 count;
    };

    struct Fill_output {};

    struct Fill_locals
    {
        Fill_input forwardedInput;
        Fill_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Fill)
    {
        locals.forwardedInput = input;
        CALL(FillBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 capacity;
        uint64 checksum;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using FillBody_input = Fill_input;

    using FillBody_output = Fill_output;

    struct FillBody_locals
    {
        uint64 i;
        uint64 total;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(FillBody)
    {
        state.mut().capacity = Alpha::CAPACITY;
        locals.total = 0;
        for (locals.i = 0; locals.i < input.count; locals.i++)
        {
        state.mut().slots.set(locals.i, locals.i + 1);
        }
        for (locals.i = 0; locals.i < Alpha::CAPACITY; locals.i++)
        {
        locals.total += state.get().slots.get(locals.i);
        }
        state.mut().checksum = locals.total;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.capacity = state.get().capacity;
        output.checksum = state.get().checksum;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Fill, 1);
    }

    INITIALIZE()
    {

    }
};
