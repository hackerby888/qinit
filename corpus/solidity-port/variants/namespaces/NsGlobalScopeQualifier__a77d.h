// NsGlobalScopeQualifier — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: the global-scope qualifier next to the namespace-qualified spelling
// Port caveat: documented divergence: the TypeScript backend refuses `::name`, clang accepts it
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint64 threshold = 7;

namespace Port
{
static constexpr uint64 threshold = 100;
}

struct NsGlobalScopeQualifier2
{
};

struct NsGlobalScopeQualifier : public ContractBase
{
    struct Scratch
    {
        uint64 compareScratch;
    };

    struct StateData
    {
        uint64 calls;
        uint64 viaNamespace;
        uint64 viaGlobalQualifier;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 viaGlobalQualifier;
        uint64 viaNamespace;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Compare_input
    {
        uint64 value;
    };

    struct Compare_output {};

    struct Compare_locals
    {
        Compare_input forwardedInput;
        Compare_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Compare)
    {
        locals.forwardedInput = input;
        CALL(CompareBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.viaGlobalQualifier = state.get().viaGlobalQualifier;
        output.viaNamespace = state.get().viaNamespace;
        output.calls = state.get().calls;
    }

    using CompareBody_input = Compare_input;

    using CompareBody_output = Compare_output;

    PRIVATE_PROCEDURE(CompareBody)
    {
        state.mut().scratch.compareScratch = input.value;
        state.mut().viaGlobalQualifier = state.mut().scratch.compareScratch > ::threshold ? 1 : 0;
        state.mut().viaNamespace = state.mut().scratch.compareScratch > Port::threshold ? 1 : 0;
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Compare, 1);
    }
};
