// NsTwoEnumsOneNamespaceSharedConstantName — ported from test/libsolidity/semanticTests/enums/enum_referencing.sol
// Stresses: two different enum types declared in the SAME namespace, sharing one constant name — so namespace qualification cannot disambiguate them and only the enum type can
// Port caveat: C++ plain enums export their constants into the enclosing scope, so this pair is ill-formed if both spell the same constant; the shared name is given different values per type to make a wrong pick visible.
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Single
{
enum First
{
alpha = 3,
shared = 15
};

enum Second
{
beta = 9
};
}

struct NsTwoEnumsOneNamespaceSharedConstantName2
{
};

struct NsTwoEnumsOneNamespaceSharedConstantName : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 alphaValue;
        uint64 sharedValue;
        uint64 betaValue;
        uint64 sum;
        uint64 calls;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 alphaValue;
        uint64 sharedValue;
        uint64 betaValue;
        uint64 sum;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        Snap_input forwardedInput;
        Snap_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        locals.forwardedInput = input;
        CALL(SnapBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.alphaValue = state.get().alphaValue;
        output.sharedValue = state.get().sharedValue;
        output.betaValue = state.get().betaValue;
        output.sum = state.get().sum;
        output.calls = state.get().calls;
    }

    using SnapBody_input = Snap_input;

    using SnapBody_output = Snap_output;

    struct SnapBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(SnapBody)
    {
        state.mut().alphaValue = Single::alpha;
        state.mut().sharedValue = Single::shared;
        state.mut().betaValue = Single::beta;
        state.mut().sum = Single::alpha + Single::shared + Single::beta;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {

    }
};
