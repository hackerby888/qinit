// NsConstantAsContainerCapacity — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: a namespaced constant in a template argument
// Variant: entryOrder=reversed entryShape=direct initStyle=full layout=padded placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Small
{
static constexpr uint64 capacity = 4;
}

namespace Large
{
static constexpr uint64 capacity = 16;
}

struct NsConstantAsContainerCapacity2
{
};

struct NsConstantAsContainerCapacity : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        Array<uint64, Small::capacity> small;
        Array<uint64, Large::capacity> large;
        uint64 smallSize;
        uint64 largeSize;
        uint64 writes;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 smallSize;
        uint64 largeSize;
        uint64 smallLast;
        uint64 largeLast;
        uint64 writes;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.smallSize = state.get().inner.smallSize;
        output.largeSize = state.get().inner.largeSize;
        output.smallLast = state.get().inner.small.get(Small::capacity - 1);
        output.largeLast = state.get().inner.large.get(Large::capacity - 1);
        output.writes = state.get().inner.writes;
    }

    struct Fill_input
    {
        uint64 value;
    };

    struct Fill_output {};

    struct Fill_locals
    {
        uint64 i;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Fill)
    {
        for (locals.i = 0; locals.i < Small::capacity; locals.i++)
        {
        state.mut().inner.small.set(locals.i, input.value + locals.i);
        }
        for (locals.i = 0; locals.i < Large::capacity; locals.i++)
        {
        state.mut().inner.large.set(locals.i, input.value * 2 + locals.i);
        }
        state.mut().inner.smallSize = sizeof(state.get().inner.small);
        state.mut().inner.largeSize = sizeof(state.get().inner.large);
        state.mut().inner.writes++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Fill, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.small.setAll(0);
        state.mut().inner.large.setAll(0);
        state.mut().inner.smallSize = 0;
        state.mut().inner.largeSize = 0;
        state.mut().inner.writes = 0;
    }
};
