// NsConstantNameEqualsStructName — ported from test/libsolidity/semanticTests/types/struct_name_collision.sol
// Stresses: a namespace declaring both a struct and a constant of the same name, so the constant read and the type spelling collide
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Mixed
{
struct Marker
{
uint64 field;
};
}

namespace Other
{
static constexpr uint64 Marker = 88;
}

struct NsConstantNameEqualsStructName2
{
};

struct NsConstantNameEqualsStructName : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 constantValue;
        uint64 structField;
        uint64 sum;
        uint64 witness;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        Snap_input forwardedInput;
        Snap_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        locals.forwardedInput = input;
        CALL(SnapBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 constantValue;
        uint64 structField;
        uint64 sum;
        uint64 witness;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using SnapBody_input = Snap_input;

    using SnapBody_output = Snap_output;

    struct SnapBody_locals
    {
        uint64 scratch;
        Mixed::Marker marker;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(SnapBody)
    {
        locals.marker.field = 4;
        state.mut().inner.constantValue = Other::Marker;
        state.mut().inner.structField = locals.marker.field;
        state.mut().inner.sum = Other::Marker + locals.marker.field;
        state.mut().inner.witness = 1;

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.constantValue = state.get().inner.constantValue;
        output.structField = state.get().inner.structField;
        output.sum = state.get().inner.sum;
        output.witness = state.get().inner.witness;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
