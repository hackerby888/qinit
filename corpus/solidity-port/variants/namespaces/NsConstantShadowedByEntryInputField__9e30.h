// NsConstantShadowedByEntryInputField — ported from test/libsolidity/semanticTests/scoping/scoping_activation.sol
// Stresses: an entry input field named exactly like a file-scope constant, so `input.seed` and the bare constant must stay distinct
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint64 seed = 77;

struct NsConstantShadowedByEntryInputField2
{
};

struct NsConstantShadowedByEntryInputField : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 witness;
        uint64 sum;
        uint64 fromInput;
        uint64 fromConstant;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromConstant;
        uint64 fromInput;
        uint64 sum;
        uint64 witness;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        Snap_input forwardedInput;
        Snap_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        locals.forwardedInput = input;
        CALL(SnapBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.fromConstant = state.get().inner.fromConstant;
        output.fromInput = state.get().inner.fromInput;
        output.sum = state.get().inner.sum;
        output.witness = state.get().inner.witness;
        output.calls = state.get().inner.calls;
    }

    using SnapBody_input = Snap_input;

    using SnapBody_output = Snap_output;

    PRIVATE_PROCEDURE(SnapBody)
    {
        state.mut().inner.fromConstant = seed;
        state.mut().inner.fromInput = input.seed;
        state.mut().inner.sum = seed + input.seed;
        state.mut().inner.witness = 1;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.fromConstant = 0;
        state.mut().inner.fromInput = 0;
        state.mut().inner.sum = 0;
        state.mut().inner.witness = 0;
        state.mut().inner.calls = 0;
    }
};
