// NsConstantAsContainerCapacity — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: a namespaced constant in a template argument
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full layout=widestFirst placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Small
{
static constexpr uint64 capacity = 4;
}

namespace Large
{
static constexpr uint64 capacity = 16;
}

struct NsConstantAsContainerCapacity2
{
};

struct NsConstantAsContainerCapacity : public ContractBase
{
    struct Scratch
    {
        uint64 fillI;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 writes;
        uint64 largeSize;
        uint64 smallSize;
        Array<uint64, Large::capacity> large;
        Array<uint64, Small::capacity> small;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 smallSize;
        uint64 largeSize;
        uint64 smallLast;
        uint64 largeLast;
        uint64 writes;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Fill_input
    {
        uint64 value;
    };

    struct Fill_output {};

    struct Fill_locals
    {
        Fill_input forwardedInput;
        Fill_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Fill)
    {
        locals.forwardedInput = input;
        CALL(FillBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.smallSize = state.get().smallSize;
        output.largeSize = state.get().largeSize;
        output.smallLast = state.get().small.get(Small::capacity - 1);
        output.largeLast = state.get().large.get(Large::capacity - 1);
        output.writes = state.get().writes;
    }

    using FillBody_input = Fill_input;

    using FillBody_output = Fill_output;

    PRIVATE_PROCEDURE(FillBody)
    {
        for (state.mut().scratch.fillI = 0; state.mut().scratch.fillI < Small::capacity; state.mut().scratch.fillI++)
        {
        state.mut().small.set(state.mut().scratch.fillI, input.value + state.mut().scratch.fillI);
        }
        for (state.mut().scratch.fillI = 0; state.mut().scratch.fillI < Large::capacity; state.mut().scratch.fillI++)
        {
        state.mut().large.set(state.mut().scratch.fillI, input.value * 2 + state.mut().scratch.fillI);
        }
        state.mut().smallSize = sizeof(state.get().small);
        state.mut().largeSize = sizeof(state.get().large);
        state.mut().writes++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Fill, 1);
    }

    INITIALIZE()
    {
        state.mut().small.setAll(0);
        state.mut().large.setAll(0);
        state.mut().smallSize = 0;
        state.mut().largeSize = 0;
        state.mut().writes = 0;
    }
};
