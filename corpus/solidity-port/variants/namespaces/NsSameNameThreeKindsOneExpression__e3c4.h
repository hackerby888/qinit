// NsSameNameThreeKindsOneExpression — ported from test/libsolidity/semanticTests/scoping/scoping_activation.sol
// Stresses: a file constant, an enum constant and a local all named the same, combined in one expression so a wrong pick changes the arithmetic rather than only one member
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace AsConst
{
static constexpr uint64 span = 2;
}

namespace AsEnum
{
enum Widths
{
span = 30
};
}

struct NsSameNameThreeKindsOneExpression2
{
};

struct NsSameNameThreeKindsOneExpression : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
        uint64 snapSpan;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 combined;
        uint64 enumValue;
        uint64 constValue;
        uint64 localValue;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 localValue;
        uint64 constValue;
        uint64 enumValue;
        uint64 combined;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.localValue = state.get().inner.localValue;
        output.constValue = state.get().inner.constValue;
        output.enumValue = state.get().inner.enumValue;
        output.combined = state.get().inner.combined;
        output.calls = state.get().inner.calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().inner.scratch.snapSpan = 500;
        state.mut().inner.localValue = state.mut().inner.scratch.snapSpan;
        state.mut().inner.constValue = AsConst::span;
        state.mut().inner.enumValue = AsEnum::span;
        state.mut().inner.combined = state.mut().inner.scratch.snapSpan + AsConst::span + AsEnum::span;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.localValue = 0;
        state.mut().inner.constValue = 0;
        state.mut().inner.enumValue = 0;
        state.mut().inner.combined = 0;
        state.mut().inner.calls = 0;
    }
};
