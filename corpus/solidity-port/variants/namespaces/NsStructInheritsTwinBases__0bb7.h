// NsStructInheritsTwinBases — ported from test/libsolidity/semanticTests/inheritance/base_base_overload.sol
// Stresses: inheriting one twin, holding the other
// Port caveat: struct inheritance stands in for contract inheritance
// Variant: entryOrder=declared entryShape=direct initStyle=absent ns=alias placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
struct Base
{
    uint64 first;
    uint32 second;
};
}

namespace Beta
{
struct Base
{
    uint32 first;
    uint64 second;
};
}

struct Derived : public Alpha::Base
{
    uint64 extra;
};

struct NsStructInheritsTwinBases2
{
};

struct NsStructInheritsTwinBases : public ContractBase
{
    struct Scratch
    {
        uint64 assignScratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        Derived derived;
        Beta::Base other;
        uint64 derivedSize;
        uint64 otherSize;
        uint64 writes;
        Scratch scratch;
    };

    struct Assign_input
    {
        uint64 value;
    };

    struct Assign_output {};

    PUBLIC_PROCEDURE(Assign)
    {
        state.mut().scratch.assignScratch = input.value;
        state.mut().derived.first = state.mut().scratch.assignScratch;
        state.mut().derived.second = (uint32)state.mut().scratch.assignScratch;
        state.mut().derived.extra = state.mut().scratch.assignScratch * 3;
        state.mut().other.first = (uint32)state.mut().scratch.assignScratch;
        state.mut().other.second = state.mut().scratch.assignScratch * 5;
        state.mut().derivedSize = sizeof(Derived);
        state.mut().otherSize = sizeof(Beta::Base);
        state.mut().writes++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 derivedFirst;
        uint32 derivedSecond;
        uint64 derivedExtra;
        uint32 otherFirst;
        uint64 otherSecond;
        uint64 derivedSize;
        uint64 otherSize;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.derivedFirst = state.get().derived.first;
        output.derivedSecond = state.get().derived.second;
        output.derivedExtra = state.get().derived.extra;
        output.otherFirst = state.get().other.first;
        output.otherSecond = state.get().other.second;
        output.derivedSize = state.get().derivedSize;
        output.otherSize = state.get().otherSize;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Assign, 1);
    }
};
