// NsTypedefsToDifferentWidths — ported from test/libsolidity/semanticTests/libraries/using_library_structs.sol
// Stresses: the same typedef name in two namespaces aliasing different scalar widths, used for a cast — a mis-resolution truncates or widens the value instead of failing
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Narrow
{
using Amount = uint16;
}

namespace Wide
{
using Amount = uint64;
}

struct NsTypedefsToDifferentWidths2
{
};

struct NsTypedefsToDifferentWidths : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 wideSize;
        uint64 narrowSize;
        uint64 wideCast;
        uint64 narrowCast;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().inner.narrowCast = (uint64)(Narrow::Amount)(input.seed + 100000);
        state.mut().inner.wideCast = (uint64)(Wide::Amount)(input.seed + 100000);
        state.mut().inner.narrowSize = sizeof(Narrow::Amount);
        state.mut().inner.wideSize = sizeof(Wide::Amount);

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 narrowCast;
        uint64 wideCast;
        uint64 narrowSize;
        uint64 wideSize;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.narrowCast = state.get().inner.narrowCast;
        output.wideCast = state.get().inner.wideCast;
        output.narrowSize = state.get().inner.narrowSize;
        output.wideSize = state.get().inner.wideSize;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
