// NsBaseMemberVersusFileConstant — ported from test/libsolidity/semanticTests/inheritance/inherited_state_var_shadowing.sol
// Stresses: a base-class member and a file-scope constant sharing a name: the member read through the object must not pick up the constant, and the bare name must not pick up the member
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint64 ceiling = 11;

struct Holder
{
uint64 ceiling;
};

struct NsBaseMemberVersusFileConstant2
{
};

struct NsBaseMemberVersusFileConstant : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
        Holder snapHolder;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 fromConstant;
        uint64 fromMember;
        uint64 sum;
        uint64 witness;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromConstant;
        uint64 fromMember;
        uint64 sum;
        uint64 witness;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromConstant = state.get().inner.fromConstant;
        output.fromMember = state.get().inner.fromMember;
        output.sum = state.get().inner.sum;
        output.witness = state.get().inner.witness;
        output.calls = state.get().inner.calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().inner.scratch.snapHolder.ceiling = 640;
        state.mut().inner.fromConstant = ceiling;
        state.mut().inner.fromMember = state.mut().inner.scratch.snapHolder.ceiling;
        state.mut().inner.sum = ceiling + state.mut().inner.scratch.snapHolder.ceiling;
        state.mut().inner.witness = 1;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {

    }
};
