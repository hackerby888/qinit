// NsSizeofLocalControl — ported from negative control
// Stresses: unqualified sizeof must not move when the qualified path changes
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Port
{
struct Wide { uint64 a; uint64 b; uint64 c; };
}

struct GlobalWide { uint64 a; uint64 b; uint64 c; };

struct NsSizeofLocalControl2
{
};

struct NsSizeofLocalControl : public ContractBase
{
    struct Scratch
    {
        GlobalWide measureSample;
        uint64 measureCounter;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 localSize;
        uint64 globalSize;
        uint64 namespacedSize;
        uint64 scalarSize;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Measure_input {};

    struct Measure_output {};

    PUBLIC_PROCEDURE(Measure)
    {
        state.mut().inner.scratch.measureCounter = 0;
        state.mut().inner.localSize = sizeof(state.mut().inner.scratch.measureSample);
        state.mut().inner.globalSize = sizeof(GlobalWide);
        state.mut().inner.namespacedSize = sizeof(Port::Wide);
        state.mut().inner.scalarSize = sizeof(state.mut().inner.scratch.measureCounter);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 localSize;
        uint64 globalSize;
        uint64 namespacedSize;
        uint64 scalarSize;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.localSize = state.get().inner.localSize;
        output.globalSize = state.get().inner.globalSize;
        output.namespacedSize = state.get().inner.namespacedSize;
        output.scalarSize = state.get().inner.scalarSize;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }
};
