// NsTwinConstantsQualifiedRead — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: two namespaced constants of the same name and different values, both read through their qualified names in one statement
// Port caveat: Solidity has no namespaces; the same collision arises between two libraries, and the qualified read is `L.C`.
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
static constexpr uint64 rate = 3;
}

namespace Beta
{
static constexpr uint64 rate = 700;
}

struct NsTwinConstantsQualifiedRead2
{
};

struct NsTwinConstantsQualifiedRead : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 alphaRate;
        uint64 betaRate;
        uint64 product;
        uint64 sum;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 alphaRate;
        uint64 betaRate;
        uint64 product;
        uint64 sum;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.alphaRate = state.get().inner.alphaRate;
        output.betaRate = state.get().inner.betaRate;
        output.product = state.get().inner.product;
        output.sum = state.get().inner.sum;
        output.calls = state.get().inner.calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        state.mut().inner.alphaRate = Alpha::rate;
        state.mut().inner.betaRate = Beta::rate;
        state.mut().inner.product = Alpha::rate * Beta::rate;
        state.mut().inner.sum = Alpha::rate + Beta::rate;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.alphaRate = 0;
        state.mut().inner.betaRate = 0;
        state.mut().inner.product = 0;
        state.mut().inner.sum = 0;
        state.mut().inner.calls = 0;
    }
};
