// NsConstantSameNameTwoNamespaces — ported from test/libsolidity/semanticTests/constants/same_constants_different_files.sol
// Stresses: same-named constants in two namespaces
// Variant: constSource=input entryOrder=declared entryShape=viaPrivate initStyle=full placement=first stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
static constexpr uint64 LIMIT = 10;
static constexpr uint64 STEP = 3;
}

namespace Beta
{
static constexpr uint64 LIMIT = 1000;
static constexpr uint64 STEP = 7;
}

struct NsConstantSameNameTwoNamespaces2
{
};

struct NsConstantSameNameTwoNamespaces : public ContractBase
{
    struct StateData
    {
        uint64 fromAlpha;
        uint64 fromBeta;
        uint64 mixed;
        uint64 placementGuard;
    };

    struct Compute_input
    {
        uint64 seed;
    };

    struct Compute_output {};

    struct Compute_locals
    {
        Compute_input forwardedInput;
        Compute_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Compute)
    {
        locals.forwardedInput = input;
        CALL(ComputeBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 fromAlpha;
        uint64 fromBeta;
        uint64 mixed;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using ComputeBody_input = Compute_input;

    using ComputeBody_output = Compute_output;

    struct ComputeBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(ComputeBody)
    {
        state.mut().fromAlpha = QPI::mod(input.seed, Alpha::LIMIT) * Alpha::STEP;
        state.mut().fromBeta = QPI::mod(input.seed, Beta::LIMIT) * Beta::STEP;
        locals.scratch = Alpha::LIMIT * Beta::STEP + Beta::LIMIT * Alpha::STEP;
        state.mut().mixed = locals.scratch;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.fromAlpha = state.get().fromAlpha;
        output.fromBeta = state.get().fromBeta;
        output.mixed = state.get().mixed;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Compute, 1);
    }

    INITIALIZE()
    {
        state.mut().fromAlpha = 0;
        state.mut().fromBeta = 0;
        state.mut().mixed = 0;
    }
};
