// NsMemberShadowsStateField — ported from test/libsolidity/semanticTests/inheritance/inherited_state.sol
// Stresses: a `_locals` member named exactly like a state member, written and read in the same entry — the QPI-specific form of SWC-119 shadowing
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NsMemberShadowsStateField2
{
};

struct NsMemberShadowsStateField : public ContractBase
{
    struct Scratch
    {
        uint64 runBalance;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 witness;
        uint64 balance;
        uint64 calls;
        uint64 accumulator;
        Scratch scratch;
    };

    struct Run_input
    {
        uint64 amount;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().scratch.runBalance = state.get().balance + input.amount;
        state.mut().witness = state.mut().scratch.runBalance;
        state.mut().balance = state.mut().scratch.runBalance;
        state.mut().accumulator = state.get().balance;

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.accumulator = state.get().accumulator;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
