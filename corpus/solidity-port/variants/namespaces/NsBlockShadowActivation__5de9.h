// NsBlockShadowActivation — ported from test/libsolidity/semanticTests/scoping/c99_scoping_activation.sol
// Stresses: C99 declaration-point shadowing: an inner name is only in scope after its declaration, so the statement before it writes the outer one
// Port caveat: QPI forbids stack locals, so the two scopes are two members of the `_locals` struct reached through nested blocks rather than two declarations of one name.
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NsBlockShadowActivation2
{
};

struct NsBlockShadowActivation : public ContractBase
{
    struct Scratch
    {
        uint64 runOuter;
        uint64 runInner;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 accumulator;
        uint64 calls;
        uint64 outerAfterBlock;
        uint64 innerInBlock;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().inner.accumulator;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 seed;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runOuter = 7;
        {
        // Writes here reach the outer name; the inner one only exists from its declaration on.
        state.mut().inner.scratch.runOuter = 3;
        state.mut().inner.scratch.runInner = 4;
        }
        state.mut().inner.outerAfterBlock = state.mut().inner.scratch.runOuter;
        state.mut().inner.innerInBlock = state.mut().inner.scratch.runInner;
        state.mut().inner.accumulator = state.mut().inner.scratch.runOuter * 100 + state.mut().inner.scratch.runInner + input.seed;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.accumulator = 0;
        state.mut().inner.calls = 0;
    }
};
