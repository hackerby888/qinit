// NsConstantNameEqualsStructName — ported from test/libsolidity/semanticTests/types/struct_name_collision.sol
// Stresses: a namespace declaring both a struct and a constant of the same name, so the constant read and the type spelling collide
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Mixed
{
struct Marker
{
uint64 field;
};
}

namespace Other
{
static constexpr uint64 Marker = 88;
}

struct NsConstantNameEqualsStructName2
{
};

struct NsConstantNameEqualsStructName : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
        Mixed::Marker snapMarker;
    };

    struct StateData
    {
        uint64 calls;
        uint64 witness;
        uint64 sum;
        uint64 structField;
        uint64 constantValue;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 constantValue;
        uint64 structField;
        uint64 sum;
        uint64 witness;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.constantValue = state.get().constantValue;
        output.structField = state.get().structField;
        output.sum = state.get().sum;
        output.witness = state.get().witness;
        output.calls = state.get().calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().scratch.snapMarker.field = 4;
        state.mut().constantValue = Other::Marker;
        state.mut().structField = state.mut().scratch.snapMarker.field;
        state.mut().sum = Other::Marker + state.mut().scratch.snapMarker.field;
        state.mut().witness = 1;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
