// NsLocalsMemberNamedLikeNamespace — ported from test/libsolidity/semanticTests/scoping/name_shadowing.sol
// Stresses: a locals member named like a namespace
// Port caveat: library-name shadowing in the original
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Port
{
static constexpr uint64 base = 1000;
struct Config
{
    uint64 limit;
};
}

struct NsLocalsMemberNamedLikeNamespace2
{
};

struct NsLocalsMemberNamedLikeNamespace : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 fromLocal;
        uint64 fromNamespace;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromLocal;
        uint64 fromNamespace;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromLocal = state.get().inner.fromLocal;
        output.fromNamespace = state.get().inner.fromNamespace;
        output.calls = state.get().inner.calls;
    }

    struct Use_input
    {
        uint64 value;
    };

    struct Use_output {};

    struct Use_locals
    {
        uint64 Port;
        Port::Config config;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Use)
    {
        locals.Port = input.value + 1;
        locals.config.limit = Port::base;
        state.mut().inner.fromLocal = locals.Port;
        state.mut().inner.fromNamespace = locals.config.limit;
        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Use, 1);
    }

    INITIALIZE()
    {

    }
};
