// NsInputFieldNamedLikeMacroArgument — ported from test/libsolidity/semanticTests/constructor/constructor_function_argument.sol
// Stresses: an input field named `state`, `output` or `locals` — the identifiers the entry macros bind, shadowed from inside the input struct
// Port caveat: No Solidity analogue for the macro binding; the parameter-shadows-field shape is the original.
// Variant: placement=nested
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NsInputFieldNamedLikeMacroArgument2
{
};

struct NsInputFieldNamedLikeMacroArgument : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 accumulator;
        uint64 calls;
        uint64 stored;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().inner.accumulator;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 qpi;
        uint64 output;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.scratch = input.qpi + input.output;
        state.mut().inner.stored = locals.scratch;
        state.mut().inner.accumulator = state.get().inner.stored;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.accumulator = 0;
        state.mut().inner.calls = 0;
    }
};
