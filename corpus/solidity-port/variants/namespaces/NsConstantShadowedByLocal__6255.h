// NsConstantShadowedByLocal — ported from test/libsolidity/semanticTests/scoping/name_shadowing.sol
// Stresses: a `_locals` member with the same name as a namespaced constant, with both read in the same body — the local wins unqualified, the constant is still reachable qualified
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Port
{
static constexpr uint64 factor = 9;
}

struct NsConstantShadowedByLocal2
{
};

struct NsConstantShadowedByLocal : public ContractBase
{
    struct Scratch
    {
        uint64 snapFactor;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 product;
        uint64 fromNamespace;
        uint64 fromLocal;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromLocal;
        uint64 fromNamespace;
        uint64 product;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromLocal = state.get().inner.fromLocal;
        output.fromNamespace = state.get().inner.fromNamespace;
        output.product = state.get().inner.product;
        output.calls = state.get().inner.calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().inner.scratch.snapFactor = 4;
        state.mut().inner.fromLocal = state.mut().inner.scratch.snapFactor;
        state.mut().inner.fromNamespace = Port::factor;
        state.mut().inner.product = state.mut().inner.scratch.snapFactor * Port::factor;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.fromLocal = 0;
        state.mut().inner.fromNamespace = 0;
        state.mut().inner.product = 0;
        state.mut().inner.calls = 0;
    }
};
