// NsBlockScopeShadowChain — ported from test/libsolidity/semanticTests/scoping/c99_scoping_activation.sol
// Stresses: a name redeclared at three nested block depths with different values, read at each depth and again after the blocks close
// Port caveat: Solidity's c99_scoping test reads the outer `x` in `uint x = x;`. This is F217: clang accepts the contract and produces the C++ answer (1, 20, 300, 1, confirmed on core's WAMR host), while the TypeScript backend refuses it — `locals share one slot per name, so shadowing is not supported`, plus a second diagnostic that mis-reads the earlier use of the OUTER name as use-before-declaration. Pinned so the row notices if block scoping ever lands.
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint64 tier = 1;

struct NsBlockScopeShadowChain2
{
};

struct NsBlockScopeShadowChain : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
        uint64 snapAtOne;
        uint64 snapAtTwo;
        uint64 snapAtThree;
        uint64 snapAfterBlocks;
    };

    struct StateData
    {
        uint64 outer;
        uint64 depthOne;
        uint64 depthTwo;
        uint64 afterAll;
        uint64 calls;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 outer;
        uint64 depthOne;
        uint64 depthTwo;
        uint64 afterAll;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.outer = state.get().outer;
        output.depthOne = state.get().depthOne;
        output.depthTwo = state.get().depthTwo;
        output.afterAll = state.get().afterAll;
        output.calls = state.get().calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().scratch.snapAtOne = tier;
        {
        uint64 tier = 20;
        state.mut().scratch.snapAtTwo = tier;
        {
        uint64 tier = 300;
        state.mut().scratch.snapAtThree = tier;
        }
        }
        state.mut().scratch.snapAfterBlocks = tier;
        state.mut().outer = state.mut().scratch.snapAtOne;
        state.mut().depthOne = state.mut().scratch.snapAtTwo;
        state.mut().depthTwo = state.mut().scratch.snapAtThree;
        state.mut().afterAll = state.mut().scratch.snapAfterBlocks;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
