// NsSameNamedStructsDifferentFieldValues — ported from test/libsolidity/semanticTests/libraries/library_struct_size.sol
// Stresses: two same-named structs whose fields have the same names and the same widths but are written with different values through each spelling — identical layouts, so only the values can tell the resolution apart
// Port caveat: This is deliberately the case round 1's twin-struct archetypes could not catch: the sizes match, so a size comparison sees nothing.
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
struct Record
{
    uint64 a;
    uint64 b;
};
}

namespace Beta
{
struct Record
{
    uint64 a;
    uint64 b;
};
}

struct NsSameNamedStructsDifferentFieldValues2
{
};

struct NsSameNamedStructsDifferentFieldValues : public ContractBase
{
    struct Scratch
    {
        Alpha::Record snapAlpha;
        Beta::Record snapBeta;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 sizesEqual;
        uint64 betaB;
        uint64 betaA;
        uint64 alphaB;
        uint64 alphaA;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        Snap_input forwardedInput;
        Snap_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        locals.forwardedInput = input;
        CALL(SnapBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 alphaA;
        uint64 alphaB;
        uint64 betaA;
        uint64 betaB;
        uint64 sizesEqual;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using SnapBody_input = Snap_input;

    using SnapBody_output = Snap_output;

    PRIVATE_PROCEDURE(SnapBody)
    {
        state.mut().inner.scratch.snapAlpha.a = 11;
        state.mut().inner.scratch.snapAlpha.b = 22;
        state.mut().inner.scratch.snapBeta.a = 33;
        state.mut().inner.scratch.snapBeta.b = 44;
        state.mut().inner.alphaA = state.mut().inner.scratch.snapAlpha.a;
        state.mut().inner.alphaB = state.mut().inner.scratch.snapAlpha.b;
        state.mut().inner.betaA = state.mut().inner.scratch.snapBeta.a;
        state.mut().inner.betaB = state.mut().inner.scratch.snapBeta.b;
        state.mut().inner.sizesEqual = sizeof(Alpha::Record) == sizeof(Beta::Record) ? 1 : 0;

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.alphaA = state.get().inner.alphaA;
        output.alphaB = state.get().inner.alphaB;
        output.betaA = state.get().inner.betaA;
        output.betaB = state.get().inner.betaB;
        output.sizesEqual = state.get().inner.sizesEqual;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
