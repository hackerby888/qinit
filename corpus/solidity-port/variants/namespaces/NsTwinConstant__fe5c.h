// NsTwinConstant — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: a namespaced capacity constant with a same-named twin of a different value
// Variant: entryOrder=reversed entryShape=direct initStyle=absent ns=aliasOfAlias placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
static constexpr uint64 CAPACITY = 8;
}

struct NsTwinConstant2
{
};

struct NsTwinConstant : public ContractBase
{
    struct Scratch
    {
        uint64 fillI;
        uint64 fillTotal;
    };

    struct StateData
    {
        struct Inner
        {
        Array<uint64, Alpha::CAPACITY> slots;
        uint64 capacity;
        uint64 checksum;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 capacity;
        uint64 checksum;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.capacity = state.get().inner.capacity;
        output.checksum = state.get().inner.checksum;
    }

    struct Fill_input
    {
        uint64 count;
    };

    struct Fill_output {};

    PUBLIC_PROCEDURE(Fill)
    {
        state.mut().inner.capacity = Alpha::CAPACITY;
        state.mut().inner.scratch.fillTotal = 0;
        for (state.mut().inner.scratch.fillI = 0; state.mut().inner.scratch.fillI < input.count; state.mut().inner.scratch.fillI++)
        {
        state.mut().inner.slots.set(state.mut().inner.scratch.fillI, state.mut().inner.scratch.fillI + 1);
        }
        for (state.mut().inner.scratch.fillI = 0; state.mut().inner.scratch.fillI < Alpha::CAPACITY; state.mut().inner.scratch.fillI++)
        {
        state.mut().inner.scratch.fillTotal += state.get().inner.slots.get(state.mut().inner.scratch.fillI);
        }
        state.mut().inner.checksum = state.mut().inner.scratch.fillTotal;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Fill, 1);
    }
};
