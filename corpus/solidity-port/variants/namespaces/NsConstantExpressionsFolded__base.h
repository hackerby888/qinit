// NsConstantExpressionsFolded — ported from test/libsolidity/semanticTests/constants/constant_expressions.sol
// Stresses: constants defined in terms of other constants across namespaces, so the folded value depends on resolving a chain rather than a single name
// Variant: base
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Base
{
static constexpr uint64 unit = 7;
}

namespace Derived
{
static constexpr uint64 unit = Base::unit * 3;
static constexpr uint64 doubled = unit * 2;
}

struct NsConstantExpressionsFolded2
{
};

struct NsConstantExpressionsFolded : public ContractBase
{
    struct StateData
    {
        uint64 baseUnit;
        uint64 derivedUnit;
        uint64 derivedDoubled;
        uint64 check;
        uint64 calls;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 baseUnit;
        uint64 derivedUnit;
        uint64 derivedDoubled;
        uint64 check;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.baseUnit = state.get().baseUnit;
        output.derivedUnit = state.get().derivedUnit;
        output.derivedDoubled = state.get().derivedDoubled;
        output.check = state.get().check;
        output.calls = state.get().calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        state.mut().baseUnit = Base::unit;
        state.mut().derivedUnit = Derived::unit;
        state.mut().derivedDoubled = Derived::doubled;
        state.mut().check = Derived::doubled == Base::unit * 6 ? 1 : 0;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {
        state.mut().baseUnit = 0;
        state.mut().derivedUnit = 0;
        state.mut().derivedDoubled = 0;
        state.mut().check = 0;
        state.mut().calls = 0;
    }
};
