// NsBaseMemberVersusFileConstant — ported from test/libsolidity/semanticTests/inheritance/inherited_state_var_shadowing.sol
// Stresses: a base-class member and a file-scope constant sharing a name: the member read through the object must not pick up the constant, and the bare name must not pick up the member
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint64 ceiling = 11;

struct Holder
{
uint64 ceiling;
};

struct NsBaseMemberVersusFileConstant2
{
};

struct NsBaseMemberVersusFileConstant : public ContractBase
{
    struct Scratch
    {
        uint64 snapScratch;
        Holder snapHolder;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 witness;
        uint64 sum;
        uint64 fromMember;
        uint64 fromConstant;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromConstant;
        uint64 fromMember;
        uint64 sum;
        uint64 witness;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromConstant = state.get().fromConstant;
        output.fromMember = state.get().fromMember;
        output.sum = state.get().sum;
        output.witness = state.get().witness;
        output.calls = state.get().calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    PUBLIC_PROCEDURE(Snap)
    {
        state.mut().scratch.snapHolder.ceiling = 640;
        state.mut().fromConstant = ceiling;
        state.mut().fromMember = state.mut().scratch.snapHolder.ceiling;
        state.mut().sum = ceiling + state.mut().scratch.snapHolder.ceiling;
        state.mut().witness = 1;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {

    }
};
