// NsTwinEnum — ported from test/libsolidity/semanticTests/enums/enum_explicit_overflow.sol
// Stresses: namespaced enum underlying width
// Variant: entryOrder=reversed entryShape=direct initStyle=full ns=collision placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
enum Status : uint64 { Idle = 0, Busy = 1, Done = 2 };
}

namespace Beta
{
enum Status : uint8 { Idle = 0, Busy = 1, Done = 2 };
}

struct NsTwinEnum2
{
};

struct NsTwinEnum : public ContractBase
{
    struct Scratch
    {
        uint64 advanceScratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 observed;
        uint64 statusSize;
        Alpha::Status status;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 statusSize;
        uint64 observed;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.statusSize = state.get().statusSize;
        output.observed = state.get().observed;
    }

    struct Advance_input
    {
        uint64 value;
    };

    struct Advance_output {};

    PUBLIC_PROCEDURE(Advance)
    {
        state.mut().status = (Alpha::Status)QPI::mod(input.value, 3ULL);
        state.mut().statusSize = sizeof(Alpha::Status);
        state.mut().observed = (uint64)state.get().status;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Advance, 1);
    }

    INITIALIZE()
    {
        state.mut().statusSize = 0;
        state.mut().observed = 0;
    }
};
