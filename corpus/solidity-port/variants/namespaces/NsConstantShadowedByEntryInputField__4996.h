// NsConstantShadowedByEntryInputField — ported from test/libsolidity/semanticTests/scoping/scoping_activation.sol
// Stresses: an entry input field named exactly like a file-scope constant, so `input.seed` and the bare constant must stay distinct
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint64 seed = 77;

struct NsConstantShadowedByEntryInputField2
{
};

struct NsConstantShadowedByEntryInputField : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 witness;
        uint64 sum;
        uint64 fromInput;
        uint64 fromConstant;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromConstant;
        uint64 fromInput;
        uint64 sum;
        uint64 witness;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromConstant = state.get().fromConstant;
        output.fromInput = state.get().fromInput;
        output.sum = state.get().sum;
        output.witness = state.get().witness;
        output.calls = state.get().calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        state.mut().fromConstant = seed;
        state.mut().fromInput = input.seed;
        state.mut().sum = seed + input.seed;
        state.mut().witness = 1;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {

    }
};
