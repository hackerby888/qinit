// NsSameNameThreeKindsOneExpression — ported from test/libsolidity/semanticTests/scoping/scoping_activation.sol
// Stresses: a file constant, an enum constant and a local all named the same, combined in one expression so a wrong pick changes the arithmetic rather than only one member
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace AsConst
{
static constexpr uint64 span = 2;
}

namespace AsEnum
{
enum Widths
{
span = 30
};
}

struct NsSameNameThreeKindsOneExpression2
{
};

struct NsSameNameThreeKindsOneExpression : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 combined;
        uint64 enumValue;
        uint64 constValue;
        uint64 localValue;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        Snap_input forwardedInput;
        Snap_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        locals.forwardedInput = input;
        CALL(SnapBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 localValue;
        uint64 constValue;
        uint64 enumValue;
        uint64 combined;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using SnapBody_input = Snap_input;

    using SnapBody_output = Snap_output;

    struct SnapBody_locals
    {
        uint64 scratch;
        uint64 span;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(SnapBody)
    {
        locals.span = 500;
        state.mut().inner.localValue = locals.span;
        state.mut().inner.constValue = AsConst::span;
        state.mut().inner.enumValue = AsEnum::span;
        state.mut().inner.combined = locals.span + AsConst::span + AsEnum::span;

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.localValue = state.get().inner.localValue;
        output.constValue = state.get().inner.constValue;
        output.enumValue = state.get().inner.enumValue;
        output.combined = state.get().inner.combined;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
