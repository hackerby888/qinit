// NsQualifiedAndUnqualifiedInOneExpression — ported from test/libsolidity/semanticTests/using/using_for_function_on_int.sol
// Stresses: two spellings of one name in a single expression
// Variant: constSource=constexpr entryOrder=declared entryShape=viaPrivate initStyle=empty placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Port
{
static constexpr uint64 factor = 3;
}

using namespace Port;

static constexpr uint64 offset = 11;

struct NsQualifiedAndUnqualifiedInOneExpression2
{
};

struct NsQualifiedAndUnqualifiedInOneExpression : public ContractBase
{
    struct Scratch
    {
        uint64 computeScratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 product;
        uint64 viaQualified;
        uint64 viaUnqualified;
        uint64 calls;
        Scratch scratch;
    };

    struct Compute_input
    {
        uint64 value;
    };

    struct Compute_output {};

    struct Compute_locals
    {
        Compute_input forwardedInput;
        Compute_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Compute)
    {
        locals.forwardedInput = input;
        CALL(ComputeBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 product;
        uint64 viaQualified;
        uint64 viaUnqualified;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using ComputeBody_input = Compute_input;

    using ComputeBody_output = Compute_output;

    PRIVATE_PROCEDURE(ComputeBody)
    {
        state.mut().scratch.computeScratch = input.value;
        state.mut().viaQualified = state.mut().scratch.computeScratch * Port::factor + offset;
        state.mut().viaUnqualified = state.mut().scratch.computeScratch * factor + offset;
        state.mut().product = state.get().viaQualified * state.get().viaUnqualified;
        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.product = state.get().product;
        output.viaQualified = state.get().viaQualified;
        output.viaUnqualified = state.get().viaUnqualified;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Compute, 1);
    }

    INITIALIZE()
    {

    }
};
