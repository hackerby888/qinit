// NsTwinWithIdField — ported from test/libsolidity/semanticTests/types/address_types.sol
// Stresses: a namespaced struct carrying a 32-byte id, where a wrong resolution changes alignment as well as size
// Variant: entryOrder=declared entryShape=direct initStyle=absent ns=aliasOfAlias placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Port
{
struct Entry
{
    id owner;
    uint64 wide;
};
using EntryA = Entry;
using EntryB = EntryA;
using EntryC = EntryB;
}

struct PlainEntry
{
    id owner;
    uint64 wide;
};

struct NsTwinWithIdField2
{
};

struct NsTwinWithIdField : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        Port::EntryC qualified;
        PlainEntry plain;
        uint64 qualifiedSize;
        uint64 plainSize;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 qualifiedSize;
        uint64 plainSize;
        uint64 payload;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.qualifiedSize = state.get().inner.qualifiedSize;
        output.plainSize = state.get().inner.plainSize;
        output.payload = (uint64)state.get().inner.qualified.wide;
    }

    struct Write_input
    {
        uint64 value;
    };

    struct Write_output {};

    struct Write_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Write)
    {
        state.mut().inner.qualified.wide = input.value;
        state.mut().inner.plain.wide = input.value;
        state.mut().inner.qualifiedSize = sizeof(Port::EntryC);
        state.mut().inner.plainSize = sizeof(PlainEntry);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }
};
