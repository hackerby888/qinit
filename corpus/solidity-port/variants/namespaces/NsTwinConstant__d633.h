// NsTwinConstant — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: a namespaced capacity constant with a same-named twin of a different value
// Variant: entryOrder=reversed entryShape=direct initStyle=empty ns=global placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Alpha
{
static constexpr uint64 CAPACITY = 8;
}

struct NsTwinConstant2
{
};

struct NsTwinConstant : public ContractBase
{
    struct Scratch
    {
        uint64 fillI;
        uint64 fillTotal;
    };

    struct StateData
    {
        uint64 placementGuard;
        Array<uint64, Alpha::CAPACITY> slots;
        uint64 capacity;
        uint64 checksum;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 capacity;
        uint64 checksum;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.capacity = state.get().capacity;
        output.checksum = state.get().checksum;
    }

    struct Fill_input
    {
        uint64 count;
    };

    struct Fill_output {};

    PUBLIC_PROCEDURE(Fill)
    {
        state.mut().capacity = Alpha::CAPACITY;
        state.mut().scratch.fillTotal = 0;
        for (state.mut().scratch.fillI = 0; state.mut().scratch.fillI < input.count; state.mut().scratch.fillI++)
        {
        state.mut().slots.set(state.mut().scratch.fillI, state.mut().scratch.fillI + 1);
        }
        for (state.mut().scratch.fillI = 0; state.mut().scratch.fillI < Alpha::CAPACITY; state.mut().scratch.fillI++)
        {
        state.mut().scratch.fillTotal += state.get().slots.get(state.mut().scratch.fillI);
        }
        state.mut().checksum = state.mut().scratch.fillTotal;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Fill, 1);
    }

    INITIALIZE()
    {

    }
};
