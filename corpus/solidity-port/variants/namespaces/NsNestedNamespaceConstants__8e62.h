// NsNestedNamespaceConstants — ported from test/libsolidity/semanticTests/constants/constant_variables.sol
// Stresses: constants of one name at three nesting depths — Outer, Outer::Middle and Outer::Middle::Deep — where the qualification length is the only thing distinguishing them
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Outer
{
static constexpr uint64 value = 2;
namespace Middle
{
static constexpr uint64 value = 20;
namespace Deep
{
static constexpr uint64 value = 200;
}
}
}

struct NsNestedNamespaceConstants2
{
};

struct NsNestedNamespaceConstants : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 outer;
        uint64 middle;
        uint64 deep;
        uint64 weighted;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        Snap_input forwardedInput;
        Snap_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        locals.forwardedInput = input;
        CALL(SnapBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 outer;
        uint64 middle;
        uint64 deep;
        uint64 weighted;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using SnapBody_input = Snap_input;

    using SnapBody_output = Snap_output;

    struct SnapBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(SnapBody)
    {
        state.mut().inner.outer = Outer::value;
        state.mut().inner.middle = Outer::Middle::value;
        state.mut().inner.deep = Outer::Middle::Deep::value;
        state.mut().inner.weighted = Outer::value * 100 + Outer::Middle::value * 10 + Outer::Middle::Deep::value;

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.outer = state.get().inner.outer;
        output.middle = state.get().inner.middle;
        output.deep = state.get().inner.deep;
        output.weighted = state.get().inner.weighted;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.outer = 0;
        state.mut().inner.middle = 0;
        state.mut().inner.deep = 0;
        state.mut().inner.weighted = 0;
        state.mut().inner.calls = 0;
    }
};
