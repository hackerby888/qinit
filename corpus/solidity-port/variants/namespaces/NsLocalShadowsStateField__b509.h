// NsLocalShadowsStateField — ported from test/libsolidity/semanticTests/scoping/scoping_local_variables.sol
// Stresses: a locals member shadowing a state member name
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NsLocalShadowsStateField2
{
};

struct NsLocalShadowsStateField : public ContractBase
{
    struct Scratch
    {
        uint64 bumpTotal;
        uint64 bumpCounter;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 witness;
        uint64 counter;
        uint64 total;
        Scratch scratch;
    };

    struct Bump_input
    {
        uint64 delta;
    };

    struct Bump_output {};

    PUBLIC_PROCEDURE(Bump)
    {
        state.mut().scratch.bumpTotal = state.get().total + input.delta;
        state.mut().scratch.bumpCounter = state.get().counter + 1;
        state.mut().total = state.mut().scratch.bumpTotal;
        state.mut().counter = state.mut().scratch.bumpCounter;
        state.mut().witness = state.mut().scratch.bumpTotal + state.mut().scratch.bumpCounter;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 total;
        uint64 counter;
        uint64 witness;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.total = state.get().total;
        output.counter = state.get().counter;
        output.witness = state.get().witness;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Bump, 1);
    }

    INITIALIZE()
    {
        state.mut().total = 0;
        state.mut().counter = 0;
        state.mut().witness = 0;
    }
};
