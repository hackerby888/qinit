// NsConstantShadowedByLocal — ported from test/libsolidity/semanticTests/scoping/name_shadowing.sol
// Stresses: a `_locals` member with the same name as a namespaced constant, with both read in the same body — the local wins unqualified, the constant is still reachable qualified
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace Port
{
static constexpr uint64 factor = 9;
}

struct NsConstantShadowedByLocal2
{
};

struct NsConstantShadowedByLocal : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 fromLocal;
        uint64 fromNamespace;
        uint64 product;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        uint64 factor;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        locals.factor = 4;
        state.mut().inner.fromLocal = locals.factor;
        state.mut().inner.fromNamespace = Port::factor;
        state.mut().inner.product = locals.factor * Port::factor;

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 fromLocal;
        uint64 fromNamespace;
        uint64 product;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromLocal = state.get().inner.fromLocal;
        output.fromNamespace = state.get().inner.fromNamespace;
        output.product = state.get().inner.product;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }

    INITIALIZE()
    {

    }
};
