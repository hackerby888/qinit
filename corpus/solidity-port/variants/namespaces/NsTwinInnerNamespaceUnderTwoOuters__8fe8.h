// NsTwinInnerNamespaceUnderTwoOuters — ported from test/libsolidity/semanticTests/scoping/scoping.sol
// Stresses: the same inner namespace name nested under two different outer namespaces, each declaring the same constant name with a different value
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

namespace OuterOne
{
namespace Shared
{
static constexpr uint64 depth = 7;
}
}

namespace OuterTwo
{
namespace Shared
{
static constexpr uint64 depth = 5000;
}
}

struct NsTwinInnerNamespaceUnderTwoOuters2
{
};

struct NsTwinInnerNamespaceUnderTwoOuters : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 fromOne;
        uint64 fromTwo;
        uint64 sum;
        uint64 product;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 fromOne;
        uint64 fromTwo;
        uint64 sum;
        uint64 product;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromOne = state.get().inner.fromOne;
        output.fromTwo = state.get().inner.fromTwo;
        output.sum = state.get().inner.sum;
        output.product = state.get().inner.product;
        output.calls = state.get().inner.calls;
    }

    struct Snap_input
    {
        uint64 seed;
    };

    struct Snap_output {};

    struct Snap_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Snap)
    {
        state.mut().inner.fromOne = OuterOne::Shared::depth;
        state.mut().inner.fromTwo = OuterTwo::Shared::depth;
        state.mut().inner.sum = OuterOne::Shared::depth + OuterTwo::Shared::depth;
        state.mut().inner.product = OuterOne::Shared::depth * OuterTwo::Shared::depth;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Snap, 1);
    }
};
