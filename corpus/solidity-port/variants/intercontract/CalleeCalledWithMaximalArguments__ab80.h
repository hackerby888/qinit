// CalleeCalledWithMaximalArguments — ported from test/libsolidity/semanticTests/functionCall/external_call.sol
// Stresses: every argument field at its maximum in one call — the widest values that can cross the boundary, where a truncated marshalling is immediately visible in the callee's record
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CalleeCalledWithMaximalArguments2
{
};

struct CalleeCalledWithMaximalArguments : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 sumBack;
        uint64 calls;
        uint64 acceptedCount;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 sumBack;
        uint64 calls;
        uint64 acceptedCount;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.sumBack = state.get().sumBack;
        output.calls = state.get().calls;
        output.acceptedCount = state.get().acceptedCount;
    }

    struct Max_input
    {
        uint64 unused;
    };

    struct Max_output {};

    struct Max_locals
    {
        EchoEleven::Accept_input request;
        EchoEleven::Accept_output reply;
        uint64 i;
        uint64 maxSeed;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Max)
    {
        locals.request.amount = 18446744073709551615ULL;
        locals.request.tag = 4294967295;
        locals.request.flag = 1;
        locals.maxSeed = 18446744073709551615ULL;
        // Hashed from a named local: K12 of an expression is F203's territory, and this
        // archetype is about marshalling rather than about that.
        locals.request.who = qpi.K12(locals.maxSeed);
        for (locals.i = 0; locals.i < 4; locals.i++)
        {
        locals.request.values.set(locals.i, 18446744073709551615ULL);
        }
        INVOKE_OTHER_CONTRACT_PROCEDURE(EchoEleven, Accept, locals.request, locals.reply, 0);
        state.mut().sumBack = locals.reply.sum;
        state.mut().acceptedCount += locals.reply.accepted;
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Max, 1);
    }
};
