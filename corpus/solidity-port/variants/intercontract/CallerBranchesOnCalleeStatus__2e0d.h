// CallerBranchesOnCalleeStatus — ported from test/libsolidity/semanticTests/functionCall/external_call_return_value.sol
// Stresses: the caller taking a different path depending on the callee's status word, with both paths writing different members — the branch is on data that crossed a contract boundary
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CallerBranchesOnCalleeStatus2
{
};

struct CallerBranchesOnCalleeStatus : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 acceptedPath;
        uint64 refusedPath;
        uint64 lastSum;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 acceptedPath;
        uint64 refusedPath;
        uint64 lastSum;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Try_input
    {
        uint64 amount;
        uint64 flag;
    };

    struct Try_output {};

    struct Try_locals
    {
        Try_input forwardedInput;
        Try_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Try)
    {
        locals.forwardedInput = input;
        CALL(TryBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.acceptedPath = state.get().inner.acceptedPath;
        output.refusedPath = state.get().inner.refusedPath;
        output.lastSum = state.get().inner.lastSum;
        output.calls = state.get().inner.calls;
    }

    using TryBody_input = Try_input;

    using TryBody_output = Try_output;

    struct TryBody_locals
    {
        EchoTwo::Accept_input request;
        EchoTwo::Accept_output reply;
        uint64 i;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(TryBody)
    {
        locals.request.amount = input.amount;
        locals.request.tag = (uint32)input.amount;
        locals.request.flag = (uint8)input.flag;
        locals.request.who = SELF;
        for (locals.i = 0; locals.i < 4; locals.i++)
        {
        locals.request.values.set(locals.i, locals.i);
        }
        INVOKE_OTHER_CONTRACT_PROCEDURE(EchoTwo, Accept, locals.request, locals.reply, 0);
        if (locals.reply.accepted != 0)
        {
        state.mut().inner.acceptedPath += input.amount;
        }
        else
        {
        state.mut().inner.refusedPath += input.amount;
        }
        state.mut().inner.lastSum = locals.reply.sum;
        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Try, 1);
    }

    INITIALIZE()
    {

    }
};
