// CalleeSharedStructDefinition — ported from multiSource/
// Stresses: a struct defined identically in caller and callee
// Variant: entryOrder=reversed entryShape=direct initStyle=empty layout=widestFirst placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CalleeSharedStructDefinition2
{
};

struct CalleeSharedStructDefinition : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 envelopeSize;
        uint64 echoed;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Send_input
    {
        uint64 wide;
        uint64 medium;
        uint64 tiny;
    };

    struct Send_output {};

    struct Send_locals
    {
        EnvelopeLedger::Accept_input request;
        EnvelopeLedger::Accept_output reply;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Send)
    {
        locals.request.envelope.wide = input.wide;
        locals.request.envelope.medium = (uint32)input.medium;
        locals.request.envelope.tiny = (uint8)input.tiny;
        INVOKE_OTHER_CONTRACT_PROCEDURE(EnvelopeLedger, Accept, locals.request, locals.reply, 0);
        state.mut().inner.echoed = locals.reply.echoed;
        state.mut().inner.envelopeSize = sizeof(locals.request.envelope);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 echoed;
        uint64 envelopeSize;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.echoed = state.get().inner.echoed;
        output.envelopeSize = state.get().inner.envelopeSize;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Send, 1);
    }

    INITIALIZE()
    {

    }
};
