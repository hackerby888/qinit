// CallerBranchesOnCalleeStatus — ported from test/libsolidity/semanticTests/functionCall/external_call_return_value.sol
// Stresses: the caller taking a different path depending on the callee's status word, with both paths writing different members — the branch is on data that crossed a contract boundary
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CallerBranchesOnCalleeStatus2
{
};

struct CallerBranchesOnCalleeStatus : public ContractBase
{
    struct StateData
    {
        uint64 calls;
        uint64 lastSum;
        uint64 refusedPath;
        uint64 acceptedPath;
        uint64 placementGuard;
    };

    struct Try_input
    {
        uint64 amount;
        uint64 flag;
    };

    struct Try_output {};

    struct Try_locals
    {
        EchoTwo::Accept_input request;
        EchoTwo::Accept_output reply;
        uint64 i;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Try)
    {
        locals.request.amount = input.amount;
        locals.request.tag = (uint32)input.amount;
        locals.request.flag = (uint8)input.flag;
        locals.request.who = SELF;
        for (locals.i = 0; locals.i < 4; locals.i++)
        {
        locals.request.values.set(locals.i, locals.i);
        }
        INVOKE_OTHER_CONTRACT_PROCEDURE(EchoTwo, Accept, locals.request, locals.reply, 0);
        if (locals.reply.accepted != 0)
        {
        state.mut().acceptedPath += input.amount;
        }
        else
        {
        state.mut().refusedPath += input.amount;
        }
        state.mut().lastSum = locals.reply.sum;
        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 acceptedPath;
        uint64 refusedPath;
        uint64 lastSum;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.acceptedPath = state.get().acceptedPath;
        output.refusedPath = state.get().refusedPath;
        output.lastSum = state.get().lastSum;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Try, 1);
    }

    INITIALIZE()
    {

    }
};
