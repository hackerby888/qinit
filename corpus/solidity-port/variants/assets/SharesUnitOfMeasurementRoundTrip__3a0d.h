// SharesUnitOfMeasurementRoundTrip — ported from openzeppelin-contracts/contracts/token/ERC20/extensions/ERC20Metadata.sol (decimals)
// Stresses: the unit-of-measurement word carried through issuance — an eight-byte field the host stores and the contract never reads back, so its only effect is on whether the issuance is accepted
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=first stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SharesUnitOfMeasurementRoundTrip2
{
};

struct SharesUnitOfMeasurementRoundTrip : public ContractBase
{
    struct StateData
    {
        uint64 calls;
        uint64 lastUnit;
        uint64 distinctUnits;
        uint64 accepted;
        sint64 result;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        sint64 outcome;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.outcome = qpi.issueAsset(5525825ULL + QPI::mod(input.b, 8ULL), SELF, 0, 100, input.a);
        state.mut().result = locals.outcome;
        if (locals.outcome > 0)
        {
        state.mut().accepted++;
        }
        if (input.a != state.get().lastUnit)
        {
        state.mut().distinctUnits++;
        }
        state.mut().lastUnit = input.a;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 result;
        uint64 accepted;
        uint64 distinctUnits;
        uint64 lastUnit;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.result = state.get().result;
        output.accepted = state.get().accepted;
        output.distinctUnits = state.get().distinctUnits;
        output.lastUnit = state.get().lastUnit;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
