// SharesOwnershipVersusPossession — ported from openzeppelin-contracts/contracts/token/ERC721/ERC721.sol (ownerOf versus approved)
// Stresses: numberOfPossessedShares asked with the contract as owner and as possessor, and with a stranger in each position — four combinations of the same query
// Port caveat: Solidity's ERC721 separates owner from approved operator; QPI separates ownership from possession at the protocol level, which is close but not the same split.
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SharesOwnershipVersusPossession2
{
};

struct SharesOwnershipVersusPossession : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        sint64 issued;
        sint64 strangerStranger;
        sint64 strangerSelf;
        sint64 selfStranger;
        sint64 selfSelf;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        id stranger;
        sint64 outcome;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.stranger = qpi.K12(input.b);
        locals.outcome = qpi.issueAsset(5525825ULL, SELF, 0, (sint64)QPI::mod(input.a, 100000ULL), 0);
        state.mut().inner.issued = locals.outcome;
        state.mut().inner.selfSelf = qpi.numberOfPossessedShares(5525825ULL, SELF, SELF, SELF, SELF_INDEX, SELF_INDEX);
        state.mut().inner.selfStranger = qpi.numberOfPossessedShares(5525825ULL, SELF, SELF, locals.stranger, SELF_INDEX, SELF_INDEX);
        state.mut().inner.strangerSelf = qpi.numberOfPossessedShares(5525825ULL, SELF, locals.stranger, SELF, SELF_INDEX, SELF_INDEX);
        state.mut().inner.strangerStranger = qpi.numberOfPossessedShares(5525825ULL, SELF, locals.stranger, locals.stranger, SELF_INDEX, SELF_INDEX);

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 selfSelf;
        sint64 selfStranger;
        sint64 strangerSelf;
        sint64 strangerStranger;
        sint64 issued;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.selfSelf = state.get().inner.selfSelf;
        output.selfStranger = state.get().inner.selfStranger;
        output.strangerSelf = state.get().inner.strangerSelf;
        output.strangerStranger = state.get().inner.strangerStranger;
        output.issued = state.get().inner.issued;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
