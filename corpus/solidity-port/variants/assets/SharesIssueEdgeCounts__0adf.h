// SharesIssueEdgeCounts — ported from openzeppelin-contracts/contracts/token/ERC20/ERC20.sol (_mint)
// Stresses: issuance at zero, one, the maximum and a negative count — four share counts the host validates differently, with the returned code stored each time
// Port caveat: Solidity's _mint takes an unsigned amount; QPI's issueAsset takes a signed one, so the negative half of the range exists only in the port.
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SharesIssueEdgeCounts2
{
};

struct SharesIssueEdgeCounts : public ContractBase
{
    struct Scratch
    {
        sint64 runOutcome;
    };

    struct StateData
    {
        sint64 result;
        sint64 possessed;
        uint64 accepted;
        uint64 refused;
        uint64 calls;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runOutcome = qpi.issueAsset(5525825ULL, SELF, 0, (sint64)input.a, 0);
        state.mut().result = state.mut().scratch.runOutcome;
        if (state.mut().scratch.runOutcome > 0)
        {
        state.mut().accepted++;
        }
        else
        {
        state.mut().refused++;
        }
        state.mut().possessed = qpi.numberOfPossessedShares(5525825ULL, SELF, SELF, SELF, SELF_INDEX, SELF_INDEX);

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 result;
        sint64 possessed;
        uint64 accepted;
        uint64 refused;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.result = state.get().result;
        output.possessed = state.get().possessed;
        output.accepted = state.get().accepted;
        output.refused = state.get().refused;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
