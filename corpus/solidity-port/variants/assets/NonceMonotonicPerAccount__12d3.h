// NonceMonotonicPerAccount — ported from openzeppelin-contracts/contracts/utils/Nonces.sol
// Stresses: nonce checked then incremented, with replays and gaps driven
// Port caveat: revert becomes an early return before the increment
// Variant: capacity=64 entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NonceMonotonicPerAccount2
{
};

struct NonceMonotonicPerAccount : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 gaps;
        uint64 replays;
        uint64 accepted;
        HashMap<id, uint64, 64> nonces;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Use_input
    {
        uint64 nonce;
    };

    struct Use_output
    {
        uint64 ok;
    };

    struct Use_locals
    {
        id who;
        uint64 expected;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Use)
    {
        locals.who = qpi.invocator();
        locals.expected = 0;
        state.get().inner.nonces.get(locals.who, locals.expected);
        if (input.nonce < locals.expected)
        {
        state.mut().inner.replays++;
        output.ok = 0;
        return;
        }
        if (input.nonce > locals.expected)
        {
        state.mut().inner.gaps++;
        output.ok = 0;
        return;
        }
        state.mut().inner.nonces.set(locals.who, locals.expected + 1);
        state.mut().inner.accepted++;
        output.ok = 1;
    }

    struct Read_input
    {
        id who;
    };

    struct Read_output
    {
        uint64 next;
        uint64 accepted;
        uint64 replays;
        uint64 gaps;
    };

    struct Read_locals
    {
        uint64 fetched;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        locals.fetched = 0;
        state.get().inner.nonces.get(input.who, locals.fetched);
        output.next = locals.fetched;
        output.accepted = state.get().inner.accepted;
        output.replays = state.get().inner.replays;
        output.gaps = state.get().inner.gaps;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Use, 1);
    }

    INITIALIZE()
    {

    }
};
