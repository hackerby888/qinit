// SharesIssueAndCount — ported from openzeppelin-contracts/contracts/token/ERC20/ERC20.sol
// Stresses: issueAsset and numberOfPossessedShares, mirrored into state
// Port caveat: the asset universe is host state; only the mirror is in the digest
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SharesIssueAndCount2
{
};

struct SharesIssueAndCount : public ContractBase
{
    struct Scratch
    {
        sint64 issueResult;
        sint64 mirrorShares;
    };

    struct StateData
    {
        uint64 calls;
        sint64 issuedFlag;
        sint64 possessed;
        sint64 issueResult;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Issue_input
    {
        uint64 name;
        sint64 shares;
    };

    struct Issue_output {};

    struct Issue_locals
    {
        Issue_input forwardedInput;
        Issue_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Issue)
    {
        locals.forwardedInput = input;
        CALL(IssueBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Mirror_input {};

    struct Mirror_output {};

    struct Mirror_locals
    {
        Mirror_input forwardedInput;
        Mirror_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Mirror)
    {
        CALL(MirrorBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 issueResult;
        sint64 possessed;
        sint64 issuedFlag;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using IssueBody_input = Issue_input;

    using IssueBody_output = Issue_output;

    PRIVATE_PROCEDURE(IssueBody)
    {
        state.mut().scratch.issueResult = qpi.issueAsset(input.name, SELF, 0, input.shares, 0);
        state.mut().issueResult = state.mut().scratch.issueResult;
        state.mut().calls++;
    }

    using MirrorBody_input = Mirror_input;

    using MirrorBody_output = Mirror_output;

    PRIVATE_PROCEDURE(MirrorBody)
    {
        state.mut().scratch.mirrorShares = qpi.numberOfPossessedShares(5521745ULL, SELF, SELF, SELF, SELF_INDEX, SELF_INDEX);
        state.mut().possessed = state.mut().scratch.mirrorShares;
        state.mut().issuedFlag = qpi.isAssetIssued(SELF, 5521745ULL) ? 1 : 0;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    struct ReadBody_locals
    {
        sint64 shares;
    };

    PRIVATE_FUNCTION_WITH_LOCALS(ReadBody)
    {
        locals.shares = qpi.numberOfPossessedShares(5521745ULL, SELF, SELF, SELF, SELF_INDEX, SELF_INDEX);
        output.issueResult = state.get().issueResult;
        output.possessed = locals.shares;
        output.issuedFlag = qpi.isAssetIssued(SELF, 5521745ULL) ? 1 : 0;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Issue, 1);
        REGISTER_USER_PROCEDURE(Mirror, 2);
    }

    INITIALIZE()
    {
        state.mut().issueResult = 0;
        state.mut().possessed = 0;
        state.mut().issuedFlag = 0;
        state.mut().calls = 0;
    }
};
