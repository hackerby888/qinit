// AssetOwnershipIteratorOwnerFilterIgnored — ported from openzeppelin-contracts/contracts/token/ERC20/extensions/ERC20Snapshot.sol (per-holder balance enumeration)
// Stresses: an AssetOwnershipIterator opened with AssetOwnershipSelect::byOwner — the filter is accepted, compiles and runs, and the walk it produces is compared against the same walk opened with any()
// Port caveat: The Solidity original enumerates a mapping it controls; QPI enumerates the host's ledger, so the port mirrors what it reads into state to make the difference visible to a state digest.
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct AssetOwnershipIteratorOwnerFilterIgnored2
{
};

struct AssetOwnershipIteratorOwnerFilterIgnored : public ContractBase
{
    struct Scratch
    {
        id probeOther;
        Asset probeAsset;
        AssetOwnershipIterator probeIter;
        uint64 probeGuard;
        sint64 probeOutcome;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 unfilteredShares;
        uint64 unfilteredCount;
        uint64 filteredShares;
        uint64 filteredCount;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 filteredCount;
        uint64 filteredShares;
        uint64 unfilteredCount;
        uint64 unfilteredShares;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Probe_input
    {
        uint64 a;
        uint64 b;
    };

    struct Probe_output {};

    struct Probe_locals
    {
        Probe_input forwardedInput;
        Probe_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Probe)
    {
        locals.forwardedInput = input;
        CALL(ProbeBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.filteredCount = state.get().inner.filteredCount;
        output.filteredShares = state.get().inner.filteredShares;
        output.unfilteredCount = state.get().inner.unfilteredCount;
        output.unfilteredShares = state.get().inner.unfilteredShares;
        output.calls = state.get().inner.calls;
    }

    using ProbeBody_input = Probe_input;

    using ProbeBody_output = Probe_output;

    PRIVATE_PROCEDURE(ProbeBody)
    {
        state.mut().inner.scratch.probeOther = id(input.a, 0, 0, 0);
        state.mut().inner.scratch.probeOutcome = qpi.issueAsset(5521745ULL, SELF, 0, 1000, 0);
        state.mut().inner.scratch.probeOutcome = qpi.transferShareOwnershipAndPossession(5521745ULL, SELF, SELF, SELF, input.b, state.mut().inner.scratch.probeOther);
        state.mut().inner.scratch.probeAsset.issuer = SELF;
        state.mut().inner.scratch.probeAsset.assetName = 5521745ULL;

        state.mut().inner.scratch.probeIter.begin(state.mut().inner.scratch.probeAsset, AssetOwnershipSelect::byOwner(state.mut().inner.scratch.probeOther));
        state.mut().inner.scratch.probeGuard = 0;
        while (!state.mut().inner.scratch.probeIter.reachedEnd() && state.mut().inner.scratch.probeGuard < 16)
        {
        state.mut().inner.filteredCount++;
        state.mut().inner.filteredShares += state.mut().inner.scratch.probeIter.numberOfOwnedShares();
        state.mut().inner.scratch.probeIter.next();
        state.mut().inner.scratch.probeGuard++;
        }

        // The same walk with no filter, as the control that separates "the filter was
        // dropped" from "the iteration is broken".
        state.mut().inner.scratch.probeIter.begin(state.mut().inner.scratch.probeAsset, AssetOwnershipSelect::any());
        state.mut().inner.scratch.probeGuard = 0;
        while (!state.mut().inner.scratch.probeIter.reachedEnd() && state.mut().inner.scratch.probeGuard < 16)
        {
        state.mut().inner.unfilteredCount++;
        state.mut().inner.unfilteredShares += state.mut().inner.scratch.probeIter.numberOfOwnedShares();
        state.mut().inner.scratch.probeIter.next();
        state.mut().inner.scratch.probeGuard++;
        }

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Probe, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.filteredCount = 0;
        state.mut().inner.filteredShares = 0;
        state.mut().inner.unfilteredCount = 0;
        state.mut().inner.unfilteredShares = 0;
        state.mut().inner.calls = 0;
    }
};
