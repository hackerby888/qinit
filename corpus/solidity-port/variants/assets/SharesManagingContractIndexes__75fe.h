// SharesManagingContractIndexes — ported from no Solidity analogue (managing contract indices)
// Stresses: the ownership- and possession-managing contract indices in a share query — two uint16 arguments that select whose rules apply, driven at this contract's index, at zero and past the end
// Port caveat: Ethereum has no notion of a managing contract for a token balance; this is protocol machinery with no original.
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SharesManagingContractIndexes2
{
};

struct SharesManagingContractIndexes : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        sint64 ownIndex;
        sint64 zeroIndex;
        sint64 farIndex;
        sint64 mixedIndex;
        uint64 calls;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 ownIndex;
        sint64 zeroIndex;
        sint64 farIndex;
        sint64 mixedIndex;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        sint64 outcome;
        uint16 far;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.outcome = qpi.issueAsset(5525825ULL, SELF, 0, 700, 0);
        locals.far = (uint16)QPI::mod(input.a, 65536ULL);
        state.mut().ownIndex = qpi.numberOfPossessedShares(5525825ULL, SELF, SELF, SELF, SELF_INDEX, SELF_INDEX);
        state.mut().zeroIndex = qpi.numberOfPossessedShares(5525825ULL, SELF, SELF, SELF, 0, 0);
        state.mut().farIndex = qpi.numberOfPossessedShares(5525825ULL, SELF, SELF, SELF, locals.far, locals.far);
        state.mut().mixedIndex = qpi.numberOfPossessedShares(5525825ULL, SELF, SELF, SELF, SELF_INDEX, 0);

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.ownIndex = state.get().ownIndex;
        output.zeroIndex = state.get().zeroIndex;
        output.farIndex = state.get().farIndex;
        output.mixedIndex = state.get().mixedIndex;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
