// NonceMonotonicPerAccount — ported from openzeppelin-contracts/contracts/utils/Nonces.sol
// Stresses: nonce checked then incremented, with replays and gaps driven
// Port caveat: revert becomes an early return before the increment
// Variant: capacity=1024 entryOrder=declared entryShape=direct initStyle=absent placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NonceMonotonicPerAccount2
{
};

struct NonceMonotonicPerAccount : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 gaps;
        uint64 replays;
        uint64 accepted;
        HashMap<id, uint64, 1024> nonces;
    };

    struct Read_input
    {
        id who;
    };

    struct Read_output
    {
        uint64 next;
        uint64 accepted;
        uint64 replays;
        uint64 gaps;
    };

    struct Read_locals
    {
        uint64 fetched;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        locals.fetched = 0;
        state.get().nonces.get(input.who, locals.fetched);
        output.next = locals.fetched;
        output.accepted = state.get().accepted;
        output.replays = state.get().replays;
        output.gaps = state.get().gaps;
    }

    struct Use_input
    {
        uint64 nonce;
    };

    struct Use_output
    {
        uint64 ok;
    };

    struct Use_locals
    {
        id who;
        uint64 expected;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Use)
    {
        locals.who = qpi.invocator();
        locals.expected = 0;
        state.get().nonces.get(locals.who, locals.expected);
        if (input.nonce < locals.expected)
        {
        state.mut().replays++;
        output.ok = 0;
        return;
        }
        if (input.nonce > locals.expected)
        {
        state.mut().gaps++;
        output.ok = 0;
        return;
        }
        state.mut().nonces.set(locals.who, locals.expected + 1);
        state.mut().accepted++;
        output.ok = 1;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Use, 1);
    }
};
