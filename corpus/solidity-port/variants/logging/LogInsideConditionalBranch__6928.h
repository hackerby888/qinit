// LogInsideConditionalBranch — ported from test/libsolidity/semanticTests/events/event_emit_in_branch.sol
// Stresses: the log sequence as a record of the path taken
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full loopShape=clamped placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

enum LogKind { Even = 10, Odd = 11 };

struct LogInsideConditionalBranch2
{
};

struct LogInsideConditionalBranch : public ContractBase
{
                        struct LogMsg
                        {
                            uint32 _contractIndex;
                            uint32 _type;
                            uint64 index;
                            uint64 value;
                            sint8 _terminator;
                        };

    struct Scratch
    {
        LogMsg emitMessage;
        uint64 emitI;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 odds;
        uint64 evens;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Emit_input
    {
        uint64 count;
    };

    struct Emit_output {};

    struct Emit_locals
    {
        Emit_input forwardedInput;
        Emit_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Emit)
    {
        locals.forwardedInput = input;
        CALL(EmitBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 evens;
        uint64 odds;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using EmitBody_input = Emit_input;

    using EmitBody_output = Emit_output;

    PRIVATE_PROCEDURE(EmitBody)
    {
        state.mut().inner.scratch.emitMessage._contractIndex = 0;
        state.mut().inner.scratch.emitMessage._terminator = 0;
        for (state.mut().inner.scratch.emitI = 0; state.mut().inner.scratch.emitI < input.count; state.mut().inner.scratch.emitI++)
        {
        state.mut().inner.scratch.emitMessage.index = state.mut().inner.scratch.emitI;
        if (QPI::mod(state.mut().inner.scratch.emitI, 2ULL) == 0)
        {
        state.mut().inner.scratch.emitMessage._type = Even;
        state.mut().inner.scratch.emitMessage.value = state.mut().inner.scratch.emitI * 2;
        LOG_INFO(state.mut().inner.scratch.emitMessage);
        state.mut().inner.evens++;
        }
        else
        {
        state.mut().inner.scratch.emitMessage._type = Odd;
        state.mut().inner.scratch.emitMessage.value = state.mut().inner.scratch.emitI * 3;
        LOG_INFO(state.mut().inner.scratch.emitMessage);
        state.mut().inner.odds++;
        }
        }
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.evens = state.get().inner.evens;
        output.odds = state.get().inner.odds;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Emit, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.evens = 0;
        state.mut().inner.odds = 0;
    }
};
