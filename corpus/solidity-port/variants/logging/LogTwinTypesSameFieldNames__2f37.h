// LogTwinTypesSameFieldNames — ported from test/libsolidity/semanticTests/events/event_anonymous_with_signature_collision.sol
// Stresses: two payload types that differ only in field width
// Port caveat: the type word replaces the signature hash
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

enum LogKind { Narrow = 7, Wide = 8 };

struct LogTwinTypesSameFieldNames2
{
};

struct LogTwinTypesSameFieldNames : public ContractBase
{
                        struct NarrowMsg
                        {
                            uint32 _contractIndex;
                            uint32 _type;
                            uint16 amount;
                            uint16 tag;
                            sint8 _terminator;
                        };

                        struct WideMsg
                        {
                            uint32 _contractIndex;
                            uint32 _type;
                            uint64 amount;
                            uint64 tag;
                            sint8 _terminator;
                        };

    struct Scratch
    {
        NarrowMsg emitNarrow;
        WideMsg emitWide;
        uint64 emitI;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 narrowEmitted;
        uint64 wideEmitted;
        Scratch scratch;
    };

    struct Emit_input
    {
        uint64 count;
    };

    struct Emit_output {};

    struct Emit_locals
    {
        Emit_input forwardedInput;
        Emit_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Emit)
    {
        locals.forwardedInput = input;
        CALL(EmitBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 narrowEmitted;
        uint64 wideEmitted;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using EmitBody_input = Emit_input;

    using EmitBody_output = Emit_output;

    PRIVATE_PROCEDURE(EmitBody)
    {
        state.mut().scratch.emitNarrow._contractIndex = 0;
        state.mut().scratch.emitNarrow._type = Narrow;
        state.mut().scratch.emitNarrow._terminator = 0;
        state.mut().scratch.emitWide._contractIndex = 0;
        state.mut().scratch.emitWide._type = Wide;
        state.mut().scratch.emitWide._terminator = 0;
        for (state.mut().scratch.emitI = 0; state.mut().scratch.emitI < input.count; state.mut().scratch.emitI++)
        {
        state.mut().scratch.emitNarrow.amount = (uint16)(state.mut().scratch.emitI * 1000);
        state.mut().scratch.emitNarrow.tag = (uint16)state.mut().scratch.emitI;
        LOG_INFO(state.mut().scratch.emitNarrow);
        state.mut().narrowEmitted++;
        state.mut().scratch.emitWide.amount = state.mut().scratch.emitI * 1000;
        state.mut().scratch.emitWide.tag = state.mut().scratch.emitI;
        LOG_INFO(state.mut().scratch.emitWide);
        state.mut().wideEmitted++;
        }
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.narrowEmitted = state.get().narrowEmitted;
        output.wideEmitted = state.get().wideEmitted;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Emit, 1);
    }

    INITIALIZE()
    {

    }
};
