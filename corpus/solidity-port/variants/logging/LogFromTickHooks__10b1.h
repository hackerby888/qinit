// LogFromTickHooks — ported from test/libsolidity/semanticTests/events/event_emit.sol
// Stresses: logs emitted from the tick hooks
// Port caveat: no out-of-transaction events in Solidity
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=first stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

enum LogKind { TickBegan = 30, TickEnded = 31, UserCalled = 32 };

struct LogFromTickHooks2
{
};

struct LogFromTickHooks : public ContractBase
{
    struct LogMsg
    {
        uint32 _contractIndex;
        uint32 _type;
        uint64 value;
        sint8 _terminator;
    };

    struct StateData
    {
        uint64 userLogs;
        uint64 endLogs;
        uint64 beginLogs;
        uint64 placementGuard;
    };

    struct Touch_input
    {
        uint64 value;
    };

    struct Touch_output {};

    struct Touch_locals
    {
        LogMsg message;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Touch)
    {
        locals.message._contractIndex = 0;
        locals.message._type = UserCalled;
        locals.message._terminator = 0;
        locals.message.value = input.value;
        LOG_INFO(locals.message);
        state.mut().userLogs++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 beginLogs;
        uint64 endLogs;
        uint64 userLogs;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.beginLogs = state.get().beginLogs;
        output.endLogs = state.get().endLogs;
        output.userLogs = state.get().userLogs;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Touch, 1);
    }

    BEGIN_TICK()
    {
        state.mut().beginLogs++;
    }

    END_TICK()
    {
        state.mut().endLogs++;
    }
};
