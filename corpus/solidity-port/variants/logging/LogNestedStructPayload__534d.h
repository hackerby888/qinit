// LogNestedStructPayload — ported from test/libsolidity/semanticTests/events/event_struct_memory_v2.sol
// Stresses: nested struct and id inside a log payload
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct LogNestedStructPayload2
{
};

struct LogNestedStructPayload : public ContractBase
{
    struct Pair
    {
        uint64 left;
        uint32 right;
    };

    struct LogMsg
    {
        uint32 _contractIndex;
        uint32 _type;
        id who;
        Pair pair;
        sint8 _terminator;
    };
    struct Scratch
    {
        LogMsg emitMessage;
        uint64 emitI;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 emitted;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Emit_input
    {
        uint64 count;
    };

    struct Emit_output {};

    PUBLIC_PROCEDURE(Emit)
    {
        state.mut().inner.scratch.emitMessage._contractIndex = 0;
        state.mut().inner.scratch.emitMessage._type = 9;
        state.mut().inner.scratch.emitMessage._terminator = 0;
        state.mut().inner.scratch.emitMessage.who = qpi.invocator();
        for (state.mut().inner.scratch.emitI = 0; state.mut().inner.scratch.emitI < input.count; state.mut().inner.scratch.emitI++)
        {
        state.mut().inner.scratch.emitMessage.pair.left = state.mut().inner.scratch.emitI;
        state.mut().inner.scratch.emitMessage.pair.right = (uint32)(state.mut().inner.scratch.emitI + 1);
        LOG_INFO(state.mut().inner.scratch.emitMessage);
        state.mut().inner.emitted++;
        }
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 emitted;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.emitted = state.get().inner.emitted;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Emit, 1);
    }

    INITIALIZE()
    {

    }
};
