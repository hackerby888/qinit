// LogInsideConditionalBranch — ported from test/libsolidity/semanticTests/events/event_emit_in_branch.sol
// Stresses: the log sequence as a record of the path taken
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent loopShape=constant placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

enum LogKind { Even = 10, Odd = 11 };

struct LogInsideConditionalBranch2
{
};

struct LogInsideConditionalBranch : public ContractBase
{
                        struct LogMsg
                        {
                            uint32 _contractIndex;
                            uint32 _type;
                            uint64 index;
                            uint64 value;
                            sint8 _terminator;
                        };

    struct Scratch
    {
        LogMsg emitMessage;
        uint64 emitI;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 odds;
        uint64 evens;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 evens;
        uint64 odds;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Emit_input
    {
        uint64 count;
    };

    struct Emit_output {};

    struct Emit_locals
    {
        Emit_input forwardedInput;
        Emit_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Emit)
    {
        locals.forwardedInput = input;
        CALL(EmitBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.evens = state.get().evens;
        output.odds = state.get().odds;
    }

    using EmitBody_input = Emit_input;

    using EmitBody_output = Emit_output;

    PRIVATE_PROCEDURE(EmitBody)
    {
        state.mut().scratch.emitMessage._contractIndex = 0;
        state.mut().scratch.emitMessage._terminator = 0;
        for (state.mut().scratch.emitI = 0; state.mut().scratch.emitI < input.count; state.mut().scratch.emitI++)
        {
        state.mut().scratch.emitMessage.index = state.mut().scratch.emitI;
        if (QPI::mod(state.mut().scratch.emitI, 2ULL) == 0)
        {
        state.mut().scratch.emitMessage._type = Even;
        state.mut().scratch.emitMessage.value = state.mut().scratch.emitI * 2;
        LOG_INFO(state.mut().scratch.emitMessage);
        state.mut().evens++;
        }
        else
        {
        state.mut().scratch.emitMessage._type = Odd;
        state.mut().scratch.emitMessage.value = state.mut().scratch.emitI * 3;
        LOG_INFO(state.mut().scratch.emitMessage);
        state.mut().odds++;
        }
        }
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Emit, 1);
    }
};
