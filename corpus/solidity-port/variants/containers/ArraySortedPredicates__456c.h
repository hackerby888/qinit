// ArraySortedPredicates — ported from test/libsolidity/semanticTests/array/sorting.sol
// Stresses: the sortedness helpers against a hand-written loop
// Port caveat: the helpers are QPI-only
// Variant: capacity=64 entryOrder=declared entryShape=direct initStyle=empty placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ArraySortedPredicates2
{
};

struct ArraySortedPredicates : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        Array<uint64, 16> values;
        uint64 sortedHelper;
        uint64 sortedLoop;
        uint64 strictHelper;
        uint64 disagreements;
        uint64 calls;
    };

    struct Load_input
    {
        uint64 mode;
    };

    struct Load_output {};

    struct Load_locals
    {
        uint64 i;
        uint64 sorted;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Load)
    {
        for (locals.i = 0; locals.i < 16; locals.i++)
        {
        if (input.mode == 0)
        {
        state.mut().values.set(locals.i, locals.i);
        }
        else if (input.mode == 1)
        {
        state.mut().values.set(locals.i, QPI::div(locals.i, 2ULL));
        }
        else
        {
        state.mut().values.set(locals.i, 16 - locals.i);
        }
        }
        state.mut().sortedHelper = isArraySorted(state.get().values, 0, 16) ? 1 : 0;
        state.mut().strictHelper = isArraySortedWithoutDuplicates(state.get().values, 0, 16) ? 1 : 0;
        locals.sorted = 1;
        for (locals.i = 1; locals.i < 16; locals.i++)
        {
        if (state.get().values.get(locals.i) < state.get().values.get(locals.i - 1))
        {
        locals.sorted = 0;
        }
        }
        state.mut().sortedLoop = locals.sorted;
        if (locals.sorted != state.get().sortedHelper)
        {
        state.mut().disagreements++;
        }
        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 sortedHelper;
        uint64 sortedLoop;
        uint64 strictHelper;
        uint64 disagreements;
        uint64 emptyRange;
        uint64 invertedRange;
        uint64 pastEnd;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.sortedHelper = state.get().sortedHelper;
        output.sortedLoop = state.get().sortedLoop;
        output.strictHelper = state.get().strictHelper;
        output.disagreements = state.get().disagreements;
        output.emptyRange = isArraySorted(state.get().values, 2, 2) ? 1 : 0;
        output.invertedRange = isArraySorted(state.get().values, 3, 1) ? 1 : 0;
        output.pastEnd = isArraySorted(state.get().values, 0, 16 + 4) ? 1 : 0;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Load, 1);
    }

    INITIALIZE()
    {

    }
};
