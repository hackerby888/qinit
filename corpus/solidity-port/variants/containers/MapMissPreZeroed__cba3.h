// MapMissPreZeroed — ported from test/libsolidity/semanticTests/storage/mapping_state.sol
// Stresses: pre-zeroed miss vs hit on the same map
// Variant: capacity=64 entryOrder=declared entryShape=viaPrivate initStyle=absent placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct MapMissPreZeroed2
{
};

struct MapMissPreZeroed : public ContractBase
{
    struct Scratch
    {
        uint64 probeFetched;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 missFlag;
        uint64 hitFlag;
        uint64 missValue;
        uint64 hitValue;
        HashMap<uint64, uint64, 64> book;
        Scratch scratch;
    };

    struct Probe_input
    {
        uint64 key;
    };

    struct Probe_output {};

    struct Probe_locals
    {
        Probe_input forwardedInput;
        Probe_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Probe)
    {
        locals.forwardedInput = input;
        CALL(ProbeBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 hitValue;
        uint64 missValue;
        uint64 hitFlag;
        uint64 missFlag;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using ProbeBody_input = Probe_input;

    using ProbeBody_output = Probe_output;

    PRIVATE_PROCEDURE(ProbeBody)
    {
        state.mut().book.set(input.key, input.key + 77);
        state.mut().scratch.probeFetched = 0;
        state.mut().hitFlag = state.get().book.get(input.key, state.mut().scratch.probeFetched) ? 1 : 0;
        state.mut().hitValue = state.mut().scratch.probeFetched;
        state.mut().scratch.probeFetched = 0;
        state.mut().missFlag = state.get().book.get(input.key + 1000000, state.mut().scratch.probeFetched) ? 1 : 0;
        state.mut().missValue = state.mut().scratch.probeFetched;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.hitValue = state.get().hitValue;
        output.missValue = state.get().missValue;
        output.hitFlag = state.get().hitFlag;
        output.missFlag = state.get().missFlag;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Probe, 1);
    }
};
