// CollectionBackwardWalkByTailIndex — ported from openzeppelin-contracts/contracts/utils/structs/DoubleEndedQueue.sol
// Stresses: tailIndex + prevElementIndex against headIndex + nextElementIndex
// Port caveat: no priorities in the Solidity original
// Variant: capacity=64 entryOrder=declared entryShape=viaPrivate initStyle=absent placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CollectionBackwardWalkByTailIndex2
{
};

struct CollectionBackwardWalkByTailIndex : public ContractBase
{
    struct Scratch
    {
        sint64 addIndex;
        sint64 walkBothIndex;
        uint64 walkBothGuard;
        uint64 walkBothSeen;
        uint64 walkBothFirstSeen;
        uint64 walkBothLastSeen;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 mismatched;
        uint64 backwardCount;
        uint64 forwardCount;
        uint64 backwardLast;
        uint64 backwardFirst;
        uint64 forwardLast;
        uint64 forwardFirst;
        Collection<uint64, 64> queue;
        Scratch scratch;
    };

    struct Add_input
    {
        uint64 value;
        sint64 priority;
    };

    struct Add_output {};

    struct Add_locals
    {
        Add_input forwardedInput;
        Add_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Add)
    {
        locals.forwardedInput = input;
        CALL(AddBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 forwardFirst;
        uint64 forwardLast;
        uint64 backwardFirst;
        uint64 backwardLast;
        uint64 forwardCount;
        uint64 backwardCount;
        uint64 mismatched;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct WalkBoth_input
    {
        uint64 unused;
    };

    struct WalkBoth_output {};

    struct WalkBoth_locals
    {
        WalkBoth_input forwardedInput;
        WalkBoth_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(WalkBoth)
    {
        locals.forwardedInput = input;
        CALL(WalkBothBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using AddBody_input = Add_input;

    using AddBody_output = Add_output;

    PRIVATE_PROCEDURE(AddBody)
    {
        state.mut().scratch.addIndex = state.mut().queue.add(SELF, input.value, input.priority);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.forwardFirst = state.get().forwardFirst;
        output.forwardLast = state.get().forwardLast;
        output.backwardFirst = state.get().backwardFirst;
        output.backwardLast = state.get().backwardLast;
        output.forwardCount = state.get().forwardCount;
        output.backwardCount = state.get().backwardCount;
        output.mismatched = state.get().mismatched;
    }

    using WalkBothBody_input = WalkBoth_input;

    using WalkBothBody_output = WalkBoth_output;

    PRIVATE_PROCEDURE(WalkBothBody)
    {
        state.mut().scratch.walkBothIndex = state.get().queue.headIndex(SELF);
        state.mut().scratch.walkBothGuard = 0;
        state.mut().scratch.walkBothSeen = 0;
        state.mut().scratch.walkBothFirstSeen = 0;
        state.mut().scratch.walkBothLastSeen = 0;
        while (state.mut().scratch.walkBothIndex >= 0 && state.mut().scratch.walkBothGuard < 128)
        {
        if (state.mut().scratch.walkBothSeen == 0)
        {
        state.mut().scratch.walkBothFirstSeen = state.get().queue.element(state.mut().scratch.walkBothIndex);
        }
        state.mut().scratch.walkBothLastSeen = state.get().queue.element(state.mut().scratch.walkBothIndex);
        state.mut().scratch.walkBothIndex = state.get().queue.nextElementIndex(state.mut().scratch.walkBothIndex);
        state.mut().scratch.walkBothSeen++;
        state.mut().scratch.walkBothGuard++;
        }
        state.mut().forwardFirst = state.mut().scratch.walkBothFirstSeen;
        state.mut().forwardLast = state.mut().scratch.walkBothLastSeen;
        state.mut().forwardCount = state.mut().scratch.walkBothSeen;

        state.mut().scratch.walkBothIndex = state.get().queue.tailIndex(SELF);
        state.mut().scratch.walkBothGuard = 0;
        state.mut().scratch.walkBothSeen = 0;
        state.mut().scratch.walkBothFirstSeen = 0;
        state.mut().scratch.walkBothLastSeen = 0;
        while (state.mut().scratch.walkBothIndex >= 0 && state.mut().scratch.walkBothGuard < 128)
        {
        if (state.mut().scratch.walkBothSeen == 0)
        {
        state.mut().scratch.walkBothFirstSeen = state.get().queue.element(state.mut().scratch.walkBothIndex);
        }
        state.mut().scratch.walkBothLastSeen = state.get().queue.element(state.mut().scratch.walkBothIndex);
        state.mut().scratch.walkBothIndex = state.get().queue.prevElementIndex(state.mut().scratch.walkBothIndex);
        state.mut().scratch.walkBothSeen++;
        state.mut().scratch.walkBothGuard++;
        }
        state.mut().backwardFirst = state.mut().scratch.walkBothFirstSeen;
        state.mut().backwardLast = state.mut().scratch.walkBothLastSeen;
        state.mut().backwardCount = state.mut().scratch.walkBothSeen;

        // The two walks must cover the same elements and mirror each other's ends.
        if (state.get().forwardCount != state.get().backwardCount)
        {
        state.mut().mismatched++;
        }
        if (state.get().forwardFirst != state.get().backwardLast)
        {
        state.mut().mismatched++;
        }
        if (state.get().forwardLast != state.get().backwardFirst)
        {
        state.mut().mismatched++;
        }
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Add, 1);
        REGISTER_USER_PROCEDURE(WalkBoth, 2);
    }
};
