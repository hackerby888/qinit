// HashMapRemoveThenReAdd — ported from test/libsolidity/semanticTests/mappings/mapping_delete.sol
// Stresses: remove-then-re-add against the table's occupation markers
// Port caveat: QPI removal leaves a marker; Solidity's delete does not
// Variant: capacity=8 entryOrder=reversed entryShape=viaPrivate fill=over initStyle=absent placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HashMapRemoveThenReAdd2
{
};

struct HashMapRemoveThenReAdd : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        HashMap<uint64, uint64, 8> table;
        uint64 removed;
        uint64 reAdded;
        uint64 populationAfterRemove;
        uint64 populationAfterReAdd;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input
    {
        uint64 key;
    };

    struct Read_output
    {
        uint64 value;
        uint64 present;
        uint64 population;
        uint64 removed;
        uint64 reAdded;
        uint64 populationAfterRemove;
        uint64 populationAfterReAdd;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        locals.forwardedInput = input;
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Cycle_input
    {
        uint64 count;
    };

    struct Cycle_output {};

    struct Cycle_locals
    {
        Cycle_input forwardedInput;
        Cycle_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Cycle)
    {
        locals.forwardedInput = input;
        CALL(CycleBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    struct ReadBody_locals
    {
        uint64 fetched;
    };

    PRIVATE_FUNCTION_WITH_LOCALS(ReadBody)
    {
        locals.fetched = 0;
        output.present = state.get().inner.table.get(input.key, locals.fetched) ? 1 : 0;
        output.value = locals.fetched;
        output.population = state.get().inner.table.population();
        output.removed = state.get().inner.removed;
        output.reAdded = state.get().inner.reAdded;
        output.populationAfterRemove = state.get().inner.populationAfterRemove;
        output.populationAfterReAdd = state.get().inner.populationAfterReAdd;
    }

    using CycleBody_input = Cycle_input;

    using CycleBody_output = Cycle_output;

    struct CycleBody_locals
    {
        uint64 i;
        sint64 slot;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(CycleBody)
    {
        for (locals.i = 0; locals.i < input.count; locals.i++)
        {
        state.mut().inner.table.set(locals.i, locals.i * 10 + 1);
        }
        for (locals.i = 0; locals.i < input.count; locals.i++)
        {
        if (QPI::mod(locals.i, 2ULL) == 0)
        {
        locals.slot = state.mut().inner.table.removeByKey(locals.i);
        if (locals.slot >= 0)
        {
        state.mut().inner.removed++;
        }
        }
        }
        state.mut().inner.populationAfterRemove = state.get().inner.table.population();
        for (locals.i = 0; locals.i < input.count; locals.i++)
        {
        if (QPI::mod(locals.i, 2ULL) == 0)
        {
        state.mut().inner.table.set(locals.i, locals.i * 10 + 2);
        state.mut().inner.reAdded++;
        }
        }
        state.mut().inner.populationAfterReAdd = state.get().inner.table.population();
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Cycle, 1);
    }
};
