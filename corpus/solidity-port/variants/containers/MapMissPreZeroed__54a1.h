// MapMissPreZeroed — ported from test/libsolidity/semanticTests/storage/mapping_state.sol
// Stresses: pre-zeroed miss vs hit on the same map
// Variant: capacity=8 entryOrder=declared entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct MapMissPreZeroed2
{
};

struct MapMissPreZeroed : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        HashMap<uint64, uint64, 8> book;
        uint64 hitValue;
        uint64 missValue;
        uint64 hitFlag;
        uint64 missFlag;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Probe_input
    {
        uint64 key;
    };

    struct Probe_output {};

    struct Probe_locals
    {
        uint64 fetched;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Probe)
    {
        state.mut().inner.book.set(input.key, input.key + 77);
        locals.fetched = 0;
        state.mut().inner.hitFlag = state.get().inner.book.get(input.key, locals.fetched) ? 1 : 0;
        state.mut().inner.hitValue = locals.fetched;
        locals.fetched = 0;
        state.mut().inner.missFlag = state.get().inner.book.get(input.key + 1000000, locals.fetched) ? 1 : 0;
        state.mut().inner.missValue = locals.fetched;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 hitValue;
        uint64 missValue;
        uint64 hitFlag;
        uint64 missFlag;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.hitValue = state.get().inner.hitValue;
        output.missValue = state.get().inner.missValue;
        output.hitFlag = state.get().inner.hitFlag;
        output.missFlag = state.get().inner.missFlag;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Probe, 1);
    }
};
