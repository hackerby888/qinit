// HashMapRemoveByIndexMasksItsArgument — ported from openzeppelin-contracts/contracts/utils/structs/EnumerableMap.sol (index bounds on removal)
// Stresses: removeByIndex masks its argument with (L-1) and never validates it, so an out-of-range or negative index is a silent alias for an in-range slot rather than an error
// Port caveat: The Solidity original reverts on an out-of-bounds index; QPI wraps, so the port asks which slot the wrap actually selects.
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HashMapRemoveByIndexMasksItsArgument2
{
};

struct HashMapRemoveByIndexMasksItsArgument : public ContractBase
{
    struct Scratch
    {
        id runK0;
        id runK8;
        id runK16;
        sint64 runTarget;
    };

    struct StateData
    {
        HashMap<id, uint64, 8> map;
        uint64 population;
        uint64 emptyAtZero;
        uint64 emptyAtSeven;
        uint64 calls;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 population;
        uint64 emptyAtZero;
        uint64 emptyAtSeven;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().map.reset();
        state.mut().scratch.runK0 = id(0, 0, 0, 0);
        state.mut().scratch.runK8 = id(8, 0, 0, 0);
        state.mut().scratch.runK16 = id(16, 0, 0, 0);
        state.mut().map.set(state.mut().scratch.runK0, 100);
        state.mut().map.set(state.mut().scratch.runK8, 200);
        state.mut().map.set(state.mut().scratch.runK16, 300);

        state.mut().scratch.runTarget = input.a;
        state.mut().map.removeByIndex(state.mut().scratch.runTarget);
        state.mut().population = state.get().map.population();
        state.mut().emptyAtZero = state.get().map.isEmptySlot(0) ? 1 : 0;
        state.mut().emptyAtSeven = state.get().map.isEmptySlot(7) ? 1 : 0;

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.population = state.get().population;
        output.emptyAtZero = state.get().emptyAtZero;
        output.emptyAtSeven = state.get().emptyAtSeven;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
