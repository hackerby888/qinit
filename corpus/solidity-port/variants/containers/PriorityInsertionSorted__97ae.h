// PriorityInsertionSorted — ported from no Solidity analogue (sorted insert)
// Stresses: insertion into a sorted array by shifting the tail — the shift runs backwards, and running it forwards instead smears one element over the rest
// Variant: capacity=64 entryOrder=declared entryShape=viaPrivate initStyle=empty placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct PriorityInsertionSorted2
{
};

struct PriorityInsertionSorted : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 outOfOrder;
        uint64 rejected;
        uint64 shifts;
        uint64 length;
        Array<uint64, 16> values;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Insert_input
    {
        uint64 value;
    };

    struct Insert_output {};

    struct Insert_locals
    {
        Insert_input forwardedInput;
        Insert_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Insert)
    {
        locals.forwardedInput = input;
        CALL(InsertBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 length;
        uint64 shifts;
        uint64 rejected;
        uint64 outOfOrder;
        uint64 first;
        uint64 last;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using InsertBody_input = Insert_input;

    using InsertBody_output = Insert_output;

    struct InsertBody_locals
    {
        uint64 i;
        uint64 position;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(InsertBody)
    {
        if (state.get().inner.length >= 16)
        {
        state.mut().inner.rejected++;
        return;
        }
        locals.position = state.get().inner.length;
        for (locals.i = 0; locals.i < state.get().inner.length; locals.i++)
        {
        if (state.get().inner.values.get(locals.i) > input.value && locals.position == state.get().inner.length)
        {
        locals.position = locals.i;
        }
        }
        for (locals.i = state.get().inner.length; locals.i > locals.position; locals.i--)
        {
        state.mut().inner.values.set(locals.i, state.get().inner.values.get(locals.i - 1));
        state.mut().inner.shifts++;
        }
        state.mut().inner.values.set(locals.position, input.value);
        state.mut().inner.length++;
        state.mut().inner.outOfOrder = 0;
        for (locals.i = 1; locals.i < state.get().inner.length; locals.i++)
        {
        if (state.get().inner.values.get(locals.i) < state.get().inner.values.get(locals.i - 1))
        {
        state.mut().inner.outOfOrder++;
        }
        }
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.length = state.get().inner.length;
        output.shifts = state.get().inner.shifts;
        output.rejected = state.get().inner.rejected;
        output.outOfOrder = state.get().inner.outOfOrder;
        output.first = state.get().inner.values.get(0);
        output.last = state.get().inner.values.get(state.get().inner.length == 0 ? 0 : state.get().inner.length - 1);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Insert, 1);
    }

    INITIALIZE()
    {

    }
};
