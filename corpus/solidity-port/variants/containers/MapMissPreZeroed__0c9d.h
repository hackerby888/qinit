// MapMissPreZeroed — ported from test/libsolidity/semanticTests/storage/mapping_state.sol
// Stresses: pre-zeroed miss vs hit on the same map
// Variant: capacity=8 entryOrder=reversed entryShape=direct initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct MapMissPreZeroed2
{
};

struct MapMissPreZeroed : public ContractBase
{
    struct Scratch
    {
        uint64 probeFetched;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 missFlag;
        uint64 hitFlag;
        uint64 missValue;
        uint64 hitValue;
        HashMap<uint64, uint64, 8> book;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 hitValue;
        uint64 missValue;
        uint64 hitFlag;
        uint64 missFlag;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.hitValue = state.get().inner.hitValue;
        output.missValue = state.get().inner.missValue;
        output.hitFlag = state.get().inner.hitFlag;
        output.missFlag = state.get().inner.missFlag;
    }

    struct Probe_input
    {
        uint64 key;
    };

    struct Probe_output {};

    PUBLIC_PROCEDURE(Probe)
    {
        state.mut().inner.book.set(input.key, input.key + 77);
        state.mut().inner.scratch.probeFetched = 0;
        state.mut().inner.hitFlag = state.get().inner.book.get(input.key, state.mut().inner.scratch.probeFetched) ? 1 : 0;
        state.mut().inner.hitValue = state.mut().inner.scratch.probeFetched;
        state.mut().inner.scratch.probeFetched = 0;
        state.mut().inner.missFlag = state.get().inner.book.get(input.key + 1000000, state.mut().inner.scratch.probeFetched) ? 1 : 0;
        state.mut().inner.missValue = state.mut().inner.scratch.probeFetched;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Probe, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.book.reset();
        state.mut().inner.hitValue = 0;
        state.mut().inner.missValue = 0;
        state.mut().inner.hitFlag = 0;
        state.mut().inner.missFlag = 0;
    }
};
