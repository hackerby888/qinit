// HashSetRemoveAndKeyByValue — ported from openzeppelin-contracts/contracts/utils/structs/EnumerableSet.sol (remove and read back)
// Stresses: HashSet's deletion half — spelled remove() where HashMap spells removeByKey() — and its key() accessor, which returns by value where HashMap's returns by const reference, so the two go through different return conventions in the backend
// Port caveat: EnumerableSet's remove swaps and pops; QPI marks, so the surviving elements keep their slots rather than being relocated.
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HashSetRemoveAndKeyByValue2
{
};

struct HashSetRemoveAndKeyByValue : public ContractBase
{
    struct Scratch
    {
        id runK1;
        id runK9;
        id runK17;
        id runTarget;
        sint64 runRemoved;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 keyOneIntact;
        uint64 population;
        uint64 idx17;
        uint64 idx9;
        uint64 idx1;
        HashSet<id, 8> set;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 idx1;
        uint64 idx9;
        uint64 idx17;
        uint64 population;
        uint64 keyOneIntact;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.idx1 = state.get().inner.idx1;
        output.idx9 = state.get().inner.idx9;
        output.idx17 = state.get().inner.idx17;
        output.population = state.get().inner.population;
        output.keyOneIntact = state.get().inner.keyOneIntact;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.set.reset();

        state.mut().inner.scratch.runK1 = id(1, 0, 0, 0);
        state.mut().inner.scratch.runK9 = id(9, 0, 0, 0);
        state.mut().inner.scratch.runK17 = id(17, 0, 0, 0);

        state.mut().inner.set.add(state.mut().inner.scratch.runK1);
        state.mut().inner.set.add(state.mut().inner.scratch.runK9);
        state.mut().inner.set.add(state.mut().inner.scratch.runK17);
        state.mut().inner.scratch.runTarget = id(input.a, 0, 0, 0);
        state.mut().inner.scratch.runRemoved = state.mut().inner.set.remove(state.mut().inner.scratch.runTarget);
        state.mut().inner.idx1 = state.get().inner.set.getElementIndex(state.mut().inner.scratch.runK1);
        state.mut().inner.idx9 = state.get().inner.set.getElementIndex(state.mut().inner.scratch.runK9);
        state.mut().inner.idx17 = state.get().inner.set.getElementIndex(state.mut().inner.scratch.runK17);
        state.mut().inner.population = state.get().inner.set.population();
        state.mut().inner.keyOneIntact = (state.get().inner.set.key(1) == state.mut().inner.scratch.runK1) ? 1 : 0;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
