// BitArrayPattern — ported from test/libsolidity/semanticTests/array/bytes_length_member.sol
// Stresses: BitArray word boundaries
// Variant: capacity=2 entryOrder=declared entryShape=direct initStyle=absent placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BitArrayPattern2
{
};

struct BitArrayPattern : public ContractBase
{
    struct Scratch
    {
        uint64 patternI;
        uint64 patternCount;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 firstSet;
        uint64 setCount;
        BitArray<2> bits;
        Scratch scratch;
    };

    struct Pattern_input
    {
        uint64 stride;
    };

    struct Pattern_output {};

    PUBLIC_PROCEDURE(Pattern)
    {
        state.mut().bits.setAll(0);
        state.mut().scratch.patternCount = 0;
        for (state.mut().scratch.patternI = 0; state.mut().scratch.patternI < 2; state.mut().scratch.patternI++)
        {
        if (QPI::mod(state.mut().scratch.patternI, input.stride + 1) == 0)
        {
        state.mut().bits.set(state.mut().scratch.patternI, 1);
        state.mut().scratch.patternCount++;
        }
        }
        state.mut().setCount = state.mut().scratch.patternCount;
        state.mut().firstSet = (uint64)state.get().bits.get(0);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 setCount;
        uint64 firstSet;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.setCount = state.get().setCount;
        output.firstSet = state.get().firstSet;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Pattern, 1);
    }
};
