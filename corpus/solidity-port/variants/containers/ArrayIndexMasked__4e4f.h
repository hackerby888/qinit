// ArrayIndexMasked — ported from test/libsolidity/semanticTests/array/array_index_out_of_bounds.sol
// Stresses: index masking rather than bounds-trapping
// Port caveat: Solidity reverts on an out-of-bounds index; Array::get masks it.
// Variant: capacity=8 entryOrder=reversed entryShape=direct initStyle=empty placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ArrayIndexMasked2
{
};

struct ArrayIndexMasked : public ContractBase
{
    struct Scratch
    {
        uint64 pokeI;
        uint64 pokeTotal;
    };

    struct StateData
    {
        uint64 placementGuard;
        Array<uint64, 8> slots;
        uint64 readBack;
        uint64 checksum;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 readBack;
        uint64 checksum;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.readBack = state.get().readBack;
        output.checksum = state.get().checksum;
    }

    struct Poke_input
    {
        uint64 index;
        uint64 value;
    };

    struct Poke_output {};

    PUBLIC_PROCEDURE(Poke)
    {
        state.mut().slots.set(input.index, input.value);
        state.mut().readBack = state.get().slots.get(input.index);
        state.mut().scratch.pokeTotal = 0;
        for (state.mut().scratch.pokeI = 0; state.mut().scratch.pokeI < 8; state.mut().scratch.pokeI++)
        {
        state.mut().scratch.pokeTotal += state.get().slots.get(state.mut().scratch.pokeI);
        }
        state.mut().checksum = state.mut().scratch.pokeTotal;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Poke, 1);
    }

    INITIALIZE()
    {

    }
};
