// CollectionBackwardWalkByTailIndex — ported from openzeppelin-contracts/contracts/utils/structs/DoubleEndedQueue.sol
// Stresses: tailIndex + prevElementIndex against headIndex + nextElementIndex
// Port caveat: no priorities in the Solidity original
// Variant: capacity=8 entryOrder=declared entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CollectionBackwardWalkByTailIndex2
{
};

struct CollectionBackwardWalkByTailIndex : public ContractBase
{
    struct Scratch
    {
        sint64 addIndex;
        sint64 walkBothIndex;
        uint64 walkBothGuard;
        uint64 walkBothSeen;
        uint64 walkBothFirstSeen;
        uint64 walkBothLastSeen;
    };

    struct StateData
    {
        struct Inner
        {
        Collection<uint64, 8> queue;
        uint64 forwardFirst;
        uint64 forwardLast;
        uint64 backwardFirst;
        uint64 backwardLast;
        uint64 forwardCount;
        uint64 backwardCount;
        uint64 mismatched;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Add_input
    {
        uint64 value;
        sint64 priority;
    };

    struct Add_output {};

    PUBLIC_PROCEDURE(Add)
    {
        state.mut().inner.scratch.addIndex = state.mut().inner.queue.add(SELF, input.value, input.priority);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 forwardFirst;
        uint64 forwardLast;
        uint64 backwardFirst;
        uint64 backwardLast;
        uint64 forwardCount;
        uint64 backwardCount;
        uint64 mismatched;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.forwardFirst = state.get().inner.forwardFirst;
        output.forwardLast = state.get().inner.forwardLast;
        output.backwardFirst = state.get().inner.backwardFirst;
        output.backwardLast = state.get().inner.backwardLast;
        output.forwardCount = state.get().inner.forwardCount;
        output.backwardCount = state.get().inner.backwardCount;
        output.mismatched = state.get().inner.mismatched;
    }

    struct WalkBoth_input
    {
        uint64 unused;
    };

    struct WalkBoth_output {};

    PUBLIC_PROCEDURE(WalkBoth)
    {
        state.mut().inner.scratch.walkBothIndex = state.get().inner.queue.headIndex(SELF);
        state.mut().inner.scratch.walkBothGuard = 0;
        state.mut().inner.scratch.walkBothSeen = 0;
        state.mut().inner.scratch.walkBothFirstSeen = 0;
        state.mut().inner.scratch.walkBothLastSeen = 0;
        while (state.mut().inner.scratch.walkBothIndex >= 0 && state.mut().inner.scratch.walkBothGuard < 16)
        {
        if (state.mut().inner.scratch.walkBothSeen == 0)
        {
        state.mut().inner.scratch.walkBothFirstSeen = state.get().inner.queue.element(state.mut().inner.scratch.walkBothIndex);
        }
        state.mut().inner.scratch.walkBothLastSeen = state.get().inner.queue.element(state.mut().inner.scratch.walkBothIndex);
        state.mut().inner.scratch.walkBothIndex = state.get().inner.queue.nextElementIndex(state.mut().inner.scratch.walkBothIndex);
        state.mut().inner.scratch.walkBothSeen++;
        state.mut().inner.scratch.walkBothGuard++;
        }
        state.mut().inner.forwardFirst = state.mut().inner.scratch.walkBothFirstSeen;
        state.mut().inner.forwardLast = state.mut().inner.scratch.walkBothLastSeen;
        state.mut().inner.forwardCount = state.mut().inner.scratch.walkBothSeen;

        state.mut().inner.scratch.walkBothIndex = state.get().inner.queue.tailIndex(SELF);
        state.mut().inner.scratch.walkBothGuard = 0;
        state.mut().inner.scratch.walkBothSeen = 0;
        state.mut().inner.scratch.walkBothFirstSeen = 0;
        state.mut().inner.scratch.walkBothLastSeen = 0;
        while (state.mut().inner.scratch.walkBothIndex >= 0 && state.mut().inner.scratch.walkBothGuard < 16)
        {
        if (state.mut().inner.scratch.walkBothSeen == 0)
        {
        state.mut().inner.scratch.walkBothFirstSeen = state.get().inner.queue.element(state.mut().inner.scratch.walkBothIndex);
        }
        state.mut().inner.scratch.walkBothLastSeen = state.get().inner.queue.element(state.mut().inner.scratch.walkBothIndex);
        state.mut().inner.scratch.walkBothIndex = state.get().inner.queue.prevElementIndex(state.mut().inner.scratch.walkBothIndex);
        state.mut().inner.scratch.walkBothSeen++;
        state.mut().inner.scratch.walkBothGuard++;
        }
        state.mut().inner.backwardFirst = state.mut().inner.scratch.walkBothFirstSeen;
        state.mut().inner.backwardLast = state.mut().inner.scratch.walkBothLastSeen;
        state.mut().inner.backwardCount = state.mut().inner.scratch.walkBothSeen;

        // The two walks must cover the same elements and mirror each other's ends.
        if (state.get().inner.forwardCount != state.get().inner.backwardCount)
        {
        state.mut().inner.mismatched++;
        }
        if (state.get().inner.forwardFirst != state.get().inner.backwardLast)
        {
        state.mut().inner.mismatched++;
        }
        if (state.get().inner.forwardLast != state.get().inner.backwardFirst)
        {
        state.mut().inner.mismatched++;
        }
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Add, 1);
        REGISTER_USER_PROCEDURE(WalkBoth, 2);
    }
};
