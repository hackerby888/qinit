// TickAndEpochHooks — ported from no Solidity analogue
// Stresses: the four lifecycle hooks
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct TickAndEpochHooks2
{
};

struct TickAndEpochHooks : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 userCalls;
        uint64 endEpochs;
        uint64 beginEpochs;
        uint64 endTicks;
        uint64 beginTicks;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 beginTicks;
        uint64 endTicks;
        uint64 beginEpochs;
        uint64 endEpochs;
        uint64 userCalls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Touch_input
    {
        uint64 value;
    };

    struct Touch_output {};

    struct Touch_locals
    {
        Touch_input forwardedInput;
        Touch_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Touch)
    {
        locals.forwardedInput = input;
        CALL(TouchBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.beginTicks = state.get().inner.beginTicks;
        output.endTicks = state.get().inner.endTicks;
        output.beginEpochs = state.get().inner.beginEpochs;
        output.endEpochs = state.get().inner.endEpochs;
        output.userCalls = state.get().inner.userCalls;
    }

    using TouchBody_input = Touch_input;

    using TouchBody_output = Touch_output;

    struct TouchBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(TouchBody)
    {
        state.mut().inner.userCalls += input.value;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Touch, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.beginTicks = 0;
        state.mut().inner.endTicks = 0;
        state.mut().inner.beginEpochs = 0;
        state.mut().inner.endEpochs = 0;
        state.mut().inner.userCalls = 0;
    }

    BEGIN_TICK()
    {
        state.mut().inner.beginTicks++;
    }

    END_TICK()
    {
        state.mut().inner.endTicks++;
    }

    BEGIN_EPOCH()
    {
        state.mut().inner.beginEpochs++;
    }

    END_EPOCH()
    {
        state.mut().inner.endEpochs++;
    }
};
