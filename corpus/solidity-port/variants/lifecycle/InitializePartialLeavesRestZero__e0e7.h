// InitializePartialLeavesRestZero — ported from test/libsolidity/semanticTests/constructor/constructor_static_array_argument.sol
// Stresses: half-initialised state, read straight back
// Port caveat: Solidity zero-initialises storage by definition
// Variant: entryOrder=declared entryShape=direct initStyle=empty layout=widestLast placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct InitializePartialLeavesRestZero2
{
};

struct InitializePartialLeavesRestZero : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 setLate;
        Array<uint64, 4> untouched;
        Array<uint64, 4> touched;
        id owner;
        uint8 flag;
        uint32 neverSet;
        uint64 setEarly;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Bump_input
    {
        uint64 delta;
    };

    struct Bump_output {};

    struct Bump_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Bump)
    {
        locals.scratch = state.get().inner.setEarly + input.delta;
        state.mut().inner.setEarly = locals.scratch;
        state.mut().inner.setLate = locals.scratch * 2;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 setEarly;
        uint32 neverSet;
        uint8 flag;
        uint64 touchedZero;
        uint64 untouchedZero;
        uint64 setLate;
        uint64 ownerIsNull;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.setEarly = state.get().inner.setEarly;
        output.neverSet = state.get().inner.neverSet;
        output.flag = state.get().inner.flag;
        output.touchedZero = state.get().inner.touched.get(0);
        output.untouchedZero = state.get().inner.untouched.get(0);
        output.setLate = state.get().inner.setLate;
        output.ownerIsNull = state.get().inner.owner == NULL_ID ? 1 : 0;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Bump, 1);
    }

    INITIALIZE()
    {

    }
};
