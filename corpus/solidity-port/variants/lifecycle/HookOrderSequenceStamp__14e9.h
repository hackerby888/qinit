// HookOrderSequenceStamp — ported from no Solidity analogue
// Stresses: the relative order of INITIALIZE, BEGIN_EPOCH, BEGIN_TICK, END_TICK and END_EPOCH
// Port caveat: hook ordering is a host contract, not a language feature
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=first stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HookOrderSequenceStamp2
{
};

struct HookOrderSequenceStamp : public ContractBase
{
    struct StateData
    {
        uint64 atCall;
        uint64 atEndEpoch;
        uint64 atEndTick;
        uint64 atBeginTick;
        uint64 atBeginEpoch;
        uint64 atInitialize;
        uint64 sequence;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 sequence;
        uint64 atInitialize;
        uint64 atBeginEpoch;
        uint64 atBeginTick;
        uint64 atEndTick;
        uint64 atEndEpoch;
        uint64 atCall;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Touch_input {};

    struct Touch_output {};

    struct Touch_locals
    {
        Touch_input forwardedInput;
        Touch_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Touch)
    {
        CALL(TouchBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.sequence = state.get().sequence;
        output.atInitialize = state.get().atInitialize;
        output.atBeginEpoch = state.get().atBeginEpoch;
        output.atBeginTick = state.get().atBeginTick;
        output.atEndTick = state.get().atEndTick;
        output.atEndEpoch = state.get().atEndEpoch;
        output.atCall = state.get().atCall;
    }

    using TouchBody_input = Touch_input;

    using TouchBody_output = Touch_output;

    struct TouchBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(TouchBody)
    {
        state.mut().sequence++;
        state.mut().atCall = state.get().sequence;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Touch, 1);
    }

    INITIALIZE()
    {
        state.mut().sequence = 0;
        state.mut().sequence++;
        state.mut().atInitialize = state.get().sequence;
    }

    BEGIN_TICK()
    {
        state.mut().sequence++;
        state.mut().atBeginTick = state.get().sequence;
    }

    END_TICK()
    {
        state.mut().sequence++;
        state.mut().atEndTick = state.get().sequence;
    }

    BEGIN_EPOCH()
    {
        state.mut().sequence++;
        state.mut().atBeginEpoch = state.get().sequence;
    }

    END_EPOCH()
    {
        state.mut().sequence++;
        state.mut().atEndEpoch = state.get().sequence;
    }
};
