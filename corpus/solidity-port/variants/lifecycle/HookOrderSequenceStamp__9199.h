// HookOrderSequenceStamp — ported from no Solidity analogue
// Stresses: the relative order of INITIALIZE, BEGIN_EPOCH, BEGIN_TICK, END_TICK and END_EPOCH
// Port caveat: hook ordering is a host contract, not a language feature
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct HookOrderSequenceStamp2
{
};

struct HookOrderSequenceStamp : public ContractBase
{
    struct Scratch
    {
        uint64 touchScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 atCall;
        uint64 atEndEpoch;
        uint64 atEndTick;
        uint64 atBeginTick;
        uint64 atBeginEpoch;
        uint64 atInitialize;
        uint64 sequence;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 sequence;
        uint64 atInitialize;
        uint64 atBeginEpoch;
        uint64 atBeginTick;
        uint64 atEndTick;
        uint64 atEndEpoch;
        uint64 atCall;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Touch_input {};

    struct Touch_output {};

    struct Touch_locals
    {
        Touch_input forwardedInput;
        Touch_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Touch)
    {
        CALL(TouchBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.sequence = state.get().inner.sequence;
        output.atInitialize = state.get().inner.atInitialize;
        output.atBeginEpoch = state.get().inner.atBeginEpoch;
        output.atBeginTick = state.get().inner.atBeginTick;
        output.atEndTick = state.get().inner.atEndTick;
        output.atEndEpoch = state.get().inner.atEndEpoch;
        output.atCall = state.get().inner.atCall;
    }

    using TouchBody_input = Touch_input;

    using TouchBody_output = Touch_output;

    PRIVATE_PROCEDURE(TouchBody)
    {
        state.mut().inner.sequence++;
        state.mut().inner.atCall = state.get().inner.sequence;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Touch, 1);
    }

    INITIALIZE()
    {

    }

    BEGIN_TICK()
    {
        state.mut().inner.sequence++;
        state.mut().inner.atBeginTick = state.get().inner.sequence;
    }

    END_TICK()
    {
        state.mut().inner.sequence++;
        state.mut().inner.atEndTick = state.get().inner.sequence;
    }

    BEGIN_EPOCH()
    {
        state.mut().inner.sequence++;
        state.mut().inner.atBeginEpoch = state.get().inner.sequence;
    }

    END_EPOCH()
    {
        state.mut().inner.sequence++;
        state.mut().inner.atEndEpoch = state.get().inner.sequence;
    }
};
