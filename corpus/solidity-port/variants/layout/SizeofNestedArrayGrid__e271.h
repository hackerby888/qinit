// SizeofNestedArrayGrid — ported from test/libsolidity/semanticTests/array/multidim_array.sol
// Stresses: sizeof of Array, Array of Array and Array of Array of Array — the product rule, checked at three depths and two element widths
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty layout=padded placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

using Flat = Array<uint64, 8>;
using Nested = Array<Flat, 4>;
using Cube = Array<Nested, 2>;
using NarrowFlat = Array<uint16, 8>;
using NarrowNested = Array<NarrowFlat, 4>;
using NarrowCube = Array<NarrowNested, 2>;

struct SizeofNestedArrayGrid2
{
};

struct SizeofNestedArrayGrid : public ContractBase
{
    struct Scratch
    {
        uint64 measureScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 flat;
        uint64 nested;
        uint64 cube;
        uint64 narrowFlat;
        uint64 narrowCube;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 flat;
        uint64 nested;
        uint64 cube;
        uint64 narrowFlat;
        uint64 narrowCube;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        Measure_input forwardedInput;
        Measure_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        locals.forwardedInput = input;
        CALL(MeasureBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.flat = state.get().inner.flat;
        output.nested = state.get().inner.nested;
        output.cube = state.get().inner.cube;
        output.narrowFlat = state.get().inner.narrowFlat;
        output.narrowCube = state.get().inner.narrowCube;
    }

    using MeasureBody_input = Measure_input;

    using MeasureBody_output = Measure_output;

    PRIVATE_PROCEDURE(MeasureBody)
    {
        state.mut().inner.flat = sizeof(Flat);
        state.mut().inner.nested = sizeof(Nested);
        state.mut().inner.cube = sizeof(Cube);
        state.mut().inner.narrowFlat = sizeof(NarrowFlat);
        state.mut().inner.narrowCube = sizeof(NarrowCube);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {

    }
};
