// BitArraySizesLadder — ported from test/libsolidity/semanticTests/various/bit_operations.sol
// Stresses: BitArray at five capacities — 2, 64, 65, 128 and 2048 — where the size is the number of 64-bit words needed, so the step from 64 to 65 is the interesting one
// Variant: entryOrder=reversed entryShape=direct initStyle=full layout=widestFirst placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

using Bits2 = BitArray<2>;
using Bits64 = BitArray<64>;
using Bits128 = BitArray<128>;
using Bits256 = BitArray<256>;
using Bits2048 = BitArray<2048>;

struct BitArraySizesLadder2
{
};

struct BitArraySizesLadder : public ContractBase
{
    struct Scratch
    {
        uint64 measureScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 bits2;
        uint64 bits64;
        uint64 bits65;
        uint64 bits128;
        uint64 bits2048;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 bits2;
        uint64 bits64;
        uint64 bits65;
        uint64 bits128;
        uint64 bits2048;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.bits2 = state.get().inner.bits2;
        output.bits64 = state.get().inner.bits64;
        output.bits65 = state.get().inner.bits65;
        output.bits128 = state.get().inner.bits128;
        output.bits2048 = state.get().inner.bits2048;
    }

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    PUBLIC_PROCEDURE(Measure)
    {
        state.mut().inner.bits2 = sizeof(Bits2);
        state.mut().inner.bits64 = sizeof(Bits64);
        state.mut().inner.bits65 = sizeof(Bits128);
        state.mut().inner.bits128 = sizeof(Bits256);
        state.mut().inner.bits2048 = sizeof(Bits2048);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.bits2 = 0;
        state.mut().inner.bits64 = 0;
        state.mut().inner.bits65 = 0;
        state.mut().inner.bits128 = 0;
        state.mut().inner.bits2048 = 0;
    }
};
