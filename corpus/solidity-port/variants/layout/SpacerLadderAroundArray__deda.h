// SpacerLadderAroundArray — ported from test/libsolidity/semanticTests/structs/struct_packing.sol
// Stresses: an array with one, two and three uint8 spacers in front of it, so the array's own alignment is forced from three different starting offsets
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty layout=padded placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

using Quad = Array<uint64, 4>;

struct Spaced1 { uint8 a; Array<uint64, 4> values; };

struct Spaced2 { uint8 a; uint8 b; Array<uint64, 4> values; };

struct Spaced3 { uint8 a; uint8 b; uint8 c; Array<uint64, 4> values; uint8 tail; };

struct SpacerLadderAroundArray2
{
};

struct SpacerLadderAroundArray : public ContractBase
{
    struct Scratch
    {
        uint64 measureScratch;
    };

    struct StateData
    {
        uint64 one;
        uint64 two;
        uint64 three;
        uint64 arrayOnly;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 one;
        uint64 two;
        uint64 three;
        uint64 arrayOnly;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        Measure_input forwardedInput;
        Measure_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        locals.forwardedInput = input;
        CALL(MeasureBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.one = state.get().one;
        output.two = state.get().two;
        output.three = state.get().three;
        output.arrayOnly = state.get().arrayOnly;
    }

    using MeasureBody_input = Measure_input;

    using MeasureBody_output = Measure_output;

    PRIVATE_PROCEDURE(MeasureBody)
    {
        state.mut().one = sizeof(Spaced1);
        state.mut().two = sizeof(Spaced2);
        state.mut().three = sizeof(Spaced3);
        state.mut().arrayOnly = sizeof(Quad);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {

    }
};
