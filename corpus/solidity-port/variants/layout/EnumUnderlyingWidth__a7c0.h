// EnumUnderlyingWidth — ported from test/libsolidity/semanticTests/enums/enum_with_underlying_type.sol
// Stresses: explicit, implicit and aliased enum underlying types side by side
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

enum Implicit { A = 0, B = 1, C = 2 };
enum Explicit : uint8 { D = 0, E = 1, F = 2 };
enum Wide : uint64 { G = 0, H = 1, I = 2 };
using AliasedWide = Wide;

struct EnumUnderlyingWidth2
{
};

struct EnumUnderlyingWidth : public ContractBase
{
    struct Scratch
    {
        uint64 setSlot;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 aliasedSize;
        uint64 explicitSize;
        uint64 implicitSize;
        AliasedWide aliasedValue;
        Explicit explicitValue;
        Implicit implicitValue;
        Scratch scratch;
    };

    struct Set_input
    {
        uint64 value;
    };

    struct Set_output {};

    struct Set_locals
    {
        Set_input forwardedInput;
        Set_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Set)
    {
        locals.forwardedInput = input;
        CALL(SetBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 implicitSize;
        uint64 explicitSize;
        uint64 aliasedSize;
        uint64 observed;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using SetBody_input = Set_input;

    using SetBody_output = Set_output;

    PRIVATE_PROCEDURE(SetBody)
    {
        state.mut().scratch.setSlot = QPI::mod(input.value, 3ULL);
        state.mut().implicitValue = (Implicit)state.mut().scratch.setSlot;
        state.mut().explicitValue = (Explicit)state.mut().scratch.setSlot;
        state.mut().aliasedValue = (AliasedWide)state.mut().scratch.setSlot;
        state.mut().implicitSize = sizeof(Implicit);
        state.mut().explicitSize = sizeof(Explicit);
        state.mut().aliasedSize = sizeof(AliasedWide);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.implicitSize = state.get().implicitSize;
        output.explicitSize = state.get().explicitSize;
        output.aliasedSize = state.get().aliasedSize;
        output.observed = (uint64)state.get().aliasedValue;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Set, 1);
    }

    INITIALIZE()
    {
        state.mut().implicitSize = 0;
        state.mut().explicitSize = 0;
        state.mut().aliasedSize = 0;
    }
};
