// StructCopyWholeAssign — ported from test/libsolidity/semanticTests/structs/struct_copy.sol
// Stresses: whole-struct assignment including padding
// Variant: placement=last
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Nested
{
    uint64 x;
    uint64 y;
};

struct Holder
{
    uint64 a;
    Nested nested;
    uint8 tag;
};

struct StructCopyWholeAssign2
{
};

struct StructCopyWholeAssign : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        Holder source;
        Holder target;
        uint64 holderSize;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 a;
        uint64 x;
        uint64 y;
        uint64 tag;
        uint64 holderSize;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.a = state.get().target.a;
        output.x = state.get().target.nested.x;
        output.y = state.get().target.nested.y;
        output.tag = (uint64)state.get().target.tag;
        output.holderSize = state.get().holderSize;
    }

    struct SetAndCopy_input
    {
        uint64 value;
    };

    struct SetAndCopy_output {};

    struct SetAndCopy_locals
    {
        Holder scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(SetAndCopy)
    {
        state.mut().source.a = input.value;
        state.mut().source.nested.x = input.value + 3;
        state.mut().source.nested.y = input.value + 4;
        state.mut().source.tag = (uint8)input.value;
        locals.scratch = state.get().source;
        state.mut().target = locals.scratch;
        state.mut().holderSize = sizeof(Holder);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(SetAndCopy, 1);
    }

    INITIALIZE()
    {
        state.mut().holderSize = 0;
    }
};
