// ArrayOfStructsStride — ported from test/libsolidity/semanticTests/array/array_of_structs.sol
// Stresses: element stride of an array of structs
// Variant: capacity=2 entryOrder=reversed entryShape=direct initStyle=absent layout=padded placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Element
{
    uint64 wide;
    uint8 hole;
    uint16 small;
    uint8 tiny;
};

struct ArrayOfStructsStride2
{
};

struct ArrayOfStructsStride : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 checksum;
        uint64 elementSize;
        Array<Element, 2> elements;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 elementSize;
        uint64 checksum;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.elementSize = state.get().elementSize;
        output.checksum = state.get().checksum;
    }

    struct Fill_input
    {
        uint64 count;
    };

    struct Fill_output {};

    struct Fill_locals
    {
        uint64 i;
        Element scratch;
        uint64 total;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Fill)
    {
        for (locals.i = 0; locals.i < input.count; locals.i++)
        {
        locals.scratch.wide = locals.i + 1;
        locals.scratch.small = (uint16)(locals.i + 2);
        locals.scratch.tiny = (uint8)(locals.i + 3);
        state.mut().elements.set(locals.i, locals.scratch);
        }
        locals.total = 0;
        for (locals.i = 0; locals.i < 2; locals.i++)
        {
        locals.total += state.get().elements.get(locals.i).wide;
        locals.total += (uint64)state.get().elements.get(locals.i).small;
        locals.total += (uint64)state.get().elements.get(locals.i).tiny;
        }
        state.mut().checksum = locals.total;
        state.mut().elementSize = sizeof(Element);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Fill, 1);
    }
};
