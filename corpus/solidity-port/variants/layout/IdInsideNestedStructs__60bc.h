// IdInsideNestedStructs — ported from test/libsolidity/semanticTests/structs/struct_packing.sol
// Stresses: a 32-byte id three structs deep, with a uint8 at every level — the alignment of the widest member propagates outward, and each level's size says whether it did
// Variant: entryOrder=declared entryShape=direct initStyle=absent layout=widestFirst placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Level3 { uint8 tag; id who; };

struct Level2 { uint8 tag; Level3 inner; };

struct Level1 { uint8 tag; Level2 inner; uint8 tail; };

struct IdInsideNestedStructs2
{
};

struct IdInsideNestedStructs : public ContractBase
{
    struct Scratch
    {
        Level1 measureValue;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 idFirstWord;
        uint64 level1;
        uint64 level2;
        uint64 level3;
        Scratch scratch;
    };

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    PUBLIC_PROCEDURE(Measure)
    {
        state.mut().scratch.measureValue.tag = (uint8)input.seed;
        state.mut().scratch.measureValue.inner.tag = (uint8)(input.seed + 1);
        state.mut().scratch.measureValue.inner.inner.tag = (uint8)(input.seed + 2);
        state.mut().scratch.measureValue.inner.inner.who = qpi.K12(input.seed);
        state.mut().scratch.measureValue.tail = (uint8)(input.seed + 3);
        state.mut().level3 = sizeof(Level3);
        state.mut().level2 = sizeof(Level2);
        state.mut().level1 = sizeof(Level1);
        state.mut().idFirstWord = state.mut().scratch.measureValue.inner.inner.who.u64._0;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 level3;
        uint64 level2;
        uint64 level1;
        uint64 idFirstWord;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.level3 = state.get().level3;
        output.level2 = state.get().level2;
        output.level1 = state.get().level1;
        output.idFirstWord = state.get().idFirstWord;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }
};
