// ArrayOfStructsStride — ported from test/libsolidity/semanticTests/array/array_of_structs.sol
// Stresses: element stride of an array of structs
// Variant: capacity=1024 entryOrder=declared entryShape=direct initStyle=absent layout=widestLast placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Element
{
    uint8 tiny;
    uint16 small;
    uint64 wide;
};

struct ArrayOfStructsStride2
{
};

struct ArrayOfStructsStride : public ContractBase
{
    struct Scratch
    {
        uint64 fillI;
        Element fillScratch;
        uint64 fillTotal;
    };

    struct StateData
    {
        struct Inner
        {
        Array<Element, 1024> elements;
        uint64 elementSize;
        uint64 checksum;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Fill_input
    {
        uint64 count;
    };

    struct Fill_output {};

    PUBLIC_PROCEDURE(Fill)
    {
        for (state.mut().inner.scratch.fillI = 0; state.mut().inner.scratch.fillI < input.count; state.mut().inner.scratch.fillI++)
        {
        state.mut().inner.scratch.fillScratch.wide = state.mut().inner.scratch.fillI + 1;
        state.mut().inner.scratch.fillScratch.small = (uint16)(state.mut().inner.scratch.fillI + 2);
        state.mut().inner.scratch.fillScratch.tiny = (uint8)(state.mut().inner.scratch.fillI + 3);
        state.mut().inner.elements.set(state.mut().inner.scratch.fillI, state.mut().inner.scratch.fillScratch);
        }
        state.mut().inner.scratch.fillTotal = 0;
        for (state.mut().inner.scratch.fillI = 0; state.mut().inner.scratch.fillI < 1024; state.mut().inner.scratch.fillI++)
        {
        state.mut().inner.scratch.fillTotal += state.get().inner.elements.get(state.mut().inner.scratch.fillI).wide;
        state.mut().inner.scratch.fillTotal += (uint64)state.get().inner.elements.get(state.mut().inner.scratch.fillI).small;
        state.mut().inner.scratch.fillTotal += (uint64)state.get().inner.elements.get(state.mut().inner.scratch.fillI).tiny;
        }
        state.mut().inner.checksum = state.mut().inner.scratch.fillTotal;
        state.mut().inner.elementSize = sizeof(Element);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 elementSize;
        uint64 checksum;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.elementSize = state.get().inner.elementSize;
        output.checksum = state.get().inner.checksum;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Fill, 1);
    }
};
