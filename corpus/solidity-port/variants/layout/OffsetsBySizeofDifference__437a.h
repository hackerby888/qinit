// OffsetsBySizeofDifference — ported from test/libsolidity/semanticTests/structs/struct_packing.sol
// Stresses: member offsets computed by measuring prefixes — sizeof of a struct with the first n members, so the difference between consecutive sizes is where the padding went
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full layout=widestFirst placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct P1 { uint8 a; };

struct P2 { uint8 a; uint64 b; };

struct P3 { uint8 a; uint64 b; uint16 c; };

struct P4 { uint8 a; uint64 b; uint16 c; uint32 d; };

struct OffsetsBySizeofDifference2
{
};

struct OffsetsBySizeofDifference : public ContractBase
{
    struct Scratch
    {
        uint64 measureScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 paddingAtTail;
        uint64 paddingAfterFirst;
        uint64 p4;
        uint64 p3;
        uint64 p2;
        uint64 p1;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 p1;
        uint64 p2;
        uint64 p3;
        uint64 p4;
        uint64 paddingAfterFirst;
        uint64 paddingAtTail;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        Measure_input forwardedInput;
        Measure_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        locals.forwardedInput = input;
        CALL(MeasureBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.p1 = state.get().inner.p1;
        output.p2 = state.get().inner.p2;
        output.p3 = state.get().inner.p3;
        output.p4 = state.get().inner.p4;
        output.paddingAfterFirst = state.get().inner.paddingAfterFirst;
        output.paddingAtTail = state.get().inner.paddingAtTail;
    }

    using MeasureBody_input = Measure_input;

    using MeasureBody_output = Measure_output;

    PRIVATE_PROCEDURE(MeasureBody)
    {
        state.mut().inner.p1 = sizeof(P1);
        state.mut().inner.p2 = sizeof(P2);
        state.mut().inner.p3 = sizeof(P3);
        state.mut().inner.p4 = sizeof(P4);
        state.mut().inner.paddingAfterFirst = sizeof(P2) - sizeof(uint64) - sizeof(uint8);
        state.mut().inner.paddingAtTail = sizeof(P4) - sizeof(uint8) - sizeof(uint64) - sizeof(uint16) - sizeof(uint32);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.p1 = 0;
        state.mut().inner.p2 = 0;
        state.mut().inner.p3 = 0;
        state.mut().inner.p4 = 0;
        state.mut().inner.paddingAfterFirst = 0;
        state.mut().inner.paddingAtTail = 0;
    }
};
