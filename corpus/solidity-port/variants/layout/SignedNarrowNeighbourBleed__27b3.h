// SignedNarrowNeighbourBleed — ported from storage/packed_storage_signed.sol
// Stresses: signed narrow members at their extremes, next to each other
// Port caveat: slot packing becomes C++ placement
// Variant: entryOrder=reversed entryShape=direct initStyle=empty layout=declared placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedNarrowNeighbourBleed2
{
};

struct SignedNarrowNeighbourBleed : public ContractBase
{
    struct Scratch
    {
        sint64 writeScratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        sint8 a;
        uint8 b;
        sint8 c;
        sint16 d;
        uint16 e;
        uint64 guard;
        Scratch scratch;
    };

    struct Write_input
    {
        sint64 value;
    };

    struct Write_output {};

    PUBLIC_PROCEDURE(Write)
    {
        state.mut().scratch.writeScratch = input.value;
        state.mut().a = (sint8)state.mut().scratch.writeScratch;
        // The original's line: b = (0 - uint8(a)) * 2.
        state.mut().b = (uint8)((0 - (uint8)state.get().a) * 2);
        state.mut().c = (sint8)(0 - state.mut().scratch.writeScratch);
        state.mut().d = (sint16)(state.mut().scratch.writeScratch * 3);
        state.mut().e = (uint16)(0 - (uint16)state.get().d);
    }

    struct Read_input {};

    struct Read_output
    {
        sint8 a;
        uint8 b;
        sint8 c;
        sint16 d;
        uint16 e;
        uint64 guard;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.a = state.get().a;
        output.b = state.get().b;
        output.c = state.get().c;
        output.d = state.get().d;
        output.e = state.get().e;
        output.guard = state.get().guard;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }

    INITIALIZE()
    {

    }
};
