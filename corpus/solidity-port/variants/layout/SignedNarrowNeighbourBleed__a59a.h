// SignedNarrowNeighbourBleed — ported from storage/packed_storage_signed.sol
// Stresses: signed narrow members at their extremes, next to each other
// Port caveat: slot packing becomes C++ placement
// Variant: entryOrder=declared entryShape=direct initStyle=empty layout=widestFirst placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedNarrowNeighbourBleed2
{
};

struct SignedNarrowNeighbourBleed : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        sint8 a;
        uint8 b;
        sint8 c;
        sint16 d;
        uint16 e;
        uint64 guard;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        sint8 a;
        uint8 b;
        sint8 c;
        sint16 d;
        uint16 e;
        uint64 guard;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.a = state.get().inner.a;
        output.b = state.get().inner.b;
        output.c = state.get().inner.c;
        output.d = state.get().inner.d;
        output.e = state.get().inner.e;
        output.guard = state.get().inner.guard;
    }

    struct Write_input
    {
        sint64 value;
    };

    struct Write_output {};

    struct Write_locals
    {
        sint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Write)
    {
        locals.scratch = input.value;
        state.mut().inner.a = (sint8)locals.scratch;
        // The original's line: b = (0 - uint8(a)) * 2.
        state.mut().inner.b = (uint8)((0 - (uint8)state.get().inner.a) * 2);
        state.mut().inner.c = (sint8)(0 - locals.scratch);
        state.mut().inner.d = (sint16)(locals.scratch * 3);
        state.mut().inner.e = (uint16)(0 - (uint16)state.get().inner.d);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }

    INITIALIZE()
    {

    }
};
