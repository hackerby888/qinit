// HashMapSizeAgainstCapacity — ported from test/libsolidity/semanticTests/mappings/mapping_of_mappings.sol
// Stresses: sizeof of a HashMap at four capacities and two value widths — the table carries occupation bookkeeping beyond its entries, and how much is a layout fact both backends must share
// Port caveat: Solidity mappings have no size at all; this measures QPI's own container, which is why it has no original.
// Variant: entryOrder=declared entryShape=direct initStyle=absent layout=widestLast placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

using Tiny = HashMap<uint64, uint64, 2>;
using Small = HashMap<uint64, uint64, 8>;
using Medium = HashMap<uint64, uint64, 64>;
using WideKey = HashMap<id, uint64, 8>;

struct HashMapSizeAgainstCapacity2
{
};

struct HashMapSizeAgainstCapacity : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 perEntry;
        uint64 wideValue;
        uint64 medium;
        uint64 small;
        uint64 tiny;
    };

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        state.mut().tiny = sizeof(Tiny);
        state.mut().small = sizeof(Small);
        state.mut().medium = sizeof(Medium);
        state.mut().wideValue = sizeof(WideKey);
        // Cast before dividing: sizeof yields an unsigned long, and QPI::div deduces one type.
        state.mut().perEntry = QPI::div((uint64)sizeof(Medium) - (uint64)sizeof(Small), 56ULL);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 tiny;
        uint64 small;
        uint64 medium;
        uint64 wideValue;
        uint64 perEntry;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.tiny = state.get().tiny;
        output.small = state.get().small;
        output.medium = state.get().medium;
        output.wideValue = state.get().wideValue;
        output.perEntry = state.get().perEntry;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }
};
