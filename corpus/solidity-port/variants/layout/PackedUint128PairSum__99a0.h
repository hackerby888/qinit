// PackedUint128PairSum — ported from OpenZeppelin DoubleEndedQueue begin/end
// Stresses: adjacent uint128 fields
// Variant: entryOrder=reversed entryShape=direct initStyle=absent layout=widestFirst placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Cursor
{
    uint128 begin;
    uint128 end;
};

struct PackedUint128PairSum2
{
};

struct PackedUint128PairSum : public ContractBase
{
    struct Scratch
    {
        uint128 advanceScratch;
    };

    struct StateData
    {
        Cursor cursor;
        uint128 span;
        uint64 cursorSize;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint128 span;
        uint64 cursorSize;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.span = state.get().span;
        output.cursorSize = state.get().cursorSize;
    }

    struct Advance_input
    {
        uint128 beginDelta;
        uint128 endDelta;
    };

    struct Advance_output {};

    PUBLIC_PROCEDURE(Advance)
    {
        state.mut().cursor.begin += input.beginDelta;
        state.mut().cursor.end += input.endDelta;
        state.mut().scratch.advanceScratch = state.get().cursor.end - state.get().cursor.begin;
        state.mut().span = state.mut().scratch.advanceScratch;
        state.mut().cursorSize = sizeof(Cursor);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Advance, 1);
    }
};
