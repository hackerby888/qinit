// SizeofScalarMatrix — ported from test/libsolidity/semanticTests/structs/struct_packing.sol
// Stresses: sizeof of every QPI scalar in one contract — the base case every other layout question is built on, asserted rather than merely compared
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty layout=widestLast placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SizeofScalarMatrix2
{
};

struct SizeofScalarMatrix : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 idSize;
        uint64 u128Size;
        uint64 s64Size;
        uint64 s8Size;
        uint64 u64Size;
        uint64 u32Size;
        uint64 u16Size;
        uint64 u8Size;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 u8Size;
        uint64 u16Size;
        uint64 u32Size;
        uint64 u64Size;
        uint64 s8Size;
        uint64 s64Size;
        uint64 u128Size;
        uint64 idSize;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        Measure_input forwardedInput;
        Measure_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        locals.forwardedInput = input;
        CALL(MeasureBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.u8Size = state.get().inner.u8Size;
        output.u16Size = state.get().inner.u16Size;
        output.u32Size = state.get().inner.u32Size;
        output.u64Size = state.get().inner.u64Size;
        output.s8Size = state.get().inner.s8Size;
        output.s64Size = state.get().inner.s64Size;
        output.u128Size = state.get().inner.u128Size;
        output.idSize = state.get().inner.idSize;
    }

    using MeasureBody_input = Measure_input;

    using MeasureBody_output = Measure_output;

    struct MeasureBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(MeasureBody)
    {
        state.mut().inner.u8Size = sizeof(uint8);
        state.mut().inner.u16Size = sizeof(uint16);
        state.mut().inner.u32Size = sizeof(uint32);
        state.mut().inner.u64Size = sizeof(uint64);
        state.mut().inner.s8Size = sizeof(sint8);
        state.mut().inner.s64Size = sizeof(sint64);
        state.mut().inner.u128Size = sizeof(uint128);
        state.mut().inner.idSize = sizeof(id);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {

    }
};
