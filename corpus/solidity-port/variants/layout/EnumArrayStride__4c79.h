// EnumArrayStride — ported from test/libsolidity/semanticTests/enums/enum_explicit_overflow.sol
// Stresses: arrays of three enums with different constant ranges — the element stride is the underlying type the compiler chose, so the array sizes say what that choice was
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty layout=declared placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

enum Small { A = 0, B = 1 };

enum Medium { C = 0, D = 300 };

enum Large { E = 0, F = 100000 };

using SmallArray = Array<Small, 8>;
using MediumArray = Array<Medium, 8>;
using LargeArray = Array<Large, 8>;

struct EnumArrayStride2
{
};

struct EnumArrayStride : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 largeSize;
        uint64 mediumSize;
        uint64 smallSize;
        uint64 largeArray;
        uint64 mediumArray;
        uint64 smallArray;
    };

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        Measure_input forwardedInput;
        Measure_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        locals.forwardedInput = input;
        CALL(MeasureBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 smallArray;
        uint64 mediumArray;
        uint64 largeArray;
        uint64 smallSize;
        uint64 mediumSize;
        uint64 largeSize;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using MeasureBody_input = Measure_input;

    using MeasureBody_output = Measure_output;

    struct MeasureBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(MeasureBody)
    {
        state.mut().smallArray = sizeof(SmallArray);
        state.mut().mediumArray = sizeof(MediumArray);
        state.mut().largeArray = sizeof(LargeArray);
        state.mut().smallSize = sizeof(Small);
        state.mut().mediumSize = sizeof(Medium);
        state.mut().largeSize = sizeof(Large);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.smallArray = state.get().smallArray;
        output.mediumArray = state.get().mediumArray;
        output.largeArray = state.get().largeArray;
        output.smallSize = state.get().smallSize;
        output.mediumSize = state.get().mediumSize;
        output.largeSize = state.get().largeSize;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {

    }
};
