// ArrayOfIdStride — ported from array/fixed_arrays_in_storage.sol
// Stresses: 32-byte element stride and index masking
// Variant: capacity=64 entryOrder=declared entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ArrayOfIdStride2
{
};

struct ArrayOfIdStride : public ContractBase
{
    struct Scratch
    {
        id writeDerived;
    };

    struct StateData
    {
        struct Inner
        {
        Array<id, 8> holders;
        uint64 canary;
        uint64 writes;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input
    {
        uint64 index;
    };

    struct Read_output
    {
        uint64 firstWord;
        uint64 lastWord;
        uint64 neighbourFirstWord;
        uint64 canary;
        uint64 writes;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.firstWord = state.get().inner.holders.get(input.index).u64._0;
        output.lastWord = state.get().inner.holders.get(input.index).u64._3;
        output.neighbourFirstWord = state.get().inner.holders.get(input.index + 1).u64._0;
        output.canary = state.get().inner.canary;
        output.writes = state.get().inner.writes;
    }

    struct Write_input
    {
        uint64 index;
        uint64 seed;
    };

    struct Write_output {};

    PUBLIC_PROCEDURE(Write)
    {
        state.mut().inner.scratch.writeDerived = qpi.K12(input.seed);
        state.mut().inner.holders.set(input.index, state.mut().inner.scratch.writeDerived);
        state.mut().inner.writes++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }
};
