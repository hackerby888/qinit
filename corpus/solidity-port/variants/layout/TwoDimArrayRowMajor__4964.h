// TwoDimArrayRowMajor — ported from array/array_2d_assignment.sol
// Stresses: two-dimensional stride
// Variant: capacity=8 entryOrder=reversed entryShape=viaPrivate initStyle=full placement=first stateOrder=declared temporaries=locals width=uint64
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct TwoDimArrayRowMajor2
{
};

struct TwoDimArrayRowMajor : public ContractBase
{
    struct StateData
    {
        Array<Array<uint64, 8>, 8> grid;
        uint64 checksum;
        uint64 gridSize;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 checksum;
        uint64 gridSize;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Poke_input
    {
        uint64 row;
        uint64 column;
        uint64 value;
    };

    struct Poke_output {};

    struct Poke_locals
    {
        Poke_input forwardedInput;
        Poke_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Poke)
    {
        locals.forwardedInput = input;
        CALL(PokeBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.checksum = state.get().checksum;
        output.gridSize = state.get().gridSize;
    }

    using PokeBody_input = Poke_input;

    using PokeBody_output = Poke_output;

    struct PokeBody_locals
    {
        uint64 r;
        uint64 c;
        uint64 total;
        Array<uint64, 8> line;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(PokeBody)
    {
        locals.line = state.get().grid.get(input.row);
        locals.line.set(input.column, (uint64)input.value);
        state.mut().grid.set(input.row, locals.line);
        locals.total = 0;
        for (locals.r = 0; locals.r < 8; locals.r++)
        {
        for (locals.c = 0; locals.c < 8; locals.c++)
        {
        locals.total += (uint64)state.get().grid.get(locals.r).get(locals.c);
        }
        }
        state.mut().checksum = locals.total;
        state.mut().gridSize = sizeof(state.get().grid);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Poke, 1);
    }

    INITIALIZE()
    {
        state.mut().checksum = 0;
        state.mut().gridSize = 0;
    }
};
