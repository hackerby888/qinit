// PackedNeighbourOverflowCanary — ported from test/libsolidity/semanticTests/storage/packed_storage_overflow.sol
// Stresses: wraparound confined to its own field
// Variant: entryOrder=reversed entryShape=direct initStyle=absent layout=padded placement=last stateOrder=reversed temporaries=locals width=uint16
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct PackedNeighbourOverflowCanary2
{
};

struct PackedNeighbourOverflowCanary : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 captured;
        uint16 b;
        uint16 a;
        uint16 x;
    };

    struct Wrap_input
    {
        uint64 seed;
    };

    struct Wrap_output {};

    struct Wrap_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Wrap)
    {
        state.mut().x = (uint16)0x1234;
        state.mut().a = (uint16)input.seed;
        state.mut().b = 0;
        state.mut().a++;
        locals.scratch = (uint64)state.get().b;
        state.mut().b = 0;
        state.mut().a -= 2;
        state.mut().captured = locals.scratch;
    }

    struct Read_input {};

    struct Read_output
    {
        uint16 x;
        uint16 a;
        uint16 b;
        uint64 captured;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.x = state.get().x;
        output.a = state.get().a;
        output.b = state.get().b;
        output.captured = state.get().captured;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Wrap, 1);
    }
};
