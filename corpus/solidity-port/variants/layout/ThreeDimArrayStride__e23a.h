// ThreeDimArrayStride — ported from test/libsolidity/semanticTests/array/multidim_array.sol
// Stresses: three-axis indexing and the corner element
// Port caveat: no slot packing
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ThreeDimArrayStride2
{
};

struct ThreeDimArrayStride : public ContractBase
{
    struct Scratch
    {
        Array<Array<uint32, 8>, 4> writePlane;
        Array<uint32, 8> writeLine;
    };

    struct StateData
    {
        uint64 placementGuard;
        Array<Array<Array<uint32, 8>, 4>, 2> cube;
        uint64 guardBefore;
        uint64 guardAfter;
        uint64 writes;
        Scratch scratch;
    };

    struct Write_input
    {
        uint64 x;
        uint64 y;
        uint64 z;
        uint32 value;
    };

    struct Write_output {};

    struct Write_locals
    {
        Write_input forwardedInput;
        Write_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Write)
    {
        locals.forwardedInput = input;
        CALL(WriteBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input
    {
        uint64 x;
        uint64 y;
        uint64 z;
    };

    struct Read_output
    {
        uint32 value;
        uint32 origin;
        uint32 corner;
        uint64 guardBefore;
        uint64 guardAfter;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        locals.forwardedInput = input;
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using WriteBody_input = Write_input;

    using WriteBody_output = Write_output;

    PRIVATE_PROCEDURE(WriteBody)
    {
        state.mut().scratch.writePlane = state.get().cube.get(input.x);
        state.mut().scratch.writeLine = state.mut().scratch.writePlane.get(input.y);
        state.mut().scratch.writeLine.set(input.z, input.value);
        state.mut().scratch.writePlane.set(input.y, state.mut().scratch.writeLine);
        state.mut().cube.set(input.x, state.mut().scratch.writePlane);
        state.mut().writes++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.value = state.get().cube.get(input.x).get(input.y).get(input.z);
        output.origin = state.get().cube.get(0).get(0).get(0);
        output.corner = state.get().cube.get(1).get(3).get(7);
        output.guardBefore = state.get().guardBefore;
        output.guardAfter = state.get().guardAfter;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }
};
