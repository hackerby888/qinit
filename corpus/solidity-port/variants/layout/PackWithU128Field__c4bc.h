// PackWithU128Field — ported from test/libsolidity/semanticTests/types/uint256_conversion.sol
// Stresses: a uint128 next to narrow fields — 16-byte alignment, which is where a hand-written layout pass most often disagrees with clang
// Port caveat: Stands in for Solidity's uint256; the alignment question is the same one width down.
// Variant: entryOrder=reversed entryShape=direct initStyle=absent layout=declared placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Packed
{
    uint128 huge;
    uint32 medium;
    uint8 tiny;
};

struct PackWithU128Field2
{
};

struct PackWithU128Field : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        Packed packed;
        uint64 packedSize;
        uint64 stateSize;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Write_input
    {
        uint64 value;
    };

    struct Write_output {};

    struct Write_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Write)
    {
        state.mut().inner.packed.medium = input.value;
        state.mut().inner.packedSize = sizeof(Packed);
        state.mut().inner.stateSize = sizeof(StateData);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 packedSize;
        uint64 stateSize;
        uint64 probe;
        uint64 guard;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.packedSize = state.get().inner.packedSize;
        output.stateSize = state.get().inner.stateSize;
        output.probe = (uint64)state.get().inner.packed.medium;
        output.guard = state.get().placementGuard;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }
};
