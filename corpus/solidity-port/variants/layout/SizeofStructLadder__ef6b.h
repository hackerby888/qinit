// SizeofStructLadder — ported from test/libsolidity/semanticTests/structs/struct_packing.sol
// Stresses: six structs adding one uint8 at a time to a uint64 base — the size steps by 8 only when the accumulated bytes cross the alignment, which is a rule a second implementation can get subtly wrong
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full layout=widestFirst placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct S0 { uint64 wide; };

struct S1 { uint64 wide; uint8 a; };

struct S2 { uint64 wide; uint8 a; uint8 b; };

struct S4 { uint64 wide; uint8 a; uint8 b; uint8 c; uint8 d; };

struct S8 { uint64 wide; uint8 a; uint8 b; uint8 c; uint8 d; uint8 e; uint8 f; uint8 g; uint8 h; };

struct S9 { uint64 wide; uint8 a; uint8 b; uint8 c; uint8 d; uint8 e; uint8 f; uint8 g; uint8 h; uint8 i; };

struct SizeofStructLadder2
{
};

struct SizeofStructLadder : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 size0;
        uint64 size1;
        uint64 size2;
        uint64 size4;
        uint64 size8;
        uint64 size9;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 size0;
        uint64 size1;
        uint64 size2;
        uint64 size4;
        uint64 size8;
        uint64 size9;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        Measure_input forwardedInput;
        Measure_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        locals.forwardedInput = input;
        CALL(MeasureBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.size0 = state.get().size0;
        output.size1 = state.get().size1;
        output.size2 = state.get().size2;
        output.size4 = state.get().size4;
        output.size8 = state.get().size8;
        output.size9 = state.get().size9;
    }

    using MeasureBody_input = Measure_input;

    using MeasureBody_output = Measure_output;

    struct MeasureBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(MeasureBody)
    {
        state.mut().size0 = sizeof(S0);
        state.mut().size1 = sizeof(S1);
        state.mut().size2 = sizeof(S2);
        state.mut().size4 = sizeof(S4);
        state.mut().size8 = sizeof(S8);
        state.mut().size9 = sizeof(S9);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {
        state.mut().size0 = 0;
        state.mut().size1 = 0;
        state.mut().size2 = 0;
        state.mut().size4 = 0;
        state.mut().size8 = 0;
        state.mut().size9 = 0;
    }
};
