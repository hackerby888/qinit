// StructCopyWholeAssign — ported from test/libsolidity/semanticTests/structs/struct_copy.sol
// Stresses: whole-struct assignment including padding
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Nested
{
    uint64 x;
    uint64 y;
};

struct Holder
{
    uint64 a;
    Nested nested;
    uint8 tag;
};

struct StructCopyWholeAssign2
{
};

struct StructCopyWholeAssign : public ContractBase
{
    struct Scratch
    {
        Holder setAndCopyScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 holderSize;
        Holder target;
        Holder source;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct SetAndCopy_input
    {
        uint64 value;
    };

    struct SetAndCopy_output {};

    struct SetAndCopy_locals
    {
        SetAndCopy_input forwardedInput;
        SetAndCopy_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(SetAndCopy)
    {
        locals.forwardedInput = input;
        CALL(SetAndCopyBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 a;
        uint64 x;
        uint64 y;
        uint64 tag;
        uint64 holderSize;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using SetAndCopyBody_input = SetAndCopy_input;

    using SetAndCopyBody_output = SetAndCopy_output;

    PRIVATE_PROCEDURE(SetAndCopyBody)
    {
        state.mut().inner.source.a = input.value;
        state.mut().inner.source.nested.x = input.value + 3;
        state.mut().inner.source.nested.y = input.value + 4;
        state.mut().inner.source.tag = (uint8)input.value;
        state.mut().inner.scratch.setAndCopyScratch = state.get().inner.source;
        state.mut().inner.target = state.mut().inner.scratch.setAndCopyScratch;
        state.mut().inner.holderSize = sizeof(Holder);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.a = state.get().inner.target.a;
        output.x = state.get().inner.target.nested.x;
        output.y = state.get().inner.target.nested.y;
        output.tag = (uint64)state.get().inner.target.tag;
        output.holderSize = state.get().inner.holderSize;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(SetAndCopy, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.holderSize = 0;
    }
};
