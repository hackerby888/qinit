// StructWithTrailingBitArray — ported from structs/struct_packing.sol
// Stresses: element stride over a struct ending in a BitArray
// Port caveat: packed bools become a BitArray
// Variant: capacity=1024 entryOrder=declared entryShape=direct initStyle=full layout=declared placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Entry
{
    uint64 amount;
    uint16 tag;
    BitArray<64> flags;
    uint8 kind;
};

struct StructWithTrailingBitArray2
{
};

struct StructWithTrailingBitArray : public ContractBase
{
    struct Scratch
    {
        Entry writeEntry;
    };

    struct StateData
    {
        struct Inner
        {
        Array<Entry, 8> entries;
        uint64 canary;
        uint64 writes;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input
    {
        uint64 index;
    };

    struct Read_output
    {
        uint64 amount;
        uint16 tag;
        uint8 kind;
        uint64 bitZero;
        uint64 bit63;
        uint64 neighbourAmount;
        uint64 canary;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.amount = state.get().inner.entries.get(input.index).amount;
        output.tag = state.get().inner.entries.get(input.index).tag;
        output.kind = state.get().inner.entries.get(input.index).kind;
        output.bitZero = state.get().inner.entries.get(input.index).flags.get(0);
        output.bit63 = state.get().inner.entries.get(input.index).flags.get(63);
        output.neighbourAmount = state.get().inner.entries.get(input.index + 1).amount;
        output.canary = state.get().inner.canary;
    }

    struct Write_input
    {
        uint64 index;
        uint64 amount;
        uint64 bit;
    };

    struct Write_output {};

    PUBLIC_PROCEDURE(Write)
    {
        state.mut().inner.scratch.writeEntry = state.get().inner.entries.get(input.index);
        state.mut().inner.scratch.writeEntry.amount = input.amount;
        state.mut().inner.scratch.writeEntry.tag = (uint16)input.bit;
        state.mut().inner.scratch.writeEntry.kind = (uint8)QPI::mod(input.amount, 251ULL);
        state.mut().inner.scratch.writeEntry.flags.set(input.bit, 1);
        state.mut().inner.entries.set(input.index, state.mut().inner.scratch.writeEntry);
        state.mut().inner.writes++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }

    struct INITIALIZE_locals
    {
        uint64 i;
        Entry entry;
    };

    INITIALIZE_WITH_LOCALS()
    {
        state.mut().inner.canary = 18446744073709551615ULL;
        state.mut().inner.writes = 0;
        locals.entry.amount = 0;
        locals.entry.tag = 0;
        locals.entry.kind = 0;
        locals.entry.flags.setAll(0);
        for (locals.i = 0; locals.i < 8; locals.i++)
        {
        state.mut().inner.entries.set(locals.i, locals.entry);
        }
    }
};
