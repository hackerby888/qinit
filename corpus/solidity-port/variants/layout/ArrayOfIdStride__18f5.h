// ArrayOfIdStride — ported from array/fixed_arrays_in_storage.sol
// Stresses: 32-byte element stride and index masking
// Variant: capacity=8 entryOrder=declared entryShape=viaPrivate initStyle=full placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ArrayOfIdStride2
{
};

struct ArrayOfIdStride : public ContractBase
{
    struct Scratch
    {
        id writeDerived;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 writes;
        uint64 canary;
        Array<id, 8> holders;
        Scratch scratch;
    };

    struct Read_input
    {
        uint64 index;
    };

    struct Read_output
    {
        uint64 firstWord;
        uint64 lastWord;
        uint64 neighbourFirstWord;
        uint64 canary;
        uint64 writes;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        locals.forwardedInput = input;
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Write_input
    {
        uint64 index;
        uint64 seed;
    };

    struct Write_output {};

    struct Write_locals
    {
        Write_input forwardedInput;
        Write_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Write)
    {
        locals.forwardedInput = input;
        CALL(WriteBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.firstWord = state.get().holders.get(input.index).u64._0;
        output.lastWord = state.get().holders.get(input.index).u64._3;
        output.neighbourFirstWord = state.get().holders.get(input.index + 1).u64._0;
        output.canary = state.get().canary;
        output.writes = state.get().writes;
    }

    using WriteBody_input = Write_input;

    using WriteBody_output = Write_output;

    PRIVATE_PROCEDURE(WriteBody)
    {
        state.mut().scratch.writeDerived = qpi.K12(input.seed);
        state.mut().holders.set(input.index, state.mut().scratch.writeDerived);
        state.mut().writes++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }

    INITIALIZE()
    {
        state.mut().holders.setAll(NULL_ID);
        state.mut().canary = 18446744073709551615ULL;
        state.mut().writes = 0;
    }
};
