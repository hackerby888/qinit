// Uint128ArrayStride — ported from test/libsolidity/semanticTests/structs/struct_packing.sol
// Stresses: an Array of uint128 written at both ends, with the neighbour read back — the widest scalar element stride QPI has
// Variant: entryOrder=declared entryShape=direct initStyle=empty layout=widestLast placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

using WideArray = Array<uint128, 4>;

struct Uint128ArrayStride2
{
};

struct Uint128ArrayStride : public ContractBase
{
    struct Scratch
    {
        uint128 measureValue;
    };

    struct StateData
    {
        Array<uint128, 4> wide;
        uint64 arraySize;
        uint64 elementSize;
        uint64 firstLow;
        uint64 lastLow;
        uint64 canary;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    PUBLIC_PROCEDURE(Measure)
    {
        state.mut().scratch.measureValue = (uint128)input.seed;
        state.mut().scratch.measureValue = state.mut().scratch.measureValue * (uint128)18446744073709551615ULL;
        state.mut().wide.set(0, state.mut().scratch.measureValue);
        state.mut().wide.set(3, state.mut().scratch.measureValue + (uint128)1);
        state.mut().arraySize = sizeof(WideArray);
        state.mut().elementSize = sizeof(uint128);
        state.mut().firstLow = (uint64)state.get().wide.get(0);
        state.mut().lastLow = (uint64)state.get().wide.get(3);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 arraySize;
        uint64 elementSize;
        uint64 firstLow;
        uint64 lastLow;
        uint64 canary;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.arraySize = state.get().arraySize;
        output.elementSize = state.get().elementSize;
        output.firstLow = state.get().firstLow;
        output.lastLow = state.get().lastLow;
        output.canary = state.get().canary;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {

    }
};
