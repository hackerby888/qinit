// EnumArrayStride — ported from test/libsolidity/semanticTests/enums/enum_explicit_overflow.sol
// Stresses: arrays of three enums with different constant ranges — the element stride is the underlying type the compiler chose, so the array sizes say what that choice was
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full layout=widestFirst placement=nested stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

enum Small { A = 0, B = 1 };

enum Medium { C = 0, D = 300 };

enum Large { E = 0, F = 100000 };

using SmallArray = Array<Small, 8>;
using MediumArray = Array<Medium, 8>;
using LargeArray = Array<Large, 8>;

struct EnumArrayStride2
{
};

struct EnumArrayStride : public ContractBase
{
    struct Scratch
    {
        uint64 measureScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 largeSize;
        uint64 mediumSize;
        uint64 smallSize;
        uint64 largeArray;
        uint64 mediumArray;
        uint64 smallArray;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 smallArray;
        uint64 mediumArray;
        uint64 largeArray;
        uint64 smallSize;
        uint64 mediumSize;
        uint64 largeSize;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Measure_input
    {
        uint64 seed;
    };

    struct Measure_output {};

    struct Measure_locals
    {
        Measure_input forwardedInput;
        Measure_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Measure)
    {
        locals.forwardedInput = input;
        CALL(MeasureBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.smallArray = state.get().inner.smallArray;
        output.mediumArray = state.get().inner.mediumArray;
        output.largeArray = state.get().inner.largeArray;
        output.smallSize = state.get().inner.smallSize;
        output.mediumSize = state.get().inner.mediumSize;
        output.largeSize = state.get().inner.largeSize;
    }

    using MeasureBody_input = Measure_input;

    using MeasureBody_output = Measure_output;

    PRIVATE_PROCEDURE(MeasureBody)
    {
        state.mut().inner.smallArray = sizeof(SmallArray);
        state.mut().inner.mediumArray = sizeof(MediumArray);
        state.mut().inner.largeArray = sizeof(LargeArray);
        state.mut().inner.smallSize = sizeof(Small);
        state.mut().inner.mediumSize = sizeof(Medium);
        state.mut().inner.largeSize = sizeof(Large);
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Measure, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.smallArray = 0;
        state.mut().inner.mediumArray = 0;
        state.mut().inner.largeArray = 0;
        state.mut().inner.smallSize = 0;
        state.mut().inner.mediumSize = 0;
        state.mut().inner.largeSize = 0;
    }
};
