// EmptyStructMemberSize — ported from test/libsolidity/semanticTests/structs/empty_struct.sol
// Stresses: the size of an empty struct member and its effect on neighbours
// Port caveat: Solidity forbids empty structs
// Variant: entryOrder=reversed entryShape=direct initStyle=empty layout=declared placement=last stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Marker
{
};

struct Framed
{
    uint64 first;
    Marker marker;
    uint64 second;
};

struct EmptyStructMemberSize2
{
};

struct EmptyStructMemberSize : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        Framed framed;
        uint64 markerSize;
        uint64 framedSize;
        uint64 writes;
    };

    struct Write_input
    {
        uint64 value;
    };

    struct Write_output {};

    struct Write_locals
    {
        uint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Write)
    {
        locals.scratch = input.value;
        state.mut().framed.first = locals.scratch;
        state.mut().framed.second = locals.scratch * 2;
        state.mut().markerSize = sizeof(Marker);
        state.mut().framedSize = sizeof(Framed);
        state.mut().writes++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 first;
        uint64 second;
        uint64 markerSize;
        uint64 framedSize;
        uint64 writes;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.first = state.get().framed.first;
        output.second = state.get().framed.second;
        output.markerSize = state.get().markerSize;
        output.framedSize = state.get().framedSize;
        output.writes = state.get().writes;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }

    INITIALIZE()
    {

    }
};
