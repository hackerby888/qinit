// IdBetweenNarrowMembers — ported from test/libsolidity/semanticTests/structs/struct_packing.sol
// Stresses: id alignment between two uint8 members
// Variant: entryOrder=reversed entryShape=direct initStyle=empty layout=widestLast placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Holder
{
    uint8 before;
    id who;
    uint8 after;
};

struct IdBetweenNarrowMembers2
{
};

struct IdBetweenNarrowMembers : public ContractBase
{
    struct Scratch
    {
        id writeDerived;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 canaryBefore;
        Holder holder;
        uint64 canaryAfter;
        uint64 size;
        uint64 writes;
        Scratch scratch;
    };

    struct Write_input
    {
        uint64 value;
    };

    struct Write_output {};

    PUBLIC_PROCEDURE(Write)
    {
        state.mut().scratch.writeDerived = qpi.K12(input.value);
        state.mut().holder.before = (uint8)input.value;
        state.mut().holder.who = state.mut().scratch.writeDerived;
        state.mut().holder.after = (uint8)(input.value + 3);
        state.mut().size = sizeof(Holder);
        state.mut().writes++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint8 before;
        uint8 after;
        uint64 idFirstWord;
        uint64 canaryBefore;
        uint64 canaryAfter;
        uint64 size;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.before = state.get().holder.before;
        output.after = state.get().holder.after;
        output.idFirstWord = state.get().holder.who.u64._0;
        output.canaryBefore = state.get().canaryBefore;
        output.canaryAfter = state.get().canaryAfter;
        output.size = state.get().size;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }

    INITIALIZE()
    {

    }
};
