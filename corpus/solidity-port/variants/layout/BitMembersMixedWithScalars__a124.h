// BitMembersMixedWithScalars — ported from types/bool_storage.sol
// Stresses: bit members between scalars
// Port caveat: bool packing differs from Solidity by construction
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full layout=widestLast placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct Flags
{
    bit first;
    uint64 middle;
    bit second;
    uint8 tail;
};

struct BitMembersMixedWithScalars2
{
};

struct BitMembersMixedWithScalars : public ContractBase
{
    struct Scratch
    {
        uint64 writeScratch;
    };

    struct StateData
    {
        Flags flags;
        uint64 flagsSize;
        uint64 bitSize;
        uint64 writes;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Write_input
    {
        uint64 value;
    };

    struct Write_output {};

    struct Write_locals
    {
        Write_input forwardedInput;
        Write_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Write)
    {
        locals.forwardedInput = input;
        CALL(WriteBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 first;
        uint64 second;
        uint64 middle;
        uint8 tail;
        uint64 flagsSize;
        uint64 bitSize;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using WriteBody_input = Write_input;

    using WriteBody_output = Write_output;

    PRIVATE_PROCEDURE(WriteBody)
    {
        state.mut().scratch.writeScratch = input.value;
        state.mut().flags.first = state.mut().scratch.writeScratch > 0;
        state.mut().flags.middle = state.mut().scratch.writeScratch;
        state.mut().flags.second = state.mut().scratch.writeScratch > 100;
        state.mut().flags.tail = (uint8)state.mut().scratch.writeScratch;
        state.mut().flagsSize = sizeof(Flags);
        state.mut().bitSize = sizeof(bit);
        state.mut().writes++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.first = state.get().flags.first ? 1 : 0;
        output.second = state.get().flags.second ? 1 : 0;
        output.middle = state.get().flags.middle;
        output.tail = state.get().flags.tail;
        output.flagsSize = state.get().flagsSize;
        output.bitSize = state.get().bitSize;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Write, 1);
    }

    INITIALIZE()
    {
        state.mut().flagsSize = 0;
        state.mut().bitSize = 0;
        state.mut().writes = 0;
    }
};
