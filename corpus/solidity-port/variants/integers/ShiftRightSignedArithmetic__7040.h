// ShiftRightSignedArithmetic — ported from test/libsolidity/semanticTests/operators/shifts/shift_right_negative_lvalue_int8.sol
// Stresses: arithmetic vs logical right shift on a negative operand — the sign bit must propagate
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=first stateOrder=reversed temporaries=locals width=sint64
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ShiftRightSignedArithmetic2
{
};

struct ShiftRightSignedArithmetic : public ContractBase
{
    struct StateData
    {
        uint64 applied;
        sint64 narrow;
        sint64 wide;
        uint64 placementGuard;
    };

    struct Shift_input
    {
        sint64 value;
        uint8 count;
    };

    struct Shift_output {};

    struct Shift_locals
    {
        sint64 shifted;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Shift)
    {
        locals.shifted = input.value >> input.count;
        state.mut().narrow = locals.shifted;
        state.mut().wide = (sint64)locals.shifted;
        state.mut().applied++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint64 narrow;
        uint64 applied;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.applied = state.get().applied;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Shift, 1);
    }

    INITIALIZE()
    {

    }
};
