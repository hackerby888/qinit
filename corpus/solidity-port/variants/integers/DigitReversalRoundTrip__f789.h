// DigitReversalRoundTrip — ported from openzeppelin-contracts/contracts/utils/Strings.sol
// Stresses: reversing a number's decimal digits and reversing again — the round trip only returns the original when it has no trailing zeros, which is the case the archetype counts
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct DigitReversalRoundTrip2
{
};

struct DigitReversalRoundTrip : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 reversed;
        uint64 doubleReversed;
        uint64 roundTrips;
        uint64 digits;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 reversed;
        uint64 doubleReversed;
        uint64 roundTrips;
        uint64 digits;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.reversed = state.get().inner.reversed;
        output.doubleReversed = state.get().inner.doubleReversed;
        output.roundTrips = state.get().inner.roundTrips;
        output.digits = state.get().inner.digits;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint64 value;
        uint64 out;
        uint64 count;
        uint64 i;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.value = input.a;
        locals.out = 0;
        locals.count = 0;
        while (locals.value != 0 && locals.count < 20)
        {
        locals.out = locals.out * 10 + QPI::mod(locals.value, 10ULL);
        locals.value = QPI::div(locals.value, 10ULL);
        locals.count++;
        }
        state.mut().inner.reversed = locals.out;
        state.mut().inner.digits = locals.count;
        locals.value = locals.out;
        locals.out = 0;
        for (locals.i = 0; locals.i < locals.count; locals.i++)
        {
        locals.out = locals.out * 10 + QPI::mod(locals.value, 10ULL);
        locals.value = QPI::div(locals.value, 10ULL);
        }
        state.mut().inner.doubleReversed = locals.out;
        state.mut().inner.roundTrips = locals.out == input.a ? 1 : 0;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
