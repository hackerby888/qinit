// SignExtendWidening — ported from test/libsolidity/semanticTests/types/convert_signed_to_wider.sol
// Stresses: sign extension when a negative narrow value widens — the bug that shows up as a huge positive number
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=nested stateOrder=reversed temporaries=locals width=uint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignExtendWidening2
{
};

struct SignExtendWidening : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        sint64 widened;
        sint8 narrow;
        uint64 calls;
        uint64 accumulator;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().inner.accumulator;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        sint8 value;
    };

    struct Run_output {};

    struct Run_locals
    {
        sint64 scratch;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        state.mut().inner.narrow = input.value;
        locals.scratch = (sint64)state.get().inner.narrow;
        state.mut().inner.widened = locals.scratch;
        state.mut().inner.accumulator += (uint64)locals.scratch;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
