// BooleanToIntegerConversions — ported from test/libsolidity/semanticTests/types/bool_conversion.sol
// Stresses: comparison results used as arithmetic — the value of `true` in a sum, and the sum of several comparisons in one expression
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BooleanToIntegerConversions2
{
};

struct BooleanToIntegerConversions : public ContractBase
{
    struct Scratch
    {
        uint64 runFirst;
        uint64 runSecond;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 asNumber;
        uint64 sumOfFlags;
        uint64 productOfFlags;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runFirst = input.a > input.b ? 1 : 0;
        state.mut().inner.scratch.runSecond = input.a == input.b ? 1 : 0;
        state.mut().inner.asNumber = state.mut().inner.scratch.runFirst;
        state.mut().inner.sumOfFlags = state.mut().inner.scratch.runFirst + state.mut().inner.scratch.runSecond + (input.a < input.b ? 1 : 0);
        state.mut().inner.productOfFlags = state.mut().inner.scratch.runFirst * 10 + state.mut().inner.scratch.runSecond;

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 asNumber;
        uint64 sumOfFlags;
        uint64 productOfFlags;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.asNumber = state.get().inner.asNumber;
        output.sumOfFlags = state.get().inner.sumOfFlags;
        output.productOfFlags = state.get().inner.productOfFlags;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
