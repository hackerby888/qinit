// ShiftRhsWiderThanLhs — ported from test/libsolidity/semanticTests/operators/shifts/shift_left_larger_type.sol
// Stresses: a shift count wider than the operand
// Variant: constSource=constexpr entryOrder=reversed entryShape=direct initStyle=full placement=nested stateOrder=reversed temporaries=locals width=sint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint8 FIXED_COUNT = 254;

struct ShiftRhsWiderThanLhs2
{
};

struct ShiftRhsWiderThanLhs : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        sint8 narrow;
        sint64 wide;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Shift_input
    {
        sint8 value;
        uint8 count;
    };

    struct Shift_output {};

    struct Shift_locals
    {
        sint8 shifted;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Shift)
    {
        locals.shifted = input.value << FIXED_COUNT;
        state.mut().inner.narrow = locals.shifted;
        state.mut().inner.wide = (sint64)locals.shifted;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint8 narrow;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().inner.wide;
        output.narrow = state.get().inner.narrow;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Shift, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.wide = 0;
        state.mut().inner.narrow = 0;
    }
};
