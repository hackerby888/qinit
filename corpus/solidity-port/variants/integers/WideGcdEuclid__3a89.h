// WideGcdEuclid — ported from openzeppelin-contracts/contracts/utils/math/Math.sol (gcd pattern)
// Stresses: Euclid's algorithm over QPI::mod — a loop whose trip count depends on the operands, with the zero and equal-operand cases driven
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct WideGcdEuclid2
{
};

struct WideGcdEuclid : public ContractBase
{
    struct Scratch
    {
        uint64 runX;
        uint64 runY;
        uint64 runT;
        uint64 runGuard;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 coprimes;
        uint64 iterations;
        uint64 gcd;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 gcd;
        uint64 iterations;
        uint64 coprimes;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.gcd = state.get().gcd;
        output.iterations = state.get().iterations;
        output.coprimes = state.get().coprimes;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runX = input.a;
        state.mut().scratch.runY = input.b;
        state.mut().scratch.runGuard = 0;
        while (state.mut().scratch.runY != 0 && state.mut().scratch.runGuard < 128)
        {
        state.mut().scratch.runT = QPI::mod(state.mut().scratch.runX, state.mut().scratch.runY);
        state.mut().scratch.runX = state.mut().scratch.runY;
        state.mut().scratch.runY = state.mut().scratch.runT;
        state.mut().scratch.runGuard++;
        state.mut().iterations++;
        }
        state.mut().gcd = state.mut().scratch.runX;
        if (state.mut().scratch.runX == 1)
        {
        state.mut().coprimes++;
        }

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
