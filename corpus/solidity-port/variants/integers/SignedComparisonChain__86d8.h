// SignedComparisonChain — ported from test/libsolidity/semanticTests/expressions/comparison_operators.sol
// Stresses: all six comparison operators evaluated on one signed pair and packed into a bitfield, so a single wrong comparison changes one bit of a number
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedComparisonChain2
{
};

struct SignedComparisonChain : public ContractBase
{
    struct Scratch
    {
        sint64 runLeft;
        sint64 runRight;
        uint64 runBits;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 packed;
        uint64 trueCount;
        sint64 lhs;
        sint64 rhs;
        uint64 calls;
        Scratch scratch;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runLeft = (sint64)input.a;
        state.mut().scratch.runRight = (sint64)input.b;
        state.mut().scratch.runBits = 0;
        state.mut().scratch.runBits = state.mut().scratch.runBits | (state.mut().scratch.runLeft < state.mut().scratch.runRight ? 1ULL : 0ULL);
        state.mut().scratch.runBits = state.mut().scratch.runBits | (state.mut().scratch.runLeft <= state.mut().scratch.runRight ? 2ULL : 0ULL);
        state.mut().scratch.runBits = state.mut().scratch.runBits | (state.mut().scratch.runLeft == state.mut().scratch.runRight ? 4ULL : 0ULL);
        state.mut().scratch.runBits = state.mut().scratch.runBits | (state.mut().scratch.runLeft != state.mut().scratch.runRight ? 8ULL : 0ULL);
        state.mut().scratch.runBits = state.mut().scratch.runBits | (state.mut().scratch.runLeft >= state.mut().scratch.runRight ? 16ULL : 0ULL);
        state.mut().scratch.runBits = state.mut().scratch.runBits | (state.mut().scratch.runLeft > state.mut().scratch.runRight ? 32ULL : 0ULL);
        state.mut().packed = state.mut().scratch.runBits;
        state.mut().trueCount = (state.mut().scratch.runBits & 1) + ((state.mut().scratch.runBits >> 1) & 1) + ((state.mut().scratch.runBits >> 2) & 1) + ((state.mut().scratch.runBits >> 3) & 1) + ((state.mut().scratch.runBits >> 4) & 1) + ((state.mut().scratch.runBits >> 5) & 1);
        state.mut().lhs = state.mut().scratch.runLeft;
        state.mut().rhs = state.mut().scratch.runRight;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 packed;
        uint64 trueCount;
        sint64 lhs;
        sint64 rhs;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.packed = state.get().packed;
        output.trueCount = state.get().trueCount;
        output.lhs = state.get().lhs;
        output.rhs = state.get().rhs;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
