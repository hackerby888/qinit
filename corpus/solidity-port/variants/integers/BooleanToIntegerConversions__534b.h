// BooleanToIntegerConversions — ported from test/libsolidity/semanticTests/types/bool_conversion.sol
// Stresses: comparison results used as arithmetic — the value of `true` in a sum, and the sum of several comparisons in one expression
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BooleanToIntegerConversions2
{
};

struct BooleanToIntegerConversions : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 productOfFlags;
        uint64 sumOfFlags;
        uint64 asNumber;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint64 first;
        uint64 second;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.first = input.a > input.b ? 1 : 0;
        locals.second = input.a == input.b ? 1 : 0;
        state.mut().asNumber = locals.first;
        state.mut().sumOfFlags = locals.first + locals.second + (input.a < input.b ? 1 : 0);
        state.mut().productOfFlags = locals.first * 10 + locals.second;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 asNumber;
        uint64 sumOfFlags;
        uint64 productOfFlags;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.asNumber = state.get().asNumber;
        output.sumOfFlags = state.get().sumOfFlags;
        output.productOfFlags = state.get().productOfFlags;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
