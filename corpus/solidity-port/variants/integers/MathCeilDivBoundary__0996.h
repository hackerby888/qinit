// MathCeilDivBoundary — ported from OpenZeppelin utils/math/Math.sol (`ceilDiv`)
// Stresses: `a == 0 ? 0 : (a - 1) / b + 1` — the guard exists precisely so the `a - 1` does not wrap, driven at a = 0 and a = max
// Port caveat: Solidity's `/` becomes QPI::div, which returns 0 for a zero divisor rather than panicking; the b = 0 row asserts that.
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=first stateOrder=declared temporaries=stateScratch width=sint16
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct MathCeilDivBoundary2
{
};

struct MathCeilDivBoundary : public ContractBase
{
    struct Scratch
    {
        sint16 runUp;
        sint16 runDown;
    };

    struct StateData
    {
        uint64 accumulator;
        uint64 calls;
        sint16 ceiling;
        sint16 floor;
        uint64 differed;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().accumulator;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        sint16 a;
        sint16 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runDown = QPI::div(input.a, input.b);
        if (input.a == 0)
        {
        state.mut().scratch.runUp = 0;
        }
        else
        {
        state.mut().scratch.runUp = QPI::div((sint16)(input.a - 1), input.b) + 1;
        }
        state.mut().ceiling = state.mut().scratch.runUp;
        state.mut().floor = state.mut().scratch.runDown;
        if (state.mut().scratch.runUp != state.mut().scratch.runDown)
        {
        state.mut().differed++;
        }
        state.mut().accumulator = (uint64)state.mut().scratch.runUp;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
