// SignedBitwiseComplementLadder — ported from test/libsolidity/semanticTests/various/bit_operations.sol
// Stresses: the identity `~x == -x - 1` checked at four widths, which only holds if the complement and the negation agree about where the sign bit is
// Variant: entryOrder=reversed entryShape=direct initStyle=full placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedBitwiseComplementLadder2
{
};

struct SignedBitwiseComplementLadder : public ContractBase
{
    struct Scratch
    {
        sint64 runValue;
        sint8 runNarrow;
    };

    struct StateData
    {
        sint64 complement;
        sint64 negated;
        uint64 identityHolds;
        sint64 narrowComplement;
        uint64 calls;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runValue = (sint64)input.a;
        state.mut().scratch.runNarrow = (sint8)input.a;
        state.mut().complement = ~state.mut().scratch.runValue;
        state.mut().negated = (0 - state.mut().scratch.runValue) - 1;
        state.mut().identityHolds = state.get().complement == state.get().negated ? 1 : 0;
        state.mut().narrowComplement = (sint64)(sint8)(~state.mut().scratch.runNarrow);

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 complement;
        sint64 negated;
        uint64 identityHolds;
        sint64 narrowComplement;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.complement = state.get().complement;
        output.negated = state.get().negated;
        output.identityHolds = state.get().identityHolds;
        output.narrowComplement = state.get().narrowComplement;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().complement = 0;
        state.mut().negated = 0;
        state.mut().identityHolds = 0;
        state.mut().narrowComplement = 0;
        state.mut().calls = 0;
    }
};
