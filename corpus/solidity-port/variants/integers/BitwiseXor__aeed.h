// BitwiseXor — ported from test/libsolidity/semanticTests/operators/bitwise.sol
// Stresses: ^ across widths
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=locals width=sint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BitwiseXor2
{
};

struct BitwiseXor : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        sint64 wide;
        sint8 narrow;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint8 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().inner.wide;
        output.narrow = state.get().inner.narrow;
        output.calls = state.get().inner.calls;
    }

    struct Apply_input
    {
        sint8 a;
        sint8 b;
    };

    struct Apply_output {};

    struct Apply_locals
    {
        sint64 value;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Apply)
    {
        locals.value = input.a ^ input.b;
        state.mut().inner.wide = locals.value;
        state.mut().inner.narrow = input.a ^ input.b;
        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }
};
