// SignedComparisonChain — ported from test/libsolidity/semanticTests/expressions/comparison_operators.sol
// Stresses: all six comparison operators evaluated on one signed pair and packed into a bitfield, so a single wrong comparison changes one bit of a number
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedComparisonChain2
{
};

struct SignedComparisonChain : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 packed;
        uint64 trueCount;
        sint64 lhs;
        sint64 rhs;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 packed;
        uint64 trueCount;
        sint64 lhs;
        sint64 rhs;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        sint64 left;
        sint64 right;
        uint64 bits;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.left = (sint64)input.a;
        locals.right = (sint64)input.b;
        locals.bits = 0;
        locals.bits = locals.bits | (locals.left < locals.right ? 1ULL : 0ULL);
        locals.bits = locals.bits | (locals.left <= locals.right ? 2ULL : 0ULL);
        locals.bits = locals.bits | (locals.left == locals.right ? 4ULL : 0ULL);
        locals.bits = locals.bits | (locals.left != locals.right ? 8ULL : 0ULL);
        locals.bits = locals.bits | (locals.left >= locals.right ? 16ULL : 0ULL);
        locals.bits = locals.bits | (locals.left > locals.right ? 32ULL : 0ULL);
        state.mut().inner.packed = locals.bits;
        state.mut().inner.trueCount = (locals.bits & 1) + ((locals.bits >> 1) & 1) + ((locals.bits >> 2) & 1) + ((locals.bits >> 3) & 1) + ((locals.bits >> 4) & 1) + ((locals.bits >> 5) & 1);
        state.mut().inner.lhs = locals.left;
        state.mut().inner.rhs = locals.right;

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.packed = state.get().inner.packed;
        output.trueCount = state.get().inner.trueCount;
        output.lhs = state.get().inner.lhs;
        output.rhs = state.get().inner.rhs;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.packed = 0;
        state.mut().inner.trueCount = 0;
        state.mut().inner.lhs = 0;
        state.mut().inner.rhs = 0;
        state.mut().inner.calls = 0;
    }
};
