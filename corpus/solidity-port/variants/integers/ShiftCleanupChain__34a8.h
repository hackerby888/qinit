// ShiftCleanupChain — ported from operators/shifts/shift_cleanup.sol
// Stresses: compound shift chain on a narrow field
// Variant: width=sint16
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ShiftCleanupChain2
{
};

struct ShiftCleanupChain : public ContractBase
{
    struct Scratch
    {
        uint64 chainScratch;
    };

    struct StateData
    {
        sint16 value;
        uint64 witness;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Chain_input
    {
        sint16 seed;
        uint8 addend;
    };

    struct Chain_output {};

    PUBLIC_PROCEDURE(Chain)
    {
        state.mut().value = input.seed;
        state.mut().value += input.addend;
        state.mut().value <<= 8;
        state.mut().value >>= 16;
        state.mut().scratch.chainScratch = (uint64)state.get().value;
        state.mut().witness = state.mut().scratch.chainScratch;
    }

    struct Read_input {};

    struct Read_output
    {
        sint16 value;
        uint64 witness;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.value = state.get().value;
        output.witness = state.get().witness;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Chain, 1);
    }

    INITIALIZE()
    {
        state.mut().value = 0;
        state.mut().witness = 0;
    }
};
