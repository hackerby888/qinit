// SignExtensionAcrossWidths — ported from test/libsolidity/semanticTests/cleanup/cleanup_bytes_types.sol
// Stresses: the same bit pattern read as sint8, sint16 and sint32 and widened to 64 bits — three sign extensions of one value, which must disagree exactly where the sign bit moves
// Variant: entryOrder=reversed entryShape=direct initStyle=full placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignExtensionAcrossWidths2
{
};

struct SignExtensionAcrossWidths : public ContractBase
{
    struct Scratch
    {
        sint8 runAsByte;
        sint16 runAsWord;
        sint32 runAsDouble;
    };

    struct StateData
    {
        sint64 fromByte;
        sint64 fromWord;
        sint64 fromDouble;
        uint64 allNegative;
        uint64 calls;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runAsByte = (sint8)input.a;
        state.mut().scratch.runAsWord = (sint16)input.a;
        state.mut().scratch.runAsDouble = (sint32)input.a;
        state.mut().fromByte = (sint64)state.mut().scratch.runAsByte;
        state.mut().fromWord = (sint64)state.mut().scratch.runAsWord;
        state.mut().fromDouble = (sint64)state.mut().scratch.runAsDouble;
        state.mut().allNegative = state.get().fromByte < 0 && state.get().fromWord < 0 && state.get().fromDouble < 0 ? 1 : 0;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 fromByte;
        sint64 fromWord;
        sint64 fromDouble;
        uint64 allNegative;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.fromByte = state.get().fromByte;
        output.fromWord = state.get().fromWord;
        output.fromDouble = state.get().fromDouble;
        output.allNegative = state.get().allNegative;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().fromByte = 0;
        state.mut().fromWord = 0;
        state.mut().fromDouble = 0;
        state.mut().allNegative = 0;
        state.mut().calls = 0;
    }
};
