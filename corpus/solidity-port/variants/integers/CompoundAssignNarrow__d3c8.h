// CompoundAssignNarrow — ported from test/libsolidity/semanticTests/operators/compound_assignment.sol
// Stresses: `a += b` on a narrow field: the promotion happens, then the result is truncated back on store
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=first stateOrder=declared temporaries=stateScratch width=sint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CompoundAssignNarrow2
{
};

struct CompoundAssignNarrow : public ContractBase
{
    struct Scratch
    {
        uint64 runScratch;
    };

    struct StateData
    {
        uint64 accumulator;
        uint64 calls;
        sint8 narrow;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        sint8 delta;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().narrow += input.delta;
        state.mut().accumulator += (uint64)state.get().narrow;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().accumulator;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
