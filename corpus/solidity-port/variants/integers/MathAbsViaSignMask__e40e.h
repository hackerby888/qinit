// MathAbsViaSignMask — ported from OpenZeppelin utils/math/SignedMath.sol (`abs`)
// Stresses: `mask = n >> 63; abs = (n + mask) ^ mask` — an arithmetic shift on the sign bit, then a signed-to-unsigned reinterpretation, exercised at INT64_MIN where the result is not representable
// Port caveat: OpenZeppelin shifts by 255 for int256; the port shifts by 63 for sint64.
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct MathAbsViaSignMask2
{
};

struct MathAbsViaSignMask : public ContractBase
{
    struct Scratch
    {
        sint64 runMask;
        uint64 runMasked;
        uint64 runBranched;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 disagreements;
        uint64 viaBranch;
        uint64 viaMask;
        uint64 calls;
        uint64 accumulator;
        Scratch scratch;
    };

    struct Run_input
    {
        sint64 value;
        sint64 unused;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runMask = input.value >> 63;
        state.mut().scratch.runMasked = (uint64)((input.value + state.mut().scratch.runMask) ^ state.mut().scratch.runMask);
        if (input.value < 0)
        {
        state.mut().scratch.runBranched = (uint64)(0 - input.value);
        }
        else
        {
        state.mut().scratch.runBranched = (uint64)input.value;
        }
        state.mut().viaMask = state.mut().scratch.runMasked;
        state.mut().viaBranch = state.mut().scratch.runBranched;
        if (state.mut().scratch.runMasked != state.mut().scratch.runBranched)
        {
        state.mut().disagreements++;
        }
        state.mut().accumulator = state.mut().scratch.runMasked;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().accumulator;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
