// DigitReversalRoundTrip — ported from openzeppelin-contracts/contracts/utils/Strings.sol
// Stresses: reversing a number's decimal digits and reversing again — the round trip only returns the original when it has no trailing zeros, which is the case the archetype counts
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct DigitReversalRoundTrip2
{
};

struct DigitReversalRoundTrip : public ContractBase
{
    struct Scratch
    {
        uint64 runValue;
        uint64 runOut;
        uint64 runCount;
        uint64 runI;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 reversed;
        uint64 doubleReversed;
        uint64 roundTrips;
        uint64 digits;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 reversed;
        uint64 doubleReversed;
        uint64 roundTrips;
        uint64 digits;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.reversed = state.get().inner.reversed;
        output.doubleReversed = state.get().inner.doubleReversed;
        output.roundTrips = state.get().inner.roundTrips;
        output.digits = state.get().inner.digits;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runValue = input.a;
        state.mut().inner.scratch.runOut = 0;
        state.mut().inner.scratch.runCount = 0;
        while (state.mut().inner.scratch.runValue != 0 && state.mut().inner.scratch.runCount < 20)
        {
        state.mut().inner.scratch.runOut = state.mut().inner.scratch.runOut * 10 + QPI::mod(state.mut().inner.scratch.runValue, 10ULL);
        state.mut().inner.scratch.runValue = QPI::div(state.mut().inner.scratch.runValue, 10ULL);
        state.mut().inner.scratch.runCount++;
        }
        state.mut().inner.reversed = state.mut().inner.scratch.runOut;
        state.mut().inner.digits = state.mut().inner.scratch.runCount;
        state.mut().inner.scratch.runValue = state.mut().inner.scratch.runOut;
        state.mut().inner.scratch.runOut = 0;
        for (state.mut().inner.scratch.runI = 0; state.mut().inner.scratch.runI < state.mut().inner.scratch.runCount; state.mut().inner.scratch.runI++)
        {
        state.mut().inner.scratch.runOut = state.mut().inner.scratch.runOut * 10 + QPI::mod(state.mut().inner.scratch.runValue, 10ULL);
        state.mut().inner.scratch.runValue = QPI::div(state.mut().inner.scratch.runValue, 10ULL);
        }
        state.mut().inner.doubleReversed = state.mut().inner.scratch.runOut;
        state.mut().inner.roundTrips = state.mut().inner.scratch.runOut == input.a ? 1 : 0;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.reversed = 0;
        state.mut().inner.doubleReversed = 0;
        state.mut().inner.roundTrips = 0;
        state.mut().inner.digits = 0;
        state.mut().inner.calls = 0;
    }
};
