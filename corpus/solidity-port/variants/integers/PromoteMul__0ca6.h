// PromoteMul — ported from test/libsolidity/semanticTests/arithmetics/checked_mul.sol
// Stresses: overflow wrap and promotion on *; the narrow field truncates a product the wide field keeps
// Port caveat: Solidity reverts on overflow; QPI wraps.
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=first stateOrder=declared temporaries=locals width=uint32
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct PromoteMul2
{
};

struct PromoteMul : public ContractBase
{
    struct StateData
    {
        uint64 wide;
        uint32 narrow;
        uint64 calls;
        uint64 placementGuard;
    };

    struct Apply_input
    {
        uint32 a;
        uint32 b;
    };

    struct Apply_output {};

    struct Apply_locals
    {
        uint64 value;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Apply)
    {
        locals.value = input.a * input.b;
        state.mut().wide = locals.value;
        state.mut().narrow = input.a * input.b;
        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 wide;
        uint32 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }

    INITIALIZE()
    {

    }
};
