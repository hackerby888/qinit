// NestedLoopMatrixTrace — ported from test/libsolidity/semanticTests/controlFlow/loops.sol
// Stresses: a triple-nested loop computing a trace and a checksum over a computed matrix — 4^3 iterations of index arithmetic with no container involved
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedLoopMatrixTrace2
{
};

struct NestedLoopMatrixTrace : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
        uint64 runJ;
        uint64 runK;
        uint64 runCell;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 trace;
        uint64 checksum;
        uint64 iterations;
        uint64 diagonalHits;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 trace;
        uint64 checksum;
        uint64 iterations;
        uint64 diagonalHits;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.trace = state.get().inner.trace;
        output.checksum = state.get().inner.checksum;
        output.iterations = state.get().inner.iterations;
        output.diagonalHits = state.get().inner.diagonalHits;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.trace = 0;
        state.mut().inner.checksum = 0;
        state.mut().inner.iterations = 0;
        state.mut().inner.diagonalHits = 0;
        for (state.mut().inner.scratch.runI = 0; state.mut().inner.scratch.runI < 4; state.mut().inner.scratch.runI++)
        {
        for (state.mut().inner.scratch.runJ = 0; state.mut().inner.scratch.runJ < 4; state.mut().inner.scratch.runJ++)
        {
        for (state.mut().inner.scratch.runK = 0; state.mut().inner.scratch.runK < 4; state.mut().inner.scratch.runK++)
        {
        state.mut().inner.scratch.runCell = (state.mut().inner.scratch.runI + 1) * (state.mut().inner.scratch.runJ + 2) * (state.mut().inner.scratch.runK + 3) + input.a;
        state.mut().inner.checksum += state.mut().inner.scratch.runCell;
        state.mut().inner.iterations++;
        if (state.mut().inner.scratch.runI == state.mut().inner.scratch.runJ && state.mut().inner.scratch.runJ == state.mut().inner.scratch.runK)
        {
        state.mut().inner.trace += state.mut().inner.scratch.runCell;
        state.mut().inner.diagonalHits++;
        }
        }
        }
        }

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
