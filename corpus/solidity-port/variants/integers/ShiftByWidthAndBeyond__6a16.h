// ShiftByWidthAndBeyond — ported from test/libsolidity/semanticTests/operators/shifts/shift_overflow.sol
// Stresses: a shift count equal to and greater than the operand width — undefined in C++, defined in Solidity
// Port caveat: Solidity defines `x << n` for every n; C++ leaves n >= width undefined, so a backend disagreement here is expected signal, not a port error.
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=nested stateOrder=reversed temporaries=locals width=uint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ShiftByWidthAndBeyond2
{
};

struct ShiftByWidthAndBeyond : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 accumulator;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint8 value;
        uint8 amount;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.accumulator = state.get().inner.accumulator;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        uint8 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.scratch = input.value << input.amount;
        state.mut().inner.accumulator += (uint64)locals.scratch;
        locals.scratch = input.value >> input.amount;
        state.mut().inner.accumulator += (uint64)locals.scratch;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.accumulator = 0;
        state.mut().inner.calls = 0;
    }
};
