// NegationOfUnsignedWidths — ported from test/libsolidity/semanticTests/types/negative_literal_unsigned.sol
// Stresses: unary minus applied to unsigned values at three widths — the wrap that Solidity forbids and C++ defines, recorded at each width
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NegationOfUnsignedWidths2
{
};

struct NegationOfUnsignedWidths : public ContractBase
{
    struct Scratch
    {
        uint8 runAsByte;
        uint16 runAsWord;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 negatedByte;
        uint64 negatedWord;
        uint64 negatedWide;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().inner.scratch.runAsByte = (uint8)input.a;
        state.mut().inner.scratch.runAsWord = (uint16)input.a;
        state.mut().inner.negatedByte = (uint64)(uint8)(0 - state.mut().inner.scratch.runAsByte);
        state.mut().inner.negatedWord = (uint64)(uint16)(0 - state.mut().inner.scratch.runAsWord);
        state.mut().inner.negatedWide = 0 - input.a;

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 negatedByte;
        uint64 negatedWord;
        uint64 negatedWide;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.negatedByte = state.get().inner.negatedByte;
        output.negatedWord = state.get().inner.negatedWord;
        output.negatedWide = state.get().inner.negatedWide;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
