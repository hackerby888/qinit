// CollatzStepCount — ported from no Solidity analogue (Collatz)
// Stresses: the Collatz sequence with a step cap — a data-dependent loop whose trip count no compiler can fold, and whose peak value is where the multiply can wrap
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CollatzStepCount2
{
};

struct CollatzStepCount : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 capped;
        uint64 reachedOne;
        uint64 peak;
        uint64 steps;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint64 value;
        uint64 count;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.value = input.a == 0 ? 1 : input.a;
        locals.count = 0;
        state.mut().peak = locals.value;
        while (locals.value != 1 && locals.count < 200)
        {
        if (QPI::mod(locals.value, 2ULL) == 0)
        {
        locals.value = QPI::div(locals.value, 2ULL);
        }
        else
        {
        locals.value = locals.value * 3 + 1;
        }
        if (locals.value > state.get().peak)
        {
        state.mut().peak = locals.value;
        }
        locals.count++;
        }
        state.mut().steps = locals.count;
        state.mut().reachedOne = locals.value == 1 ? 1 : 0;
        state.mut().capped = locals.count == 200 ? 1 : 0;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 steps;
        uint64 peak;
        uint64 reachedOne;
        uint64 capped;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.steps = state.get().steps;
        output.peak = state.get().peak;
        output.reachedOne = state.get().reachedOne;
        output.capped = state.get().capped;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
