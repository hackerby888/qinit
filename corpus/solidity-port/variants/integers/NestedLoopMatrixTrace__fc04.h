// NestedLoopMatrixTrace — ported from test/libsolidity/semanticTests/controlFlow/loops.sol
// Stresses: a triple-nested loop computing a trace and a checksum over a computed matrix — 4^3 iterations of index arithmetic with no container involved
// Variant: entryOrder=reversed entryShape=direct initStyle=full placement=nested stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedLoopMatrixTrace2
{
};

struct NestedLoopMatrixTrace : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        uint64 diagonalHits;
        uint64 iterations;
        uint64 checksum;
        uint64 trace;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint64 i;
        uint64 j;
        uint64 k;
        uint64 cell;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        state.mut().inner.trace = 0;
        state.mut().inner.checksum = 0;
        state.mut().inner.iterations = 0;
        state.mut().inner.diagonalHits = 0;
        for (locals.i = 0; locals.i < 4; locals.i++)
        {
        for (locals.j = 0; locals.j < 4; locals.j++)
        {
        for (locals.k = 0; locals.k < 4; locals.k++)
        {
        locals.cell = (locals.i + 1) * (locals.j + 2) * (locals.k + 3) + input.a;
        state.mut().inner.checksum += locals.cell;
        state.mut().inner.iterations++;
        if (locals.i == locals.j && locals.j == locals.k)
        {
        state.mut().inner.trace += locals.cell;
        state.mut().inner.diagonalHits++;
        }
        }
        }
        }

        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 trace;
        uint64 checksum;
        uint64 iterations;
        uint64 diagonalHits;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.trace = state.get().inner.trace;
        output.checksum = state.get().inner.checksum;
        output.iterations = state.get().inner.iterations;
        output.diagonalHits = state.get().inner.diagonalHits;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.trace = 0;
        state.mut().inner.checksum = 0;
        state.mut().inner.iterations = 0;
        state.mut().inner.diagonalHits = 0;
        state.mut().inner.calls = 0;
    }
};
