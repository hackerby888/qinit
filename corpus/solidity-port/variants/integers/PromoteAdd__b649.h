// PromoteAdd — ported from test/libsolidity/semanticTests/arithmetics/checked_add_v2.sol
// Stresses: integer promotion on +: both operands widen to int, so the wide and narrow fields disagree for uint8/uint16
// Port caveat: Solidity 0.8 reverts on overflow (`FAILURE, hex"4e487b71", 0x11`); QPI wraps, so the wrapped value is asserted instead.
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=nested stateOrder=reversed temporaries=locals width=sint32
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct PromoteAdd2
{
};

struct PromoteAdd : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        sint32 narrow;
        sint64 wide;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Apply_input
    {
        sint32 a;
        sint32 b;
    };

    struct Apply_output {};

    struct Apply_locals
    {
        sint64 value;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Apply)
    {
        locals.value = input.a + input.b;
        state.mut().inner.wide = locals.value;
        state.mut().inner.narrow = input.a + input.b;
        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint32 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().inner.wide;
        output.narrow = state.get().inner.narrow;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.wide = 0;
        state.mut().inner.narrow = 0;
        state.mut().inner.calls = 0;
    }
};
