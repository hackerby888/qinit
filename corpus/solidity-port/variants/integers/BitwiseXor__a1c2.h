// BitwiseXor — ported from test/libsolidity/semanticTests/operators/bitwise.sol
// Stresses: ^ across widths
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=last stateOrder=declared temporaries=stateScratch width=uint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BitwiseXor2
{
};

struct BitwiseXor : public ContractBase
{
    struct Scratch
    {
        uint64 applyValue;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 wide;
        uint8 narrow;
        uint64 calls;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 wide;
        uint8 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.calls = state.get().calls;
    }

    struct Apply_input
    {
        uint8 a;
        uint8 b;
    };

    struct Apply_output {};

    PUBLIC_PROCEDURE(Apply)
    {
        state.mut().scratch.applyValue = input.a ^ input.b;
        state.mut().wide = state.mut().scratch.applyValue;
        state.mut().narrow = input.a ^ input.b;
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }
};
