// DivQpi — ported from test/libsolidity/semanticTests/arithmetics/divisiod_by_zero.sol
// Stresses: QPI::div truncation toward zero, including the signed cases
// Port caveat: Solidity panics 0x12 on a zero divisor; QPI::div returns 0. Covered by DivByZero; here the divisor is non-zero.
// Variant: entryOrder=reversed entryShape=direct initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch width=sint16
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct DivQpi2
{
};

struct DivQpi : public ContractBase
{
    struct Scratch
    {
        sint64 applyValue;
    };

    struct StateData
    {
        uint64 calls;
        sint16 narrow;
        sint64 wide;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint16 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.calls = state.get().calls;
    }

    struct Apply_input
    {
        sint16 a;
        sint16 b;
    };

    struct Apply_output {};

    PUBLIC_PROCEDURE(Apply)
    {
        state.mut().scratch.applyValue = QPI::div(input.a, input.b);
        state.mut().wide = state.mut().scratch.applyValue;
        state.mut().narrow = QPI::div(input.a, input.b);
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }
};
