// NegationOfUnsignedWidths — ported from test/libsolidity/semanticTests/types/negative_literal_unsigned.sol
// Stresses: unary minus applied to unsigned values at three widths — the wrap that Solidity forbids and C++ defines, recorded at each width
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NegationOfUnsignedWidths2
{
};

struct NegationOfUnsignedWidths : public ContractBase
{
    struct Scratch
    {
        uint8 runAsByte;
        uint16 runAsWord;
    };

    struct StateData
    {
        uint64 calls;
        uint64 negatedWide;
        uint64 negatedWord;
        uint64 negatedByte;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 negatedByte;
        uint64 negatedWord;
        uint64 negatedWide;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.negatedByte = state.get().negatedByte;
        output.negatedWord = state.get().negatedWord;
        output.negatedWide = state.get().negatedWide;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runAsByte = (uint8)input.a;
        state.mut().scratch.runAsWord = (uint16)input.a;
        state.mut().negatedByte = (uint64)(uint8)(0 - state.mut().scratch.runAsByte);
        state.mut().negatedWord = (uint64)(uint16)(0 - state.mut().scratch.runAsWord);
        state.mut().negatedWide = 0 - input.a;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
