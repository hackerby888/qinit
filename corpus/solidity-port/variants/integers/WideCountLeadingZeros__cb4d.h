// WideCountLeadingZeros — ported from openzeppelin-contracts/contracts/utils/math/Math.sol (log2)
// Stresses: leading-zero count by binary search over the word, next to the shift-until-zero loop, with both results stored
// Port caveat: OpenZeppelin's log2 is over uint256; the port is the same ladder over 64 bits.
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct WideCountLeadingZeros2
{
};

struct WideCountLeadingZeros : public ContractBase
{
    struct Scratch
    {
        uint64 runValue;
        uint64 runZeros;
        uint64 runShift;
        uint64 runCounted;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 binarySearch;
        uint64 loopCount;
        uint64 disagreements;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 binarySearch;
        uint64 loopCount;
        uint64 disagreements;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.binarySearch = state.get().inner.binarySearch;
        output.loopCount = state.get().inner.loopCount;
        output.disagreements = state.get().inner.disagreements;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.scratch.runValue = input.a;
        state.mut().inner.scratch.runZeros = 0;
        if (state.mut().inner.scratch.runValue == 0)
        {
        state.mut().inner.scratch.runZeros = 64;
        }
        else
        {
        state.mut().inner.scratch.runShift = 32;
        while (state.mut().inner.scratch.runShift > 0)
        {
        if ((state.mut().inner.scratch.runValue >> (64 - state.mut().inner.scratch.runShift)) == 0)
        {
        state.mut().inner.scratch.runZeros += state.mut().inner.scratch.runShift;
        state.mut().inner.scratch.runValue = state.mut().inner.scratch.runValue << state.mut().inner.scratch.runShift;
        }
        state.mut().inner.scratch.runShift = state.mut().inner.scratch.runShift >> 1;
        }
        }
        state.mut().inner.binarySearch = state.mut().inner.scratch.runZeros;

        state.mut().inner.scratch.runValue = input.a;
        state.mut().inner.scratch.runCounted = 0;
        while (state.mut().inner.scratch.runCounted < 64 && (state.mut().inner.scratch.runValue & 9223372036854775808ULL) == 0)
        {
        state.mut().inner.scratch.runValue = state.mut().inner.scratch.runValue << 1;
        state.mut().inner.scratch.runCounted++;
        }
        state.mut().inner.loopCount = state.mut().inner.scratch.runCounted;
        if (state.mut().inner.scratch.runCounted != state.mut().inner.scratch.runZeros)
        {
        state.mut().inner.disagreements++;
        }

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
