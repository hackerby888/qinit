// MathSqrtNewton — ported from OpenZeppelin utils/math/Math.sol (`sqrt`)
// Stresses: an msb estimate followed by a fixed number of Newton steps, each `xn = (xn + div(a, xn)) / 2` — a loop whose every iteration divides
// Port caveat: OpenZeppelin recurses in places; QPI forbids recursion, so the six Newton steps are unrolled into a bounded loop.
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full loopShape=clamped placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct MathSqrtNewton2
{
};

struct MathSqrtNewton : public ContractBase
{
    struct Scratch
    {
        uint64 runEstimate;
        uint64 runI;
        uint64 runNext;
    };

    struct StateData
    {
        uint64 exact;
        uint64 squared;
        uint64 root;
        uint64 calls;
        uint64 accumulator;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 value;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.accumulator = state.get().accumulator;
        output.calls = state.get().calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        if (input.value == 0)
        {
        state.mut().root = 0;
        state.mut().squared = 0;
        state.mut().exact = 1;
        }
        else
        {
        state.mut().scratch.runEstimate = 1;
        for (state.mut().scratch.runI = 0; state.mut().scratch.runI < 32; state.mut().scratch.runI++)
        {
        if ((state.mut().scratch.runEstimate * state.mut().scratch.runEstimate) < input.value)
        {
        state.mut().scratch.runEstimate <<= 1;
        }
        }
        for (state.mut().scratch.runI = 0; state.mut().scratch.runI < 8; state.mut().scratch.runI++)
        {
        state.mut().scratch.runNext = QPI::div(state.mut().scratch.runEstimate + QPI::div(input.value, state.mut().scratch.runEstimate), 2ULL);
        state.mut().scratch.runEstimate = state.mut().scratch.runNext;
        }
        state.mut().root = state.mut().scratch.runEstimate;
        state.mut().squared = state.mut().scratch.runEstimate * state.mut().scratch.runEstimate;
        state.mut().exact = (state.get().squared == input.value) ? 1 : 0;
        }
        state.mut().accumulator = state.get().root;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().accumulator = 0;
        state.mut().calls = 0;
    }
};
