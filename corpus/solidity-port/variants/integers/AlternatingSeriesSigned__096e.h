// AlternatingSeriesSigned — ported from test/libsolidity/semanticTests/arithmetics/signed_accumulation.sol
// Stresses: an alternating signed series whose partial sums cross zero repeatedly — the sign flips are counted, and the total must land where the closed form says
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct AlternatingSeriesSigned2
{
};

struct AlternatingSeriesSigned : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
        uint64 runBounded;
        sint64 runRunning;
        sint64 runTerm;
    };

    struct StateData
    {
        uint64 placementGuard;
        sint64 total;
        uint64 crossings;
        sint64 maximum;
        sint64 minimum;
        uint64 calls;
        Scratch scratch;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 total;
        uint64 crossings;
        sint64 maximum;
        sint64 minimum;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().scratch.runBounded = input.a > 32 ? 32 : input.a;
        state.mut().scratch.runRunning = 0;
        state.mut().crossings = 0;
        state.mut().maximum = 0;
        state.mut().minimum = 0;
        for (state.mut().scratch.runI = 1; state.mut().scratch.runI <= state.mut().scratch.runBounded; state.mut().scratch.runI++)
        {
        state.mut().scratch.runTerm = QPI::mod(state.mut().scratch.runI, 2ULL) == 1 ? (sint64)state.mut().scratch.runI : (0 - (sint64)state.mut().scratch.runI);
        if ((state.mut().scratch.runRunning < 0) != (state.mut().scratch.runRunning + state.mut().scratch.runTerm < 0))
        {
        state.mut().crossings++;
        }
        state.mut().scratch.runRunning += state.mut().scratch.runTerm;
        if (state.mut().scratch.runRunning > state.get().maximum)
        {
        state.mut().maximum = state.mut().scratch.runRunning;
        }
        if (state.mut().scratch.runRunning < state.get().minimum)
        {
        state.mut().minimum = state.mut().scratch.runRunning;
        }
        }
        state.mut().total = state.mut().scratch.runRunning;

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.total = state.get().total;
        output.crossings = state.get().crossings;
        output.maximum = state.get().maximum;
        output.minimum = state.get().minimum;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
