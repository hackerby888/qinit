// CastDownAndBackUp — ported from test/libsolidity/semanticTests/cleanup/cleanup_in_compound_assign.sol
// Stresses: a value narrowed to 8, 16 and 32 bits and widened straight back, so each round trip records exactly how much was lost
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=last stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CastDownAndBackUp2
{
};

struct CastDownAndBackUp : public ContractBase
{
    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 lossyCount;
        uint64 viaDouble;
        uint64 viaWord;
        uint64 viaByte;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint8 asByte;
        uint16 asWord;
        uint32 asDouble;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.asByte = (uint8)input.a;
        locals.asWord = (uint16)input.a;
        locals.asDouble = (uint32)input.a;
        state.mut().viaByte = (uint64)locals.asByte;
        state.mut().viaWord = (uint64)locals.asWord;
        state.mut().viaDouble = (uint64)locals.asDouble;
        if (state.get().viaByte != input.a)
        {
        state.mut().lossyCount++;
        }

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 viaByte;
        uint64 viaWord;
        uint64 viaDouble;
        uint64 lossyCount;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.viaByte = state.get().viaByte;
        output.viaWord = state.get().viaWord;
        output.viaDouble = state.get().viaDouble;
        output.lossyCount = state.get().lossyCount;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
