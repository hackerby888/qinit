// SignedOverflowDetection — ported from openzeppelin-contracts/contracts/utils/math/Math.sol (tryAdd, trySub)
// Stresses: checked signed addition and subtraction written the way a library writes them — the guard is a comparison against the extremes, computed before the operation rather than after
// Variant: entryOrder=reversed entryShape=direct initStyle=full placement=first stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedOverflowDetection2
{
};

struct SignedOverflowDetection : public ContractBase
{
    struct Scratch
    {
        sint64 runLhs;
        sint64 runRhs;
    };

    struct StateData
    {
        sint64 sum;
        sint64 difference;
        uint64 addOverflows;
        uint64 subOverflows;
        uint64 calls;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runLhs = (sint64)input.a;
        state.mut().scratch.runRhs = (sint64)input.b;
        state.mut().addOverflows = 0;
        state.mut().subOverflows = 0;
        if (state.mut().scratch.runRhs > 0 && state.mut().scratch.runLhs > 9223372036854775807LL - state.mut().scratch.runRhs)
        {
        state.mut().addOverflows = 1;
        state.mut().sum = 9223372036854775807LL;
        }
        else if (state.mut().scratch.runRhs < 0 && state.mut().scratch.runLhs < (0 - 9223372036854775807LL) - 1 - state.mut().scratch.runRhs)
        {
        state.mut().addOverflows = 1;
        state.mut().sum = (0 - 9223372036854775807LL) - 1;
        }
        else
        {
        state.mut().sum = state.mut().scratch.runLhs + state.mut().scratch.runRhs;
        }
        if (state.mut().scratch.runRhs < 0 && state.mut().scratch.runLhs > 9223372036854775807LL + state.mut().scratch.runRhs)
        {
        state.mut().subOverflows = 1;
        state.mut().difference = 9223372036854775807LL;
        }
        else
        {
        state.mut().difference = state.mut().scratch.runLhs - state.mut().scratch.runRhs;
        }

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 sum;
        sint64 difference;
        uint64 addOverflows;
        uint64 subOverflows;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.sum = state.get().sum;
        output.difference = state.get().difference;
        output.addOverflows = state.get().addOverflows;
        output.subOverflows = state.get().subOverflows;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().sum = 0;
        state.mut().difference = 0;
        state.mut().addOverflows = 0;
        state.mut().subOverflows = 0;
        state.mut().calls = 0;
    }
};
