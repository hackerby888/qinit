// MathSaturatingAddSub — ported from OpenZeppelin utils/math/Math.sol (`saturatingAdd`, `saturatingSub`)
// Stresses: overflow detection by `c >= a` — an idiom that is only correct because wrap is defined, and which a compiler that optimises the check away would break
// Port caveat: Solidity 0.8 needs `unchecked` for the wrap the check depends on; QPI wraps natively.
// Variant: placement=nested temporaries=stateScratch width=uint32
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct MathSaturatingAddSub2
{
};

struct MathSaturatingAddSub : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 accumulator;
        uint64 calls;
        uint32 added;
        uint32 subtracted;
        uint64 saturations;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().inner.accumulator;
        output.calls = state.get().inner.calls;
    }

    struct Run_input
    {
        uint32 a;
        uint32 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        uint32 sum;
        uint32 difference;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.sum = input.a + input.b;
        if (locals.sum < input.a)
        {
        locals.sum = 4294967295;
        state.mut().inner.saturations++;
        }
        locals.difference = input.a - input.b;
        if (input.a < input.b)
        {
        locals.difference = 0;
        state.mut().inner.saturations++;
        }
        state.mut().inner.added = locals.sum;
        state.mut().inner.subtracted = locals.difference;
        state.mut().inner.accumulator = (uint64)locals.sum;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.accumulator = 0;
        state.mut().inner.calls = 0;
    }
};
