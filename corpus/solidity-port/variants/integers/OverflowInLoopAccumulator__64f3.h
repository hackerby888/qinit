// OverflowInLoopAccumulator — ported from test/libsolidity/semanticTests/arithmetics/overflow_in_loop.sol
// Stresses: an accumulator wrapping inside a bounded loop, where the trip count decides how far past the wrap it lands
// Variant: entryOrder=reversed entryShape=direct initStyle=empty loopShape=constant placement=last stateOrder=reversed temporaries=stateScratch width=sint64
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct OverflowInLoopAccumulator2
{
};

struct OverflowInLoopAccumulator : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
    };

    struct StateData
    {
        uint64 placementGuard;
        sint64 total;
        uint64 calls;
        uint64 accumulator;
        Scratch scratch;
    };

    struct Run_input
    {
        uint64 rounds;
        uint64 step;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        for (state.mut().scratch.runI = 0; state.mut().scratch.runI < input.rounds; state.mut().scratch.runI++)
        {
        state.mut().total += (sint64)input.step;
        }
        state.mut().accumulator = (uint64)state.get().total;

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().accumulator;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
