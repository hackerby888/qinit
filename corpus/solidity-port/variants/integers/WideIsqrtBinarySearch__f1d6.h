// WideIsqrtBinarySearch — ported from openzeppelin-contracts/contracts/utils/math/Math.sol (sqrt)
// Stresses: integer square root by binary search over the bit positions, checked against squaring the answer — where an off-by-one in the bound leaves a result one too high
// Port caveat: OpenZeppelin's sqrt is Newton's method over uint256 with a seed from log2; the port is the bit-by-bit variant over 64 bits, chosen because it has no division at all.
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=absent placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct WideIsqrtBinarySearch2
{
};

struct WideIsqrtBinarySearch : public ContractBase
{
    struct Scratch
    {
        uint64 runValue;
        uint64 runRoot;
        uint64 runBit;
        uint64 runTrial;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 root;
        uint64 remainder;
        uint64 exact;
        uint64 offByOne;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 root;
        uint64 remainder;
        uint64 exact;
        uint64 offByOne;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.root = state.get().inner.root;
        output.remainder = state.get().inner.remainder;
        output.exact = state.get().inner.exact;
        output.offByOne = state.get().inner.offByOne;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.scratch.runValue = input.a;
        state.mut().inner.scratch.runRoot = 0;
        state.mut().inner.scratch.runBit = 4611686018427387904ULL;
        while (state.mut().inner.scratch.runBit > state.mut().inner.scratch.runValue)
        {
        state.mut().inner.scratch.runBit = state.mut().inner.scratch.runBit >> 2;
        }
        while (state.mut().inner.scratch.runBit != 0)
        {
        state.mut().inner.scratch.runTrial = state.mut().inner.scratch.runRoot + state.mut().inner.scratch.runBit;
        state.mut().inner.scratch.runRoot = state.mut().inner.scratch.runRoot >> 1;
        if (state.mut().inner.scratch.runValue >= state.mut().inner.scratch.runTrial)
        {
        state.mut().inner.scratch.runValue -= state.mut().inner.scratch.runTrial;
        state.mut().inner.scratch.runRoot += state.mut().inner.scratch.runBit;
        }
        state.mut().inner.scratch.runBit = state.mut().inner.scratch.runBit >> 2;
        }
        state.mut().inner.root = state.mut().inner.scratch.runRoot;
        state.mut().inner.remainder = state.mut().inner.scratch.runValue;
        if (state.mut().inner.scratch.runRoot * state.mut().inner.scratch.runRoot == input.a)
        {
        state.mut().inner.exact++;
        }
        if ((state.mut().inner.scratch.runRoot + 1) * (state.mut().inner.scratch.runRoot + 1) <= input.a)
        {
        state.mut().inner.offByOne++;
        }

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
