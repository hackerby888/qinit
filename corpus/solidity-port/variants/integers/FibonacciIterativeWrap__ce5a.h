// FibonacciIterativeWrap — ported from test/libsolidity/semanticTests/arithmetics/fibonacci.sol
// Stresses: an iterative Fibonacci to a caller-chosen index, where the 94th term is the last that fits — a two-variable rotation the compiler may reorder
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct FibonacciIterativeWrap2
{
};

struct FibonacciIterativeWrap : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
        uint64 runBounded;
        uint64 runA;
        uint64 runB;
        uint64 runNext;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 value;
        uint64 previous;
        uint64 index;
        uint64 wrapped;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 value;
        uint64 previous;
        uint64 index;
        uint64 wrapped;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.scratch.runBounded = input.a > 100 ? 100 : input.a;
        state.mut().inner.scratch.runA = 0;
        state.mut().inner.scratch.runB = 1;
        for (state.mut().inner.scratch.runI = 0; state.mut().inner.scratch.runI < state.mut().inner.scratch.runBounded; state.mut().inner.scratch.runI++)
        {
        state.mut().inner.scratch.runNext = state.mut().inner.scratch.runA + state.mut().inner.scratch.runB;
        if (state.mut().inner.scratch.runNext < state.mut().inner.scratch.runB)
        {
        state.mut().inner.wrapped = 1;
        }
        state.mut().inner.scratch.runA = state.mut().inner.scratch.runB;
        state.mut().inner.scratch.runB = state.mut().inner.scratch.runNext;
        }
        state.mut().inner.value = state.mut().inner.scratch.runA;
        state.mut().inner.previous = state.mut().inner.scratch.runB;
        state.mut().inner.index = state.mut().inner.scratch.runBounded;

        state.mut().inner.calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.value = state.get().inner.value;
        output.previous = state.get().inner.previous;
        output.index = state.get().inner.index;
        output.wrapped = state.get().inner.wrapped;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
