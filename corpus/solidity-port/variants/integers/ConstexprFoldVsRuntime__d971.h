// ConstexprFoldVsRuntime — ported from test/libsolidity/semanticTests/constantEvaluator/rounding.sol
// Stresses: the same expression constant-folded and computed at runtime — a fold that disagrees with the emitted code is silent
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=last stateOrder=reversed temporaries=stateScratch width=uint16
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ConstexprFoldVsRuntime2
{
};

struct ConstexprFoldVsRuntime : public ContractBase
{
    struct Scratch
    {
        uint16 runScratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 agree;
        uint16 computed;
        uint16 folded;
        uint64 calls;
        uint64 accumulator;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 accumulator;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.accumulator = state.get().accumulator;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint16 a;
        uint16 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().folded = (uint16)(65535 + 3);
        state.mut().scratch.runScratch = input.a + input.b;
        state.mut().computed = state.mut().scratch.runScratch;
        state.mut().agree = (state.get().folded == state.get().computed) ? 1 : 0;
        state.mut().accumulator += state.get().agree;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
