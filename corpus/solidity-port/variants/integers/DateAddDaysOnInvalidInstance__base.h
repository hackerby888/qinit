// DateAddDaysOnInvalidInstance — ported from test/libsolidity/semanticTests/various/date_time.sol (guarding an uninitialised date)
// Stresses: add() checks isValid() before doing anything, so a default-constructed DateAndTime refuses every day arithmetic — the one guard that fires before any mutation
// Variant: base
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct DateAddDaysOnInvalidInstance2
{
};

struct DateAddDaysOnInvalidInstance : public ContractBase
{
    struct StateData
    {
        uint64 ok;
        uint64 year;
        uint64 month;
        uint64 day;
        uint64 calls;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 ok;
        uint64 year;
        uint64 month;
        uint64 day;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.ok = state.get().ok;
        output.year = state.get().year;
        output.month = state.get().month;
        output.day = state.get().day;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        DateAndTime moment;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        state.mut().ok = locals.moment.addDays(input.a) ? 1 : 0;
        state.mut().year = locals.moment.getYear();
        state.mut().month = locals.moment.getMonth();
        state.mut().day = locals.moment.getDay();

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().ok = 0;
        state.mut().year = 0;
        state.mut().month = 0;
        state.mut().day = 0;
        state.mut().calls = 0;
    }
};
