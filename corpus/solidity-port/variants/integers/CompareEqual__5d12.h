// CompareEqual — ported from test/libsolidity/semanticTests/operators/comparison.sol
// Stresses: == after promotion, including a narrow value compared against a wide one
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch width=sint32
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CompareEqual2
{
};

struct CompareEqual : public ContractBase
{
    struct Scratch
    {
        sint64 applyValue;
    };

    struct StateData
    {
        uint64 calls;
        sint32 narrow;
        sint64 wide;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Apply_input
    {
        sint32 a;
        sint32 b;
    };

    struct Apply_output {};

    PUBLIC_PROCEDURE(Apply)
    {
        state.mut().scratch.applyValue = (input.a == input.b) ? 1 : 0;
        state.mut().wide = state.mut().scratch.applyValue;
        state.mut().narrow = (input.a == input.b) ? 1 : 0;
        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint32 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }
};
