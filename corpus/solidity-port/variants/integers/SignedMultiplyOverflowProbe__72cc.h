// SignedMultiplyOverflowProbe — ported from openzeppelin-contracts/contracts/utils/math/Math.sol (tryMul)
// Stresses: signed multiplication with the division check libraries use to detect overflow, on operands that reach the extremes in both signs
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=last stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedMultiplyOverflowProbe2
{
};

struct SignedMultiplyOverflowProbe : public ContractBase
{
    struct Scratch
    {
        sint64 runLhs;
        sint64 runRhs;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 calls;
        uint64 exact;
        sint64 recovered;
        uint64 overflowed;
        sint64 product;
        Scratch scratch;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 product;
        uint64 overflowed;
        sint64 recovered;
        uint64 exact;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().scratch.runLhs = (sint64)input.a;
        state.mut().scratch.runRhs = (sint64)input.b;
        state.mut().product = state.mut().scratch.runLhs * state.mut().scratch.runRhs;
        if (state.mut().scratch.runLhs == 0)
        {
        state.mut().overflowed = 0;
        state.mut().recovered = 0;
        state.mut().exact = 1;
        }
        else
        {
        state.mut().recovered = QPI::div(state.get().product, state.mut().scratch.runLhs);
        state.mut().overflowed = state.get().recovered != state.mut().scratch.runRhs ? 1 : 0;
        state.mut().exact = state.get().recovered == state.mut().scratch.runRhs ? 1 : 0;
        }

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.product = state.get().product;
        output.overflowed = state.get().overflowed;
        output.recovered = state.get().recovered;
        output.exact = state.get().exact;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().product = 0;
        state.mut().overflowed = 0;
        state.mut().recovered = 0;
        state.mut().exact = 0;
        state.mut().calls = 0;
    }
};
