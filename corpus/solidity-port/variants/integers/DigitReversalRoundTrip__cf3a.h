// DigitReversalRoundTrip — ported from openzeppelin-contracts/contracts/utils/Strings.sol
// Stresses: reversing a number's decimal digits and reversing again — the round trip only returns the original when it has no trailing zeros, which is the case the archetype counts
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct DigitReversalRoundTrip2
{
};

struct DigitReversalRoundTrip : public ContractBase
{
    struct Scratch
    {
        uint64 runValue;
        uint64 runOut;
        uint64 runCount;
        uint64 runI;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 reversed;
        uint64 doubleReversed;
        uint64 roundTrips;
        uint64 digits;
        uint64 calls;
        Scratch scratch;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 reversed;
        uint64 doubleReversed;
        uint64 roundTrips;
        uint64 digits;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().scratch.runValue = input.a;
        state.mut().scratch.runOut = 0;
        state.mut().scratch.runCount = 0;
        while (state.mut().scratch.runValue != 0 && state.mut().scratch.runCount < 20)
        {
        state.mut().scratch.runOut = state.mut().scratch.runOut * 10 + QPI::mod(state.mut().scratch.runValue, 10ULL);
        state.mut().scratch.runValue = QPI::div(state.mut().scratch.runValue, 10ULL);
        state.mut().scratch.runCount++;
        }
        state.mut().reversed = state.mut().scratch.runOut;
        state.mut().digits = state.mut().scratch.runCount;
        state.mut().scratch.runValue = state.mut().scratch.runOut;
        state.mut().scratch.runOut = 0;
        for (state.mut().scratch.runI = 0; state.mut().scratch.runI < state.mut().scratch.runCount; state.mut().scratch.runI++)
        {
        state.mut().scratch.runOut = state.mut().scratch.runOut * 10 + QPI::mod(state.mut().scratch.runValue, 10ULL);
        state.mut().scratch.runValue = QPI::div(state.mut().scratch.runValue, 10ULL);
        }
        state.mut().doubleReversed = state.mut().scratch.runOut;
        state.mut().roundTrips = state.mut().scratch.runOut == input.a ? 1 : 0;

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.reversed = state.get().reversed;
        output.doubleReversed = state.get().doubleReversed;
        output.roundTrips = state.get().roundTrips;
        output.digits = state.get().digits;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().reversed = 0;
        state.mut().doubleReversed = 0;
        state.mut().roundTrips = 0;
        state.mut().digits = 0;
        state.mut().calls = 0;
    }
};
