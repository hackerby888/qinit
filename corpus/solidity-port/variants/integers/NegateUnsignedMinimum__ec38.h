// NegateUnsignedMinimum — ported from test/libsolidity/semanticTests/arithmetics/unary_operators.sol
// Stresses: 0 - x on an unsigned type
// Port caveat: Solidity rejects unary minus on unsigned; `0 - x` is the equivalent wrap.
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=reversed temporaries=stateScratch width=uint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NegateUnsignedMinimum2
{
};

struct NegateUnsignedMinimum : public ContractBase
{
    struct Scratch
    {
        uint8 negateScratch;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 widened;
        uint8 negated;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint8 negated;
        uint64 widened;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.negated = state.get().inner.negated;
        output.widened = state.get().inner.widened;
    }

    struct Negate_input
    {
        uint8 value;
    };

    struct Negate_output {};

    PUBLIC_PROCEDURE(Negate)
    {
        state.mut().inner.scratch.negateScratch = (uint8)0 - input.value;
        state.mut().inner.negated = state.mut().inner.scratch.negateScratch;
        state.mut().inner.widened = (uint64)state.mut().inner.scratch.negateScratch;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Negate, 1);
    }

    INITIALIZE()
    {

    }
};
