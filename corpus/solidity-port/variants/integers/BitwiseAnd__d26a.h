// BitwiseAnd — ported from test/libsolidity/semanticTests/operators/bitwise.sol
// Stresses: & across widths, and the high bits a narrowing store must clear
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=first stateOrder=reversed temporaries=stateScratch width=sint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BitwiseAnd2
{
};

struct BitwiseAnd : public ContractBase
{
    struct Scratch
    {
        sint64 applyValue;
    };

    struct StateData
    {
        uint64 calls;
        sint8 narrow;
        sint64 wide;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint8 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.calls = state.get().calls;
    }

    struct Apply_input
    {
        sint8 a;
        sint8 b;
    };

    struct Apply_output {};

    PUBLIC_PROCEDURE(Apply)
    {
        state.mut().scratch.applyValue = input.a & input.b;
        state.mut().wide = state.mut().scratch.applyValue;
        state.mut().narrow = input.a & input.b;
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }

    INITIALIZE()
    {

    }
};
