// PromoteMul — ported from test/libsolidity/semanticTests/arithmetics/checked_mul.sol
// Stresses: overflow wrap and promotion on *; the narrow field truncates a product the wide field keeps
// Port caveat: Solidity reverts on overflow; QPI wraps.
// Variant: entryOrder=declared entryShape=direct initStyle=empty placement=nested stateOrder=reversed temporaries=stateScratch width=sint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct PromoteMul2
{
};

struct PromoteMul : public ContractBase
{
    struct Scratch
    {
        sint64 applyValue;
    };

    struct StateData
    {
        struct Inner
        {
        uint64 calls;
        sint8 narrow;
        sint64 wide;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Apply_input
    {
        sint8 a;
        sint8 b;
    };

    struct Apply_output {};

    PUBLIC_PROCEDURE(Apply)
    {
        state.mut().inner.scratch.applyValue = input.a * input.b;
        state.mut().inner.wide = state.mut().inner.scratch.applyValue;
        state.mut().inner.narrow = input.a * input.b;
        state.mut().inner.calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint8 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().inner.wide;
        output.narrow = state.get().inner.narrow;
        output.calls = state.get().inner.calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }

    INITIALIZE()
    {

    }
};
