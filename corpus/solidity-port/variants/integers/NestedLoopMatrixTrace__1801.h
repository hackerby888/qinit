// NestedLoopMatrixTrace — ported from test/libsolidity/semanticTests/controlFlow/loops.sol
// Stresses: a triple-nested loop computing a trace and a checksum over a computed matrix — 4^3 iterations of index arithmetic with no container involved
// Variant: entryOrder=reversed entryShape=direct initStyle=full placement=last stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NestedLoopMatrixTrace2
{
};

struct NestedLoopMatrixTrace : public ContractBase
{
    struct Scratch
    {
        uint64 runI;
        uint64 runJ;
        uint64 runK;
        uint64 runCell;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 trace;
        uint64 checksum;
        uint64 iterations;
        uint64 diagonalHits;
        uint64 calls;
        Scratch scratch;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().trace = 0;
        state.mut().checksum = 0;
        state.mut().iterations = 0;
        state.mut().diagonalHits = 0;
        for (state.mut().scratch.runI = 0; state.mut().scratch.runI < 4; state.mut().scratch.runI++)
        {
        for (state.mut().scratch.runJ = 0; state.mut().scratch.runJ < 4; state.mut().scratch.runJ++)
        {
        for (state.mut().scratch.runK = 0; state.mut().scratch.runK < 4; state.mut().scratch.runK++)
        {
        state.mut().scratch.runCell = (state.mut().scratch.runI + 1) * (state.mut().scratch.runJ + 2) * (state.mut().scratch.runK + 3) + input.a;
        state.mut().checksum += state.mut().scratch.runCell;
        state.mut().iterations++;
        if (state.mut().scratch.runI == state.mut().scratch.runJ && state.mut().scratch.runJ == state.mut().scratch.runK)
        {
        state.mut().trace += state.mut().scratch.runCell;
        state.mut().diagonalHits++;
        }
        }
        }
        }

        state.mut().calls++;
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 trace;
        uint64 checksum;
        uint64 iterations;
        uint64 diagonalHits;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.trace = state.get().trace;
        output.checksum = state.get().checksum;
        output.iterations = state.get().iterations;
        output.diagonalHits = state.get().diagonalHits;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().trace = 0;
        state.mut().checksum = 0;
        state.mut().iterations = 0;
        state.mut().diagonalHits = 0;
        state.mut().calls = 0;
    }
};
