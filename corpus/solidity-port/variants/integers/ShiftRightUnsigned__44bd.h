// ShiftRightUnsigned — ported from test/libsolidity/semanticTests/operators/shifts/shift_right.sol
// Stresses: logical right shift on unsigned operands — no sign bit to propagate
// Variant: width=uint16
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ShiftRightUnsigned2
{
};

struct ShiftRightUnsigned : public ContractBase
{
    struct Scratch
    {
        uint16 shiftShifted;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint64 wide;
        uint16 narrow;
        uint64 applied;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 wide;
        uint16 narrow;
        uint64 applied;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
        output.applied = state.get().applied;
    }

    struct Shift_input
    {
        uint16 value;
        uint8 count;
    };

    struct Shift_output {};

    PUBLIC_PROCEDURE(Shift)
    {
        state.mut().scratch.shiftShifted = input.value >> input.count;
        state.mut().narrow = state.mut().scratch.shiftShifted;
        state.mut().wide = (uint64)state.mut().scratch.shiftShifted;
        state.mut().applied++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Shift, 1);
    }

    INITIALIZE()
    {
        state.mut().wide = 0;
        state.mut().narrow = 0;
        state.mut().applied = 0;
    }
};
