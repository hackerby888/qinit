// SignedOverflowDetection — ported from openzeppelin-contracts/contracts/utils/math/Math.sol (tryAdd, trySub)
// Stresses: checked signed addition and subtraction written the way a library writes them — the guard is a comparison against the extremes, computed before the operation rather than after
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=first stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedOverflowDetection2
{
};

struct SignedOverflowDetection : public ContractBase
{
    struct StateData
    {
        uint64 calls;
        uint64 subOverflows;
        uint64 addOverflows;
        sint64 difference;
        sint64 sum;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 sum;
        sint64 difference;
        uint64 addOverflows;
        uint64 subOverflows;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        sint64 lhs;
        sint64 rhs;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.lhs = (sint64)input.a;
        locals.rhs = (sint64)input.b;
        state.mut().addOverflows = 0;
        state.mut().subOverflows = 0;
        if (locals.rhs > 0 && locals.lhs > 9223372036854775807LL - locals.rhs)
        {
        state.mut().addOverflows = 1;
        state.mut().sum = 9223372036854775807LL;
        }
        else if (locals.rhs < 0 && locals.lhs < (0 - 9223372036854775807LL) - 1 - locals.rhs)
        {
        state.mut().addOverflows = 1;
        state.mut().sum = (0 - 9223372036854775807LL) - 1;
        }
        else
        {
        state.mut().sum = locals.lhs + locals.rhs;
        }
        if (locals.rhs < 0 && locals.lhs > 9223372036854775807LL + locals.rhs)
        {
        state.mut().subOverflows = 1;
        state.mut().difference = 9223372036854775807LL;
        }
        else
        {
        state.mut().difference = locals.lhs - locals.rhs;
        }

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.sum = state.get().sum;
        output.difference = state.get().difference;
        output.addOverflows = state.get().addOverflows;
        output.subOverflows = state.get().subOverflows;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
