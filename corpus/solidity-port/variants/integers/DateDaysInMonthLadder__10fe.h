// DateDaysInMonthLadder — ported from test/libsolidity/semanticTests/various/date_time.sol (calendar table lookup)
// Stresses: the month-length table, its February leap-year special case, and the out-of-range guard that returns 0 rather than indexing the array
// Port caveat: The local lookup table shadows the function's own name, so this also exercises the name-resolution path that produced nine findings in earlier rounds.
// Variant: entryOrder=declared entryShape=direct initStyle=full placement=first stateOrder=reversed temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct DateDaysInMonthLadder2
{
};

struct DateDaysInMonthLadder : public ContractBase
{
    struct StateData
    {
        uint64 calls;
        uint64 days;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 days;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.days = state.get().days;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        DateAndTime moment;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        state.mut().days = DateAndTime::daysInMonth(input.a, input.b);
        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().days = 0;
        state.mut().calls = 0;
    }
};
