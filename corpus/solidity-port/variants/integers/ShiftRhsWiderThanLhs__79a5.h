// ShiftRhsWiderThanLhs — ported from test/libsolidity/semanticTests/operators/shifts/shift_left_larger_type.sol
// Stresses: a shift count wider than the operand
// Variant: constSource=constexpr entryOrder=reversed entryShape=viaPrivate initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch width=sint32
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

static constexpr uint8 FIXED_COUNT = 254;

struct ShiftRhsWiderThanLhs2
{
};

struct ShiftRhsWiderThanLhs : public ContractBase
{
    struct Scratch
    {
        sint32 shiftShifted;
    };

    struct StateData
    {
        sint32 narrow;
        sint64 wide;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Shift_input
    {
        sint32 value;
        uint8 count;
    };

    struct Shift_output {};

    struct Shift_locals
    {
        Shift_input forwardedInput;
        Shift_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Shift)
    {
        locals.forwardedInput = input;
        CALL(ShiftBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        sint64 wide;
        sint32 narrow;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using ShiftBody_input = Shift_input;

    using ShiftBody_output = Shift_output;

    PRIVATE_PROCEDURE(ShiftBody)
    {
        state.mut().scratch.shiftShifted = input.value << FIXED_COUNT;
        state.mut().narrow = state.mut().scratch.shiftShifted;
        state.mut().wide = (sint64)state.mut().scratch.shiftShifted;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.wide = state.get().wide;
        output.narrow = state.get().narrow;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Shift, 1);
    }
};
