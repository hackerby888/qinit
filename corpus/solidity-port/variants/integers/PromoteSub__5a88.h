// PromoteSub — ported from test/libsolidity/semanticTests/arithmetics/checked_sub.sol
// Stresses: underflow wrap and promotion on -
// Port caveat: Solidity reverts on underflow; QPI wraps.
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=nested stateOrder=declared temporaries=locals width=uint64
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct PromoteSub2
{
};

struct PromoteSub : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 wide;
        uint64 narrow;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 wide;
        uint64 narrow;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.wide = state.get().inner.wide;
        output.narrow = state.get().inner.narrow;
        output.calls = state.get().inner.calls;
    }

    struct Apply_input
    {
        uint64 a;
        uint64 b;
    };

    struct Apply_output {};

    struct Apply_locals
    {
        uint64 value;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Apply)
    {
        locals.value = input.a - input.b;
        state.mut().inner.wide = locals.value;
        state.mut().inner.narrow = input.a - input.b;
        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Apply, 1);
    }

    INITIALIZE()
    {

    }
};
