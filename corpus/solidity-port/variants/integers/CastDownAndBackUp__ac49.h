// CastDownAndBackUp — ported from test/libsolidity/semanticTests/cleanup/cleanup_in_compound_assign.sol
// Stresses: a value narrowed to 8, 16 and 32 bits and widened straight back, so each round trip records exactly how much was lost
// Variant: entryOrder=reversed entryShape=viaPrivate initStyle=full placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct CastDownAndBackUp2
{
};

struct CastDownAndBackUp : public ContractBase
{
    struct Scratch
    {
        uint8 runAsByte;
        uint16 runAsWord;
        uint32 runAsDouble;
    };

    struct StateData
    {
        uint64 calls;
        uint64 lossyCount;
        uint64 viaDouble;
        uint64 viaWord;
        uint64 viaByte;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint64 viaByte;
        uint64 viaWord;
        uint64 viaDouble;
        uint64 lossyCount;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().scratch.runAsByte = (uint8)input.a;
        state.mut().scratch.runAsWord = (uint16)input.a;
        state.mut().scratch.runAsDouble = (uint32)input.a;
        state.mut().viaByte = (uint64)state.mut().scratch.runAsByte;
        state.mut().viaWord = (uint64)state.mut().scratch.runAsWord;
        state.mut().viaDouble = (uint64)state.mut().scratch.runAsDouble;
        if (state.get().viaByte != input.a)
        {
        state.mut().lossyCount++;
        }

        state.mut().calls++;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.viaByte = state.get().viaByte;
        output.viaWord = state.get().viaWord;
        output.viaDouble = state.get().viaDouble;
        output.lossyCount = state.get().lossyCount;
        output.calls = state.get().calls;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().viaByte = 0;
        state.mut().viaWord = 0;
        state.mut().viaDouble = 0;
        state.mut().lossyCount = 0;
        state.mut().calls = 0;
    }
};
