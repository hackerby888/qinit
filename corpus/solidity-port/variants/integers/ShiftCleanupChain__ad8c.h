// ShiftCleanupChain — ported from operators/shifts/shift_cleanup.sol
// Stresses: compound shift chain on a narrow field
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=first stateOrder=reversed temporaries=locals width=uint8
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct ShiftCleanupChain2
{
};

struct ShiftCleanupChain : public ContractBase
{
    struct StateData
    {
        uint64 witness;
        uint8 value;
        uint64 placementGuard;
    };

    struct Chain_input
    {
        uint8 seed;
        uint8 addend;
    };

    struct Chain_output {};

    struct Chain_locals
    {
        Chain_input forwardedInput;
        Chain_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Chain)
    {
        locals.forwardedInput = input;
        CALL(ChainBody, locals.forwardedInput, locals.forwardedOutput);
    }

    struct Read_input {};

    struct Read_output
    {
        uint8 value;
        uint64 witness;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    using ChainBody_input = Chain_input;

    using ChainBody_output = Chain_output;

    struct ChainBody_locals
    {
        uint64 scratch;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(ChainBody)
    {
        state.mut().value = input.seed;
        state.mut().value += input.addend;
        state.mut().value <<= 8;
        state.mut().value >>= 16;
        locals.scratch = (uint64)state.get().value;
        state.mut().witness = locals.scratch;
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.value = state.get().value;
        output.witness = state.get().witness;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Chain, 1);
    }

    INITIALIZE()
    {
        state.mut().value = 0;
        state.mut().witness = 0;
    }
};
