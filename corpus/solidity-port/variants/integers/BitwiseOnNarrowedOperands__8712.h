// BitwiseOnNarrowedOperands — ported from test/libsolidity/semanticTests/various/bit_operations.sol
// Stresses: and, or, xor and not applied after truncation to eight bits, with the complement stored at both widths so the promotion is visible
// Variant: entryOrder=declared entryShape=direct initStyle=absent placement=first stateOrder=reversed temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BitwiseOnNarrowedOperands2
{
};

struct BitwiseOnNarrowedOperands : public ContractBase
{
    struct Scratch
    {
        uint8 runLhs;
        uint8 runRhs;
    };

    struct StateData
    {
        uint64 calls;
        uint64 notWide;
        uint64 notNarrow;
        uint64 xorValue;
        uint64 orValue;
        uint64 andValue;
        Scratch scratch;
        uint64 placementGuard;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 andValue;
        uint64 orValue;
        uint64 xorValue;
        uint64 notNarrow;
        uint64 notWide;
        uint64 calls;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.andValue = state.get().andValue;
        output.orValue = state.get().orValue;
        output.xorValue = state.get().xorValue;
        output.notNarrow = state.get().notNarrow;
        output.notWide = state.get().notWide;
        output.calls = state.get().calls;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    PUBLIC_PROCEDURE(Run)
    {
        state.mut().scratch.runLhs = (uint8)input.a;
        state.mut().scratch.runRhs = (uint8)input.b;
        state.mut().andValue = (uint64)(uint8)(state.mut().scratch.runLhs & state.mut().scratch.runRhs);
        state.mut().orValue = (uint64)(uint8)(state.mut().scratch.runLhs | state.mut().scratch.runRhs);
        state.mut().xorValue = (uint64)(uint8)(state.mut().scratch.runLhs ^ state.mut().scratch.runRhs);
        state.mut().notNarrow = (uint64)(uint8)(~state.mut().scratch.runLhs);
        state.mut().notWide = ~input.a;

        state.mut().calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }
};
