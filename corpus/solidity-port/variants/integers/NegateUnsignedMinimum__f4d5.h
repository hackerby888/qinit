// NegateUnsignedMinimum — ported from test/libsolidity/semanticTests/arithmetics/unary_operators.sol
// Stresses: 0 - x on an unsigned type
// Port caveat: Solidity rejects unary minus on unsigned; `0 - x` is the equivalent wrap.
// Variant: entryOrder=reversed entryShape=direct initStyle=empty placement=last stateOrder=declared temporaries=stateScratch width=uint16
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct NegateUnsignedMinimum2
{
};

struct NegateUnsignedMinimum : public ContractBase
{
    struct Scratch
    {
        uint16 negateScratch;
    };

    struct StateData
    {
        uint64 placementGuard;
        uint16 negated;
        uint64 widened;
        Scratch scratch;
    };

    struct Read_input {};

    struct Read_output
    {
        uint16 negated;
        uint64 widened;
    };

    PUBLIC_FUNCTION(Read)
    {
        output.negated = state.get().negated;
        output.widened = state.get().widened;
    }

    struct Negate_input
    {
        uint16 value;
    };

    struct Negate_output {};

    PUBLIC_PROCEDURE(Negate)
    {
        state.mut().scratch.negateScratch = (uint16)0 - input.value;
        state.mut().negated = state.mut().scratch.negateScratch;
        state.mut().widened = (uint64)state.mut().scratch.negateScratch;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Negate, 1);
    }

    INITIALIZE()
    {

    }
};
