// BitwiseOnNarrowedOperands — ported from test/libsolidity/semanticTests/various/bit_operations.sol
// Stresses: and, or, xor and not applied after truncation to eight bits, with the complement stored at both widths so the promotion is visible
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=full placement=nested stateOrder=declared temporaries=locals
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct BitwiseOnNarrowedOperands2
{
};

struct BitwiseOnNarrowedOperands : public ContractBase
{
    struct StateData
    {
        struct Inner
        {
        uint64 andValue;
        uint64 orValue;
        uint64 xorValue;
        uint64 notNarrow;
        uint64 notWide;
        uint64 calls;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        uint64 andValue;
        uint64 orValue;
        uint64 xorValue;
        uint64 notNarrow;
        uint64 notWide;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.andValue = state.get().inner.andValue;
        output.orValue = state.get().inner.orValue;
        output.xorValue = state.get().inner.xorValue;
        output.notNarrow = state.get().inner.notNarrow;
        output.notWide = state.get().inner.notWide;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    struct RunBody_locals
    {
        uint8 lhs;
        uint8 rhs;
    };

    PRIVATE_PROCEDURE_WITH_LOCALS(RunBody)
    {
        locals.lhs = (uint8)input.a;
        locals.rhs = (uint8)input.b;
        state.mut().inner.andValue = (uint64)(uint8)(locals.lhs & locals.rhs);
        state.mut().inner.orValue = (uint64)(uint8)(locals.lhs | locals.rhs);
        state.mut().inner.xorValue = (uint64)(uint8)(locals.lhs ^ locals.rhs);
        state.mut().inner.notNarrow = (uint64)(uint8)(~locals.lhs);
        state.mut().inner.notWide = ~input.a;

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {
        state.mut().inner.andValue = 0;
        state.mut().inner.orValue = 0;
        state.mut().inner.xorValue = 0;
        state.mut().inner.notNarrow = 0;
        state.mut().inner.notWide = 0;
        state.mut().inner.calls = 0;
    }
};
