// SignedMinMaxAndAverage — ported from openzeppelin-contracts/contracts/utils/math/SignedMath.sol (min, max, average)
// Stresses: signed min, max and the overflow-free average — the average is `(a & b) + ((a ^ b) >> 1)` for unsigned, and the signed version has to shift arithmetically
// Variant: entryOrder=declared entryShape=viaPrivate initStyle=empty placement=nested stateOrder=declared temporaries=stateScratch
// Generated by scripts/solidity-port/generate.ts. Do not edit; edit the archetype instead.
using namespace QPI;

struct SignedMinMaxAndAverage2
{
};

struct SignedMinMaxAndAverage : public ContractBase
{
    struct Scratch
    {
        sint64 runLhs;
        sint64 runRhs;
    };

    struct StateData
    {
        struct Inner
        {
        sint64 minimum;
        sint64 maximum;
        sint64 average;
        sint64 naiveAverage;
        uint64 calls;
        Scratch scratch;
        };
        uint64 placementGuard;
        Inner inner;
    };

    struct Read_input {};

    struct Read_output
    {
        sint64 minimum;
        sint64 maximum;
        sint64 average;
        sint64 naiveAverage;
        uint64 calls;
    };

    struct Read_locals
    {
        Read_input forwardedInput;
        Read_output forwardedOutput;
    };

    PUBLIC_FUNCTION_WITH_LOCALS(Read)
    {
        CALL(ReadBody, locals.forwardedInput, locals.forwardedOutput);
        output = locals.forwardedOutput;
    }

    struct Run_input
    {
        uint64 a;
        uint64 b;
    };

    struct Run_output {};

    struct Run_locals
    {
        Run_input forwardedInput;
        Run_output forwardedOutput;
    };

    PUBLIC_PROCEDURE_WITH_LOCALS(Run)
    {
        locals.forwardedInput = input;
        CALL(RunBody, locals.forwardedInput, locals.forwardedOutput);
    }

    using ReadBody_input = Read_input;

    using ReadBody_output = Read_output;

    PRIVATE_FUNCTION(ReadBody)
    {
        output.minimum = state.get().inner.minimum;
        output.maximum = state.get().inner.maximum;
        output.average = state.get().inner.average;
        output.naiveAverage = state.get().inner.naiveAverage;
        output.calls = state.get().inner.calls;
    }

    using RunBody_input = Run_input;

    using RunBody_output = Run_output;

    PRIVATE_PROCEDURE(RunBody)
    {
        state.mut().inner.scratch.runLhs = (sint64)input.a;
        state.mut().inner.scratch.runRhs = (sint64)input.b;
        state.mut().inner.minimum = state.mut().inner.scratch.runLhs < state.mut().inner.scratch.runRhs ? state.mut().inner.scratch.runLhs : state.mut().inner.scratch.runRhs;
        state.mut().inner.maximum = state.mut().inner.scratch.runLhs > state.mut().inner.scratch.runRhs ? state.mut().inner.scratch.runLhs : state.mut().inner.scratch.runRhs;
        state.mut().inner.average = (state.mut().inner.scratch.runLhs & state.mut().inner.scratch.runRhs) + ((state.mut().inner.scratch.runLhs ^ state.mut().inner.scratch.runRhs) >> 1);
        state.mut().inner.naiveAverage = QPI::div(state.mut().inner.scratch.runLhs + state.mut().inner.scratch.runRhs, (sint64)2);

        state.mut().inner.calls++;
    }

    REGISTER_USER_FUNCTIONS_AND_PROCEDURES()
    {
        REGISTER_USER_FUNCTION(Read, 1);
        REGISTER_USER_PROCEDURE(Run, 1);
    }

    INITIALIZE()
    {

    }
};
