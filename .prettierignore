# layout is by hand (CODING_RULES.md: no column limit, wrap only long lists); prettier can only wrap to a width or join every break,
# so it formats nothing here and an editor's format-on-save leaves files alone.
*
